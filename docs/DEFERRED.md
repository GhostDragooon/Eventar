# DEFERRED — technical debt with a due date

Every deferral carries a **re-entry criterion** — the observable event that makes it wrong to keep deferring. Reviewed at each sprint boundary (standing ritual). Living tracker, not a phase doc.

_Last updated: 2026-08-14 (prospect/strategy session — 9 rows added below for R1, R2, R5, M1–M4, S1, S9: engineering proposals/recommendations from reviewing the session, NOT Ivan's decisions — see `docs/plans/PROJECT_STATE.md`'s 2026-08-14 section for the settled decisions D-A through D-G from the same session, which are NOT deferrals and are not duplicated here). Previously: 2026-08-13 (2026 dual-track Task 1 — seeded MCHK + 15 HKAM Colleges, closed the empty-`organisation_body_authorisations` gate. Phase-completion review found 4 non-blocking items, all pre-existing and out of Task 1's diff — added below.). Previously: 2026-07-25 (**three-lens phase-completion review of the CPD MVP build** — 3 rows added for what the review found and could NOT fully close in the same pass: the missing organiser↔body authorisation model (grant hole shut, product model deferred), the credit window's generous ±24h bound (real fraud closed, semantically-right bound blocked on the demo fixture), and `credit_ledger.actor_id`'s inability to name a staff actor (worked around via the staff auth-user id). Everything else the review found was fixed outright — see `PROJECT_STATE.md`'s CPD MVP section. Previously: CPD MVP execution plan Stage 7 close-out: 6 rows added for the ⚪ Full-product items the config-free attendance-issuance build deliberately left out — retry-queue/`pending_credit`, `creditIssued` API field, live kill-switch toggle, real-world issuance-rate bound (self-serve accounts), per-body chains + lock-timeout, and cross-cutting UX/perf polish. Previously: 2026-07-18 Milestone A phase-completion review — 4 non-blocking rows from the three-lens review + backtest (`set_staff_role` hardening, `anon` staff-grant footgun, HR11 positive-test gap, `WK-` prefix optics), item 19 closed. Before that: 2026-07-11 external-review reword per the 3a/3b split — see `docs/plans/handoff_10072026.md`)._

| Deferred item | Re-entry criterion | Earliest |
|---|---|---|
| **`NewEventForm.test.tsx` "clears the save confirmation on the next submit attempt" is flaky under full-suite concurrency, clean in isolation** — 2 occurrences across the week prior (`handoff_25072026.md`, which set the "promote to an active fix if it recurs a third time" threshold — not `_v2`, that doc doesn't mention this flake at all) + 2 more directly observed in this session (2026-07-26), crossing the threshold. Diagnosed, not fixed: `onSubmit`'s `setSavedAt(null)` is a plain synchronous setState, but the button's `disabled`/label both key off `useTransition`'s `pending` flag, and the confirmation banner's `setSavedAt(new Date())` is set inside the transition's post-`await` continuation — under CPU pressure from concurrent test-file workers, React's transition scheduler can interleave these renders differently than in isolation. Two credible hypotheses examined (stale `findByRole` resolution vs. transition-continuation batching), neither confirmed — the bug is non-deterministic by nature, so a "fix" attempted without reliable reproduction can't be trusted by TDD's own discipline (can't watch it fail on demand, can't prove a change actually addresses the race vs. got lucky). | Build a reliable repro harness first (e.g. run this file alongside synthetic concurrent load) before attempting a fix, or the flake starts costing more than the investigation would | Next time this file is touched, or it blocks a real gate run |
| **Staff MFA workstream** — password+TOTP backend on `auth.mfa`, `mfa_is_fresh` helper, wrapper freshness-gate wiring, enrolment UI, TOTP-mandatory enforcement | Signing ships; moved wholesale from Sprint 2 (had no Sprint 2 consumer) | **Sprint 4** |
| **Turnstile provisioning + escalating unauth strike ladder** | **Cloudflare keys provisioned** — gates public exposure (ADR Finding 2). Unchanged by Sprint 2 | When keys land |
| **Resend cutover** — real-user OTP + email-template customisation + zh-HK variant | Pre-pilot (before any real attendee cohort) | Sprint 2 exit / Sprint 3 start |
| **CSP enforcement mode + nonce-boundary decision** | A clean real-user violation window in report-only mode (Sprint 2 shipped report-only, allowlist verified against actual app calls — nominatim + Google Fonts) | Sprint 3/4 |
| **Historical registration linking** | Wallet unfreeze (ledger keys on `licence_id`, so not needed for credits) | post-M4 |
| **`request_events` table + pg_cron scoring job** (4 inference signals → flags) | Ships alongside the first authenticated API surface, one sprint after data accrues | Sprint 5 |
| ~~**`Staff.role` TS union widen**~~ ✅ **CLOSED 2026-07-09** — shipped in Sprint 3a Task 2 (`6c31bff`): widened to the 5-role union at all 3 declaration sites, plus the 4 app-layer authorization checks and cascading test fixtures the widen broke. | — | Closed |
| ~~5-role staff enum~~ ✅ **CLOSED 2026-07-09** — shipped in Sprint 3a Task 2 (`6c31bff`): `staff_role_check` widened to `organiser_admin`/`organiser_member`/`body_admin`/`auditor`/`eventar_staff`, the sole live row remapped from `manager`. | — | Closed |
| ~~**P2 shared-vs-separate audit-chain lock decision**~~ ✅ **CLOSED 2026-07-09** — answered in the vault before this tracker caught up: separate chain, own advisory lock (avoids doubling contention with `audit_events` under check-in bursts). See vault `10 — Architecture/Credit Ledger.md` §8.4. | — | Closed |
| **`lib/abuseTier.ts` live-caller wire-up** — the §4 3-in-60 session-revoke capability shipped in Sprint 2 has no live caller; nothing authenticated-attendee-facing exists yet to feed it | First authenticated attendee-facing mutation surface (post-freeze) | Sprint 3+ |
| ~~**Three residual bare-`PUBLIC`/`anon` grant entries** (`self_check_in`, `app_private.require_active_staff`, `public.pseudonymise_user`)~~ ✅ **RESOLVED 2026-07-16** by `20260716233440_grant_hygiene_residual_public` (Milestone A Task 10): `pseudonymise_user` closed to `authenticated`-only, `require_active_staff` explicitly revoked from all PostgREST roles, `self_check_in` re-asserted anon-open by design — all confirmed on live Seoul via the migration's self-verifying DO block + a phase-completion backtest (`has_function_privilege` matrix + `get_advisors` no longer flags `pseudonymise_user` under 0028). | — | Closed |
| **Exit-gate burst-throughput test's 2s P99 threshold occasionally flakes** (~1-in-4 runs) against real network conditions to the remote Seoul project — genuine variance, always well under 3s when it happens, not a functional regression | Ivan's call: loosen the bound, make it informational-only, or measure DB-side timing instead of client-observed latency | Whenever it becomes annoying |
| Audited-function template / codegen | ~12 near-identical definer functions accumulate (gate-drift risk) | Sprint 3/4 |
| **Replay-from-zero + chain/tamper test as a *standing CI check*** — the local-stack replay was run once by hand on 2026-07-10 and immediately found two portability bugs (now fixed: `rls_auto_enable` guard, stale `seed.sql`). But it's not automated: nothing re-runs it, so the next migration that assumes hosted-Supabase state or drifts from the seed would only be caught by hand again. The chain/tamper test (owner-level edit → `verify_*_chain()` catches it) also has no CI home — the *capability* is proven (demonstrated live this session), the *harness* isn't. Both need a CI job that spins a local stack (Docker), `supabase db reset`s, and runs the assertions. Foundation now proven working (local stack comes up clean with the fixes). | **Before the Singapore fresh-provisioning** (a fresh replay must be green first) or when CI is stood up | Before Singapore provisioning / Sprint 4 |
| **[AUDIT-COVERAGE GAP — body-onboarding critical path] `staff` role mutations are unaudited and `service_role`-writable** — found during the Sprint 3a Task 10 service_role sweep. Same *class* as the service_role tamper gap (privilege-layer integrity), but audit-coverage rather than tamper-resistance: `staff` is the role source gating everything (`is_manager()`, every `body_admin`/`eventar_staff` check), yet there's no definer function and no `audit_events` write when a role changes, so "who granted `body_admin`, and when" is recorded **nowhere**. Correctly deferred (no live consumer, no staff-role churn yet — it's a *build*, not a hole), but for a platform selling audit-ready evidence, the privilege-granting layer being the one unaudited mutation surface is exactly what a body's security review asks about first. Build: `set_staff_role()` audited definer fn + the Hard Rule 11 grant posture. **Not undifferentiated backlog — this is on the critical path for the first body enterprise contract**, not merely "later". | **Before the first accrediting-body enterprise/security review** (a body will ask "who can change roles, and is it logged"), or first staff-role churn at scale | Before first body contract |
| **`credit_disputes` has no automated RLS test** (Task 9) — its `ledger_entry_id` is a NOT NULL FK to `credit_ledger`, and after Task 8's C3 hardening `service_role` (the only credential a committed vitest suite has) cannot DELETE `credit_ledger` rows by design. A committed test that creates a real ledger row as a fixture (via `record_credit_entry`, which service_role CAN call) would therefore permanently reside in the live ledger — the exact outcome the branch-testing decision for Task 9's concurrency test was made to avoid. `credit_disputes`' own RLS shape (self-read/self-raise, now with an ownership check on raise) is a well-precedented pattern already covered elsewhere (`consent_records`, `practitioner_licences_self_read`), so the risk is judged low, but it's genuinely untested. | Sprint 3b's real event-transition wiring produces real ledger rows as a natural byproduct (test against those, not synthetic ones), or the Supabase plan upgrades to support branching for CI | Sprint 3b |
| ~~**`credit_ledger`'s hash chain has zero automated test coverage**~~ 🟡 **PARTIALLY CLOSED 2026-07-24** — `tests/cpd/attendance_issuance.rls.test.ts` (CPD MVP Stage 5) now exercises `record_credit_entry`/`verify_ledger_chain` live for the attendance-issuance path specifically (issue, idempotency, a real concurrent race, guard skips) and asserts the chain stays fully valid. The broader hash-chain properties this item originally named (hash-collision safety, GUC-independence, `credit_transferred`/other entry types) are still only manually verified, not in CI — that residual is unchanged. Was (Tasks 8/9): `compute_credit_ledger_hash`/`verify_ledger_chain`/`record_credit_entry` are the actual tamper-evidence mechanism for the regulator-facing ledger, and no committed test exercised any of them (no safe cleanup path against the live ledger — see the next row, which is why this is 🟡 not ✅). | Same as above for the residual — Sprint 3b's real event-transition wiring, or Supabase branch-based CI support | Sprint 3b |
| **`tests/cpd/attendance_issuance.rls.test.ts` leaves permanent residue on every run that actually issues a credit** (Stage 5, 2026-07-24) — confirmed empirically: 3 dev-project runs while building this test left exactly 6 permanent `credit_ledger` rows (2 per full run: the issue-test's credit + the concurrency-race winner). Root cause, found running this file: `credit_ledger.licence_id`/`.user_id`/`.event_id`/`.body_id` are all plain `references` (Postgres default **NO ACTION**), so once a real credit is issued, its licence, practitioner (auth user + `public.users` mirror), event, accrediting-body fixture, AND — one hop further, via `events.created_by -> staff.id`, also NO ACTION — the staff row that created the event ALL become permanently undeletable, not just the ledger row itself (a materially wider blast radius than items above already accepted for `credit_ledger` alone). Regulator-grade property by design (can't erase the entities behind an issued credit); the test's `afterAll` only cleans what carries zero credit reference (documented in the file's header). Same accepted-debt class as the rows above, logged separately because the blast radius (auth users + accrediting-body + event + staff fixtures, not just one ledger row) is worth tracking on its own. | Same as above — Supabase branch-based CI (a throwaway branch per run makes residue moot), or accept as permanent dev-project cost | Sprint 3b |
| ~~**`declare_licence` leaks a raw Postgres FK-violation dump for a bad `p_body_id`**~~ ✅ **CLOSED 2026-07-26** — TDD red→green: `tests/audit/licence_mutations.test.ts` now asserts a bogus `p_body_id` fails `P0002` ("accrediting body ... not found"), not a raw FK dump; watched it fail (`23503`) before the fix. `declare_licence` now checks `exists (... accrediting_bodies ...)` before the insert — not the same shape as `verify_licence`/`lapse_licence`/`revoke_licence` (those add an org-ownership check after their not-found check, since a *licence* is scoped to an org's body; a *body* itself has no such ownership dimension for the caller declaring against it), so "mirrors the siblings" means same not-found *mechanism* (`P0002`, no raw constraint text), not an identical function shape. Still not a security leak: `accrediting_bodies` is publicly readable (`accrediting_bodies_public_read_active`), and the function only ever echoes back the UUID the caller supplied — no new information crosses the boundary. Migration `20260726070748_declare_licence_clean_not_found`, applied live to Seoul. 19/19 green, no regressions. | — | Closed |
| **The licence-mutation functions have two different not-found philosophies, not one** (found during the 2026-07-26 backlog review, pre-existing, not a regression) — `verify_licence`/`lapse_licence`/`revoke_licence` (staff actors) return `P0002` for a nonexistent licence id but `42501` for one that exists at a different org, which is a narrow cross-tenant existence oracle (a staff member can distinguish "no such licence" from "exists, not yours" for a guessed UUID). `set_primary_licence`/`supersede_licence` (self actors) deliberately collapse both into a single `42501`, avoiding exactly that oracle. `declare_licence`'s new check (above) is a third shape with no ownership dimension. Nothing forces these to reconcile — the self-actor functions made a real security choice the staff-actor ones didn't visibly consider — worth a deliberate decision, not silent drift. | Before a body's security review asks about this, or next time this file is touched | Sprint 3b |
| ~~**`declare_licence`'s new not-found check has no `status='active'` filter**~~ ✅ **CLOSED 2026-08-14 (Task 10.2/10.9)** — a second, separate check added ahead of the insert: a real-but-non-active `p_body_id` now fails `P0002` with a distinct "not active" message, while a genuinely nonexistent id still hits the original not-found branch unchanged. Landed alongside the primary-per-body index fix and `track`/`cycle_started_on` in `supabase/migrations/20260814200000_declare_licence_active_body_and_primary_per_body.sql`. `tests/audit/licence_mutations.test.ts` — "a real but non-active body fails with P0002 and a distinct 'not active' message (DEFERRED item 30)" — green live on the local stack. | — | Closed |
| **`record_credit_entry` doesn't cross-validate that `p_licence_id` belongs to `p_user_id`/`p_body_id`** (Task 10 user-lens review) — each FK is checked individually, but nothing enforces the three are mutually consistent; a caller could pass a real licence for user X at body Y alongside mismatched `p_user_id`/`p_body_id` and get a self-consistent-looking but wrong ledger row. Low risk today (service_role-only, no live caller), but exactly the kind of landmine Sprint 3b's event-transition wiring could hit silently. | Before Sprint 3b wires a real caller to record_credit_entry | Sprint 3b |
| **`record_credit_entry`'s app-facing role parity with `is_manager()` is not yet extended** (Task 2/10) — `is_manager()` now grants cross-owner event/registration read to `role in ('organiser_admin','eventar_staff')`, but the 4 corresponding app-layer TS authorization checks (`app/dashboard/actions.ts`, `app/(public)/events/[id]/page.tsx`, `app/events/[id]/details/page.tsx`, `app/events/[id]/details/emailActions.ts`) still gate on `role === 'eventar_staff'` only — a 1:1 preservation of the pre-widen behavior, not yet extended to `organiser_admin`. Deliberate minimal-scope call for Sprint 3a (no `organiser_admin` account exists yet; widening would itself touch the frozen frontend beyond the approved carve-out). | First real `organiser_admin` staff account is provisioned (Sprint 3b onboarding) | Sprint 3b |
| **Redundant explicit `pg_advisory_xact_lock(hashtext('audit_events_chain'))` in the 6 licence-mutation functions** (Task 6) — the `compute_audit_hash` BEFORE-INSERT trigger already takes that lock during `write_audit_event`'s insert, so the explicit acquisition is reentrant/redundant. Harmless (same lock, same xact) but a convention break (no other audited fn does it) and a cargo-cult risk into the credit_ledger functions, which use a *different* lock. Left in place (already applied, all tests green); flagged so it isn't copied. | Next time these functions are touched, or a grant/hash-hygiene pass runs | Sprint 3b/4 |
| **Licence-mutation functions are transition-agnostic on `verify`/`lapse`/`revoke`/`set_primary`** (Task 6) — only `supersede_licence` got a from-state guard (Ivan's call, blocks superseding revoked/superseded). The staff-gated ones will e.g. flip a `revoked` licence to `verified` leaving `revoked_at` set. Consistent with `transition_dsr`'s own transition-agnostic precedent; lower risk since staff-gated. Decide whether this family should enforce a real state machine. | Before a body-reviewer workflow relies on status transitions (Sprint 3b) | Sprint 3b |
| **`public.staff` holds column-level UPDATE on (email, full_name, organisation_id, status) for `authenticated` with ZERO UPDATE policies** (2026-08-04 three-lens review, MEDIUM) — RLS denies every write today, so nothing is exploitable, but the grant layer is not holding the line and Hard Rule 11 exists precisely because RLS-as-the-only-control has failed this repo four times. Deliberately NOT revoked in the same pass that revoked `credit_disputes`' resolution fields: those must never be self-writable, whereas these four columns are the natural surface for a future staff self-profile edit, and revoking now would remove a grant that a shipped policy would need. Recorded rather than silently decided either way. | Whenever a staff self-edit surface ships (revoke, then column-grant exactly what the policy allows), or at the Stage 8 grant sweep | Stage 8 |
| Cross-jurisdiction schema polymorphism (bodies, licence types, credit schemes) | AU or 2nd jurisdiction onboards — **migration cost accepted as debt** | Sprint 5+ |
| Practitioner-in-a-week product-flow sketch | Before wallet-unfreeze consideration | Before Sprint 4 |
| External review by real accrediting body + organiser | **Gates Sprint 3b** (the CPD domain: `body_rules`/evaluator/issuance/roster + the governance half) — the 3a/3b split resolved the original wording: 3a's *ungated* foundation incl. the `credit_ledger` core shipped 2026-07-10 without the review, deliberately config-reference-free; the review also resolves Q26 | Before Sprint 3b |
| Bulk-verify endpoint for accreditors | Documented request from an onboarded body | On request |
| W3C Verifiable Credentials + DID:web + Status List 2021 | First accrediting-body request, or verifier-tooling maturity | Phase 2 |
| RFC 3161 TSA anchor (external tamper proof) | First body asks how tampering is externally provable | Phase 2 |
| Behavioural detection graduation (flags → auto-action) | **Locked at "never"** — standing pushback flags any attempt | — |
| ~~7-year retention grounding~~ ✅ **CLOSED 2026-07-09** — no body's CPD retention clause actually reads 7 years; grounded per-body via primary-source citations (GL24 §3.8 = 3yr, Law Society Guideline 10.6 = 2yr, VSB FAQ Q18 = 6yr, PT Board CPD Manual = 6yr, HKICPA Statement 1.500 = 5yr, MPFA Guidelines VI.4 §15(a) = 3yr; HKIE confirmed no stated figure; HKCR unverified). Full detail: vault `10 — Architecture/Credit Ledger.md` §8.5, Decisions Log Q24. | — | Closed |
| Organiser-attested liability T&Cs (HK counsel) | Before first pilot organiser signs a contract with attested-vs-confirmed terms, per Slice 0.11 §6.7 | Before launch |
| ~~**`tests/rls/checkin_throughput.rls.test.ts` grabs "a" staff row via unfiltered `select('id').limit(1).single()`**~~ ✅ **CLOSED 2026-07-26** — root-cause fix: the file now owns a disposable, uniquely-timestamped staff fixture (created in `beforeAll`, deleted in `afterAll`) instead of reading whatever staff row happens to exist, matching every other RLS suite's pattern. Can no longer race another file's staff insert/delete lifecycle. Re-verified green (1/1, 200 concurrent check-ins, P99 < 2s, chain valid). | — | Closed |
| **Systemic: multiple RLS test files' `afterAll` don't check their `staff`/`auth.users` delete errors, and orphaned fixture rows recur under back-to-back `pnpm test:rls` invocations** — first found in `tests/rls/dsr_audited.rls.test.ts` during Sprint 3a Task 2 (`dsr-manager@rls-test.invalid` recurred repeatedly), then again in `tests/rls/mark_attended.rls.test.ts` during Task 5 (`markattended-owner`/`markattended-nonowner@rls-test.invalid`) — different files, same root shape (unchecked `.delete()` in `afterAll`, or a run that aborts before `afterAll` completes under connection/socket pressure from repeated invocations in one session). Each orphan silently blocks the next run's `beforeAll` insert with a `staff_email_org_key`/similar violation until manually deleted via the Supabase MCP. Not blocking any Sprint 3a task so far — always caught and cleaned during that task's own review — but worth a real fix (check delete errors + throw, or a session-scoped cleanup sweep) before it costs more time than it has already. | Recurs a third time, or before Sprint 3b adds more RLS test files that would compound it | Sprint 3b/4 |
| **`users.locale` vs `users.display_language` overlap** — Sprint 1 shipped `locale text not null default 'en'` (no CHECK); Sprint 3a Task 1 added `display_language text not null default 'en' check (in 'en','zh-Hant','zh-Hans')` for the same practitioner-facing-language concern, per Q23 item 6. Neither column is read anywhere in the codebase yet (both dead). Found during Sprint 3a Task 1 code review, not reconciled in Data Model.md. | First feature that reads either column — decide then whether `locale` is superseded (deprecate/drop) or genuinely serves a different purpose (e.g. date/number formatting vs. UI translation) | Sprint 3b/4 |
| **`set_staff_role` residual hardening** (Milestone A Task 9 phase-completion review — dev + security lens) — the function's value-safety rests entirely on the `staff_role_check` CHECK defined elsewhere (a drifted/fresh DB without it turns `set_staff_role` into an arbitrary-role setter, and this repo has known migration-drift history), and there is no separation-of-duties guard: an `eventar_staff` can demote/suspend the last active `eventar_staff` (self-lockout) or mint another `eventar_staff`, with only the audit trail as a control. Not a live boundary break (gate is already the top role; every change is audited; CHECK bounds the value on the live DB — backtested), and org-admin delegation is explicitly deferred (item 23 build shipped, delegation model is Sprint 3b). Build when delegation lands: an in-body role allowlist (clean error vs raw `23514`), a "cannot remove the last active `eventar_staff`" guard, and the matching Zod enum on the future Server Action (three-layer validation). | When the org-admin delegation model lands, or first multi-`eventar_staff` operation | Sprint 3b |
| ~~**`anon` in the `staff` column-UPDATE grant-back**~~ ✅ **CLOSED 2026-07-24** — migration `20260724155945_staff_grant_narrow_anon` applied to live Seoul; `has_column_privilege('anon','public.staff','full_name','UPDATE')` = false confirmed (authenticated + service_role retain). Was (LOW-2 / dev M3): Migration `20260723000000_staff_grant_narrow_anon.sql` written (revokes `update (email, full_name, organisation_id, status)` on `public.staff` from `anon`; keeps `authenticated, service_role`; self-verifying DO block) and `seed.sql` updated to drop `anon` from the grant-back so a local `db reset` doesn't re-widen it. **Not yet applied to live Seoul** — the MCP `apply_migration` write was blocked by the auto-mode classifier; Ivan applies via `supabase db push` (uses the file's timestamp) or by re-authorising the MCP apply. Was dormant regardless (staff RLS is `SELECT`-only, so anon writes are default-denied — backtested), so no live exposure while pending. **Re-entry closes when the migration lands on Seoul and `has_column_privilege('anon','public.staff','full_name','UPDATE')` returns false there.** | Apply the written migration to Seoul | This session's follow-up (code ready) |
| ~~**Hard Rule 11 positive round-trip test missing for `staff`'s retained column grants**~~ ✅ **CLOSED, found already shipped 2026-07-26** — `staff_role_mutations.rls.test.ts` test 5 ("service-role update of a non-role column (full_name) still succeeds") was added in `d110c0e` alongside item 45's anon-grant fix, but never marked closed here. Re-verified live against Seoul this session: 5/5 green, including this exact test. Was: HR11 wants a positive round-trip for retained grants, not just the negative on the locked column — this pairs with test 1's `42501` on `role` to pin the column boundary exactly where intended. | — | Closed |
| **`WK-` (workshop) registration-code prefix visible during CPD/regulator demos** (Milestone A Task 6 user-lens review — cosmetic) — codes are `WK-` + 6 chars (`lib/registrationCode.ts`); the Beat-4 pass shows `WK-XXXXXX` and the staff manual-entry placeholder shows a 4-char `WK-XXXX` (real is 6). A sharp accrediting-body observer may notice "workshop" on a clinical seminar. Branding leak, not a defect; changing the prefix touches the (frozen) real app + existing live codes, so deferred. | Frontend unfreeze / first body demo where optics matter | Sprint 3b+ |
| **CPD issuance retry-queue + `pending_credit` state** (CPD MVP execution plan, component J, ⚪ Full) — today a transient technical failure on the inline award (Stage 3) is logged (`[cpd] award_attendance_credit failed`) and left for `reconcile-event.ts` (Stage 6) to backfill on next manual run. No automatic retry, no queue, no `pending_credit` flag surfaced anywhere. Deliberately not built for the MVP — the reconcile script already closes the gap it would solve, just not automatically/immediately. | Real event volume where a manual reconcile-per-event is no longer practical, or a scheduling substrate (pg_cron) exists to run reconcile automatically | Sprint 3b/4 |
| **`creditIssued` status not surfaced in the attendance API response** (component K, ⚪ Full) — `selfCheckIn`/`markAttended`'s response shape is unchanged by design (Runtime Contract: attendance is authoritative, a credit outcome must never affect it), so nothing in the UI can show "credit posted" vs "credit pending" today. Needs an unfrozen surface to display it meaningfully anyway. | First unfrozen attendee-facing surface that would show this (S-Attendee wallet) | Post-M2 unfreeze |
| **CPD kill switch is deploy-time only** (`CPD_ISSUANCE_ENABLED`, env-based, fails open) — no live mid-event toggle. If issuance needs to be killed *during* a real event, it requires a redeploy (Vercel) or an env change + restart (self-hosted), not a runtime flip. Found + explicitly scoped out during Stage 4's live verification (the kill-switch polarity bug). | A config-row/feature-flag substrate exists for something else and can be reused, or a real event needs a live kill mid-run | Sprint 3b/4, or when it actually bites |
| **Real-world CPD issuance rate is bounded by self-serve identity resolution** (Stage 4/5 finding, architecture doc's "identity reality" note) — `award_attendance_credit` only fires for attendees who already have a real account + a verified licence matching the event's body; today that's exactly one seeded identity (Karen Lau) and, in production, will be close to zero real attendees until self-serve accounts + licence declaration ship (S-Attendee, K1). Config-free issuance is real and correct, but its *coverage* — not its *correctness* — is the open question until then. Roster ingestion (bulk-resolving many attendees against a body's member list, rather than one-at-a-time self-serve) is the other mechanism that would raise coverage, tracked separately as a B3 item. | First self-serve attendee account ships, or roster ingestion lands | Sprint 3b (roster) / post-M2 (self-serve) |
| **Global `credit_ledger` advisory lock has no acquisition timeout; per-body chains deferred** (2nd review, point #11) — every issuance serialises through one global lock, awaited on the check-in request. A spiky arrival distribution at a real live event could queue tail requests behind a held lock. `SET LOCAL lock_timeout` (degrade to `skip` rather than block the scanner) is a cheap, recommended-before-real-events knob, not yet added — the MVP's seeded-demo load profile never approaches this ceiling. Per-body/per-event chains remain the real fix at scale, already deferred at the K2/B3 design level. **✅ CLOSED 2026-08-04** (the timeout half; the chain-split stays deferred) — migration `20260804020000_credit_ledger_lock_timeout`, applied local + Seoul. Measured red→green on the local stack with the chain lock held by a second session: **before**, `record_credit_entry` blocked the full 6s ceiling and was stopped only by an explicit `statement_timeout` (57014) that nothing in the product sets — i.e. unbounded in production; **after**, it returns `55P03` at **1074 ms**. `alter function … set lock_timeout to '1s'` — not `create or replace`, because re-declaring a 14-argument body to change one setting is exactly how two agents clobbered each other earlier the same day (`20260803120000`). **Deliberately NOT caught and rewritten to `skipped:lock_timeout`:** 55P03 propagates past `award_attendance_credit`'s `unique_violation` handler to `lib/cpd/awardAttendanceCredit.ts`'s error path, which logs `console.error` and returns `{status:'failed'}` — the correct classification, since a lock timeout is a *technical* failure meaning "reconcile this", not a business skip that logs at info because nothing is wrong (rule 12). The scanner is never blocked regardless: `checkin/confirm/actions.ts` treats attendance as authoritative and ignores the credit outcome. 1s chosen against the burst gate's P99 < 2s budget. **Per-body/per-event chains remain the real fix at scale — still deferred to Stage 10.** | Chain-split only: at scale, with the B3 chain design | Stage 10 |
| **No organiser↔body authorisation model — an organiser can declare their own event accredited by any body** (2026-07-25 three-lens review, HIGH-1; *partially* closed) — the grant-layer hole is shut (`20260725144446` column-locks `accrediting_body_id`/`cpd_hours` so no app role can write them via PostgREST; only service_role paths and definer RPCs can), and `cpd_hours` is now bounded at 24. What remains open is the **product** question the hole exposed: there is no model of which organisations may legitimately claim a relationship with which accrediting body, so when a real CPD-config surface ships, *something* must authorise the claim. Deliberately NOT invented during the review — registry relationships are a B2 decision requiring the body conversation, not a fix to be guessed at mid-hardening. Was true until 2026-08-02 that nothing could set these columns except the seed script. **🔴 NO LONGER TRUE — RE-ENTRY CRITERION FIRED 2026-08-02.** M2 Stage 3 shipped `set_event_cpd_config()` (migration `20260802022345`), the CPD-config surface this row was waiting for. Verified 2026-08-03 on the local stack: `has_function_privilege('authenticated','set_event_cpd_config','EXECUTE')` = **true**, and an organiser was driven end-to-end through the UI binding their own event to PT_BOARD at 3h. NOT a security regression — the grant-layer hole stays shut (columns still locked; only the audited definer writes them), and the function is role-gated (`organiser_member` excluded), owner-checked inside the body, active-body-checked and capped at 24h. What is live is the **self-assertion**: nothing models which organisation may claim which body. `handoff_02082026.md` §5 acknowledged this in prose ("the organiser still self-asserts. Approval workflow is B3 / Milestone C") but this row was never updated. **✅ CLOSED 2026-08-04** — migration `20260804000000_organisation_body_authorisations`, applied local + Seoul. An explicit allow-list (`organisation_body_authorisations`: organisation × body × status, unique pair) is now checked inside `set_event_cpd_config` against the **event's** organisation, with no `eventar_staff` exception — the body's authority is not ours to override. Only *asserting* a claim is gated; **clearing** stays open, since withdrawing is never an assertion of authority and must survive a body revoking the org. Hard Rule 11 posture: `authenticated` revoked at the grant layer for INSERT/UPDATE/DELETE (proven `42501`, not merely RLS-denied), org-scoped SELECT only, `service_role` retains DML as the seeding path. Backfilled from reality — every (organisation, body) pair already in use on an event became active, so no in-flight accredited event was stranded, but nothing else was blanket-granted, so the gate bites from minute one. TDD red→green: watched it fail as a real `organiser_admin` JWT (`expected undefined to be '42501'` — the call *succeeded* and wrote `accrediting_body_id`, i.e. the hole demonstrated rather than described), 8/8 green after. Test: `tests/cpd/organisation_body_authorisation.rls.test.ts`. **Still deferred to Stage 10:** the body-driven approval workflow (organiser submits → reference number → body confirms/suspends). Rows are seeded service-role-side until then. **⚠️ The first close was INCOMPLETE and the three-lens review caught it the same day:** the gate reads the *event's* `organisation_id`, and `authenticated` held column INSERT+UPDATE on that column while `events_organizer_update_own` constrains only `created_by` — so an organiser could PATCH their own event into the default organisation (which holds every authorisation) and accredit anyway, via raw PostgREST with no Server Action involved. Reproduced live on Seoul as a real `organiser_admin` JWT. Closed by `20260804030000`: the column is revoked from all app roles and a `set_event_organisation` BEFORE INSERT trigger derives it from the creating staff row — which also fixed a latent multi-tenancy bug where EVERY event created through the product landed in the default organisation regardless of tenant. Regression cover: three cases in `tests/cpd/organisation_body_authorisation.rls.test.ts`, watched failing on Seoul before the fix. **The lesson is the fifth instance of the same class in this repo: a control placed one layer above where the write actually happens.** **⚠️ Correction 2026-08-14:** the "backfilled from reality … no in-flight accredited event was stranded" claim above was never actually exercised on Seoul — verified live, `organisation_body_authorisations` sat at **0 rows there** from this migration's apply (2026-08-04) until `20260813000000` seeded 22 rows explicitly (`PROJECT_STATE.md` carried this as an unresolved ⛔ blocker for over a week). Root cause, found 2026-08-14: the 18 events that DID carry `accrediting_body_id` on Seoul were all test-fixture events (`ELIG-*`/`RLS-CPD-*`/`RLS-FREEZE-*` bodies) created by `pnpm test:rls` runs against production (`.env.local` points at Seoul) that **post-date** this migration's one-time backfill — so the backfill's WHERE clause matched zero rows at the moment it ran, not because it no-op'd, but because nothing real existed yet to backfill. The claim was accidentally true (no real event *was* stranded) but unverified, and the mechanism it describes — "backfill from reality inside the migration" — does not reliably work for a table seeded before real usage exists. Do not reuse that pattern for tenant #2 without seeding explicitly, as `20260813000000` now does. | — | Closed |
| **Attendance-credit window is a generous ±24h bound, not the real check-in window** (2026-07-25 review, HIGH-2; *partially* closed) — `award_attendance_credit` now skips `outside_window` (start−24h .. end+24h) and `cancelled` registrations, which kills the actual fraud the review found (never attend, POST the emailed code days later; or check in on a cancelled registration). But 24h is not the semantically right bound — presence should mean the event's own check-in window (`start − CHECKIN_OPEN_MINUTES`). It can't be tightened yet because the demo fixture registers at Beat 3 and checks in at Beat 4 minutes later, ~170 min BEFORE start, and registration closes exactly when the check-in window opens — so the two beats cannot both sit inside a strict window. Underlying reality: `self_check_in` itself still enforces no time window, no `checkin_modes.self_serve` check, and no publication check; the credit guard is compensating for that at one remove. **✅ CLOSED 2026-08-04** — the root cause is fixed rather than the symptom. Migration `20260804010000_self_check_in_lifecycle_guards`, applied local + Seoul: `self_check_in` now enforces publication (+ `deleted_at`), `checkin_modes.self_serve`, the event's own window `[start − CHECKIN_OPEN_MINUTES, end]`, and a cancelled registration — and its UPDATE predicate tightened from `status <> 'attended'` to `status = 'registered'`, which is what let a **cancelled registration be flipped straight to attended**. TDD red→green: all six guard cases returned `'ok'` before the fix (check-in 3h before the window, after the event ended, on a draft event, on a *cancelled* event, with self-serve explicitly off, and on a cancelled registration), 15/15 after — with a deliberate control test asserting the legitimate pre-start case still works, since the reminder QR goes out exactly at `start − 60`. Refusals return **distinct result codes** (`not_open` / `self_serve_off` / `cancelled` / `unavailable`) each mapped to its own attendee copy in `app/(public)/checkin/confirm/actions.ts`; the previous single `default` told an early attendee their registration was invalid. **`award_attendance_credit`'s ±24h bound is deliberately KEPT** as defence in depth — `mark_attended` (staff scan, trusted physically-present actor) is intentionally not time-gated, so that path still needs a backstop. **Surfaced, not fixed:** closing this proved the registration and self-serve check-in windows are *disjoint by design* (`registerForEvent` closes at `min(registration_close_at, start − CHECKIN_OPEN_MINUTES)`), so the demo run-sheet's Beat 3 → Beat 4 sequence only ever worked because the guard was missing — three options recorded in `demo-run-sheet.md` Beat 4, Ivan's call, none implemented. | — | Closed |
| **`credit_ledger.actor_id` cannot reference a staff actor** (2026-07-25 review, MEDIUM-4; *worked around*) — `actor_id` references `public.users(id)`, but `staff.id` is an unrelated `gen_random_uuid()` (zero staff ids exist in `users`), so the execution plan's literal "pass `actorId = staff.id`" would have raised `23503` on every staff-scanned check-in. The staff path now passes the staff member's **auth user id** instead, which is the correct mapping and is already inside the hash envelope. Residual: `staff`↔`auth.users` are matched by email with no FK, so they can drift — verified live that 1 of 5 Seoul staff rows currently has no `public.users` mirror. `award_attendance_credit` degrades attribution to NULL with a `raise warning` rather than costing the practitioner a credit. A real `staff`→`auth.users` FK (or an `actor_staff_id` column, which would need ledger hash versioning) is the durable fix. | The staff↔auth linkage is formalised (likely alongside the org-admin delegation model, DEFERRED item 44), or an audit asks "which operator issued this credit" | Sprint 3b/4 |
| **UX/perf polish deferred with the frontend freeze** (skeleton loaders, caching, optimistic rendering, tooltips — architecture doc's cross-cutting table) — all four bind to S-Attendee/S-Organiser surfaces that can't change before the M2 review. Each already has a native/already-installed target picked (Next `loading.tsx`, Next 16 fetch/`unstable_cache`, React 19 `useOptimistic`, `@base-ui/react` Tooltip) so the unfreeze plan doesn't have to re-research them — no new dependency for any of the four. | M2 frontend-unfreeze review | Post-M2 |
| **`/api/cron/dispatch` has NO trigger — nothing fires the scheduler** (found 2026-08-05 auditing the orchestrator before Stage 8). A `vercel.json` entry existed and was reverted in `72cda80`; no pg_cron job was ever created. The route's own header claimed the Vercel trigger was still shipped — corrected 2026-08-05, but the trigger itself is deliberately still absent because the choice depends on the host Ivan picks. **Deploying without adding one ships a dead scheduler that fails silently**: no reminder → no QR pass → no door check-in → no attendance → no CPD credit. Note Vercel Hobby caps crons at ONE RUN PER DAY, which silently misses every 60-min reminder window; Pro or an external scheduler is required. | **Hard blocker on Stage 8 deploy** — the trigger must be created and its first real tick observed before any live event relies on the loop | Stage 8 |
| **Resend idempotency: three residual gaps, none silent** (2026-08-05, after the retry/idempotency work) — (1) **Unverified by execution.** Both load-bearing facts — that a reused key with a changed payload returns 409, and that keys are retained 24 hours — are read from Resend's docs, not tested here; verifying needs a live API key and real sends. Re-confirm at the Resend cutover. (2) **A 409 whose body is not valid JSON** is collapsed by the SDK to `name: 'application_error'` with no `statusCode`, so `isAlreadySubmitted()` misses it and it degrades to an ordinary failure — bounded by `MAX_SEND_ATTEMPTS`, visible in the ledger, not silent. (3) **Retention (24h) exactly equals `SURVEY_GRACE_HOURS` (24).** Unreachable via the scheduler, whose 3 attempts land on consecutive 5-minute ticks (~15 min), but a **manual** re-send of a `failed`-recorded-yet-actually-delivered survey more than 24h later would fall outside the dedup window and deliver twice. Narrow, and the manual path is uncapped by design. | At the Resend cutover (already a tracked row above) — verify 409 + retention against the live API then | Pre-pilot |
| **Cancelling an event writes NO audit trail** (corrected 2026-08-14, live-tested — this row previously read "nothing in the product can cancel an event," which is wrong) — `20260802182411_events_status_definer_only` revoked `events.status` from `authenticated`/`anon` but **deliberately kept `service_role`'s grant**, precisely so `cancelEvents()` in `app/dashboard/actions.ts` keeps working (its own header says so explicitly). Live-tested end-to-end this session: dashboard → select event → bulk "Cancel" → confirm → row correctly flips to `status='cancelled'`, UI updates, no errors. **What's actually missing**: zero `audit_events` rows are written for the mutation (checked `subject_id = <cancelled event id>`, 0 rows) — cancellation is the one events-lifecycle transition in this codebase that bypasses the audited-definer pattern every sibling transition uses (`publish_event`, `set_event_cpd_config`, `mark_attended`). Needs an audited `cancel_event()` definer mirroring `publish_event` (owner/staff gate, audit-insert-last, then revoke `service_role`'s direct status grant so the definer is structural, not conventional — same doctrine as the column lock itself), plus the decision on what cancelling does to issued credits and sent reminders — that decision is still genuinely open and is why this wasn't built during this session's review rather than a scoping oversight. | Before an organiser needs to cancel a real event with an audit-ready evidence trail — i.e. **the first real cohort** — or whenever a body's security review asks "is this logged" | Stage 8 |
| ~~**`supabase db reset` (replay-from-zero) FAILS on CLI 2.109 — the local stack cannot rebuild itself**~~ ✅ **CLOSED 2026-08-10.** Root cause was narrower than this row described: default privileges are not absent, they differ **by creating role**. Measured on the local stack — a table created by `supabase_admin` gives anon/authenticated/service_role `arwdDxtm`, but one created by **`postgres`** (which is who runs migrations) gives only `Dxtm` — TRUNCATE/REFERENCES/TRIGGER/MAINTAIN, no DML. Hosted Supabase grants DML for both. Fixed by re-stating the platform's own default privileges at the top of `20260514164414_init_staff.sql`, the first migration, so every table the chain creates inherits DML exactly as it does on hosted. **Not** in the CI script — `supabase start` applies migrations itself, so a harness grant runs too late; **not** in `seed.sql` — `db reset` runs seed *after* every migration. Additive, idempotent, and a no-op on Seoul, which ran that migration in May. **Proven by execution, not reasoning:** `supabase db reset` now replays all 92 migrations clean, and `bash scripts/ci/replay-and-verify.sh` — the exact script `.github/workflows/replay-verify.yml` runs — exits **0** with `REPLAY + TAMPER GATE: PASS`. **Hard Rule 11 verified intact afterwards**, which was the real risk of a blanket grant: `service_role` INSERT is `true` on `events`/`email_log` and `false` on `credit_ledger`/`audit_events`. This also unblocks the Singapore precondition in the row above ("a fresh replay must be green first"). Was: (found 2026-08-06 during consolidation, PRE-EXISTING, not from either session's work) — several migrations self-verify that `service_role` still holds table INSERT/UPDATE (`20260802182411_events_status_definer_only` raises *"service_role lost its events write path"*; a later one raises *"over-revoked: service_role lost write access it needs"* for `email_log`/`rate_limits`). Those grants are NOT created by any migration — on a real Supabase project they come from the platform bootstrap, and on the local stack **CLI 2.109 zeroes them** (see the `eventar-local-stack-cli-2109-grants` note). `seed.sql` restores them at line 18, but `db reset` runs seed.sql AFTER every migration, so the assertions fire against a database that has not been granted yet. Confirmed empirically: on a fresh DB `has_table_privilege('service_role','public.events','INSERT')` is **false**. **Seoul is unaffected** (the grants exist there, which is why these migrations applied cleanly), so this is a local/CI defect, not a live one — but `DEFERRED`'s own precondition for Singapore provisioning is *"a fresh replay must be green first"*, and today it cannot be. Restoring local after a failed reset takes: apply `seed.sql`'s blanket grant, `db push --local --include-all`, re-apply `seed.sql`. Fixing it properly means either granting `service_role` in an early migration (edits applied history), relaxing the assertions, or granting before replay in the CI harness — a decision, not a patch. | **Before the Singapore provisioning or any CI replay job** — both depend on a green replay | Stage 8 |
| **`test:rls`'s exclusion list is undocumented, and getting it wrong writes permanent rows to the regulator-facing ledger** (found 2026-08-05 while adding `tests/cron` to the gate) — the script names `tests/rls tests/audit tests/cron` as directories but then two *individual* `tests/cpd/` files. The other two CPD suites (`attendance_issuance`, `event_freeze_trigger`) are excluded **deliberately and correctly**: both call `record_credit_entry`, and `credit_ledger`'s NO ACTION FKs make the resulting row — plus its licence, practitioner, event and staff fixtures — permanently undeletable, so gating them would pollute the live ledger on every run. **Nothing states this.** package.json cannot carry comments, and an exclusion that looks arbitrary is exactly how `tests/cron`'s *accidental* exclusion looked — a future session "tidying" the list to `tests/cpd` would silently start writing permanent credit rows on every gate run. Cheap fix: a `test:rls:ledger` sibling script that runs the two excluded files explicitly, making the split self-describing, or a note in the vault's testing doc. **Partially mitigated 2026-08-14:** `tests/helpers/clients.ts` — the single choke point every file in this suite imports its Supabase client from — now hard-throws before creating any client unless `NEXT_PUBLIC_SUPABASE_URL` matches `127.0.0.1`/`localhost`. Tested against all three real scenarios: the local stack (passes, 162/162 unaffected), a Seoul-shaped URL (blocks), and the repo's actual current `.env.local` value, which genuinely is Seoul's URL (blocks). This closes the worst-case consequence (a silent write to production) but not the underlying file-list-clarity question this row was originally about — that's still open. | Next time `test:rls` is edited — or immediately if anyone proposes globbing `tests/cpd` | Stage 8 |
| ~~**Manual send failures still consume the SCHEDULER's retry budget**~~ ✅ **CLOSED 2026-08-06** — `email_log.origin` ('scheduler'\|'manual') added by `20260806000200`; the budget read counts scheduler-origin failures only. Backtested live (`scripts/backtest/send-ledger.ts`): three manual failures leave the scheduler budget untouched, and a manual row still cannot bypass the dedup index (origin is deliberately NOT in the index, or a manual send could double-deliver). Was: (2026-08-05 dev-lens review, I-3) — `{ manual: true }` skips the budget *read*, not the *write*, so a manual send that fails still writes a `failed` row. Scenario: provider outage at T−45, operator clicks "Send reminders now" three times, every click fails for everyone, and the budget is now exhausted for the entire roster; the provider recovers at T−20 and the scheduler gives up on all of them. The rescue lever itself disables the automation. The comment at `lib/email/eventEmails.ts` (MAX_SEND_ATTEMPTS) currently claims this case is prevented — it is not, it is relocated. Fix needs an `email_log.origin` ('scheduler'\|'manual') column so the budget counts scheduler failures only. No test covers this interaction. | Before the first real cohort, or the first time an operator troubleshoots a live provider outage | Stage 8 |
| ~~**`gaveUp` is unreachable on every human surface**~~ ✅ **CLOSED 2026-08-06** — `PassDeliveryPanel` on the event details page now names every attendee the scheduler gave up on, with the roster recovery path. Was: (2026-08-05 dev-lens review, I-4, confirmed structurally) — the give-up notice added to `lib/emailSendSummary.ts` renders only via `composeSendMessage`, whose sole non-test caller is `EmailSendControls.tsx`, which calls the two Server Actions, which both pass `{ manual: true }`, which skips the cap — so `gaveUp` is always 0 there. It is produced only on the cron path, where it lands in JSON and a `console.error` that no operator UI reads. **The scheduler permanently giving up on a recipient is currently invisible to the organiser.** Its test passes because it calls the formatter directly with a hand-built object (rule-9 vacuous coverage in behavioural costume). Real fix is a per-recipient email-outcome surface — no such surface exists anywhere (the only `email_log` read in the app is a confirmation *count*), so this is new frontend and subject to the design pipeline. | When a per-recipient email-outcome surface is designed, or immediately if a real event loses a pass unnoticed | Stage 8 / frontend |
| ~~**`/api/cron/dispatch` returns `ok: true` unconditionally**~~ ✅ **CLOSED 2026-08-06** — `ok` is derived from the batch and a `needsAttention` roll-up was added so a trigger can alert without parsing the per-event array. Was: (2026-08-05 dev-lens review, I-5) — a tick in which every send failed, or every event hit `readRecipients`' error path, still returns HTTP 200 `{ok: true}`. Per-event detail is honest inside `dispatched[]`, but an external trigger or uptime monitor keys on the status code and top-level shape. Signature defect class at the response layer: the honesty lives one layer below where the consumer looks — and Stage 8 is what wires the consumer. Cheap fix: derive `ok` from the batch, or add a top-level `failed`/`gaveUp` roll-up to alert on. | **Stage 8, when the trigger is created** — the trigger is the consumer this misleads | Stage 8 |
| ~~**The retry-budget read silently truncates at PostgREST's `max_rows`**~~ ✅ **CLOSED 2026-08-06** — explicit `.limit(BUDGET_READ_LIMIT)` plus a loud log when the bound is reached (an undercount means extra retries, never a double-send). Was: (2026-08-05 dev-lens review, I-6; measured, not theorised — 1500 rows inserted on the local stack returned exactly 1000) — `supabase/config.toml` sets `max_rows = 1000` and the budget query has no `.limit()`, no count and no truncation detection, so above that it under-counts by an arbitrary unordered slice and recipients past the boundary have their budget silently reset to zero, resuming the retry storm the cap exists to prevent. Triggers at ~334 recipients × 3 failures on one (event, purpose) — i.e. a provider-wide outage on a 350-person event, exactly the scenario the cap is for. Above the current stated envelope (20 events × 30 registrants), hence tracked not fixed. Confirm the hosted project's `db.max_rows` before Stage 8, then either `.limit()` deliberately or compare `count` against `data.length` and log the truncation. | Before the first event above ~300 registrants, or at Stage 8 provisioning when `db.max_rows` is set | Stage 8 |
| ~~**`skipped` renders "already sent" for a stale `queued` row that was never emailed**~~ ✅ **CLOSED 2026-08-06** — the prior-send read now also carries status, so a 23505 against a `queued` row is counted `stalled` and rendered "logged earlier but never emailed — check them in from the Roster" instead of "already sent". Was: (2026-08-05 user-lens review, CRITICAL-3; pre-existing, same class as the `failed` hole closed by `20260805000000`) — `queued` remains terminal under the dedup index by deliberate decision, but a `queued` row means either the dev stub ran OR the process died between the ledger insert and the status update. After a production timeout mid-batch the operator clicks re-send and sees "30 already sent" — total, permanent mail loss reported as complete success. `maxDuration` narrows the window without closing it. Cheap fix with no extra round trip: widen the existing budget read to `select('registration_id, status')` without the status filter, so the 23505 branch already knows what it collided with and can render "logged earlier but never emailed — check them in from the Roster". | Same trigger as the recipient-chunking row above, or the first observed mid-batch timeout | Stage 8 |
| ~~**A whole-event send throw reports as `1 failed` regardless of roster size**~~ ✅ **CLOSED 2026-08-06** — now returns `zero('send threw — no recipients were processed for this event')`, surfaced verbatim by the existing error branch. Was: (2026-08-05 user-lens review, MEDIUM) — `app/api/cron/dispatch/route.ts`'s catch pushes `failed: 1`, so a 200-person event whose send threw reads as "1 failed". No `email_log` rows are written so the next tick retries cleanly (the behaviour is correct), but any human reading the number radically under-reads the blast radius. Suggest `{...zero(), error: 'send threw — no recipients processed'}` so the existing error branch surfaces it verbatim instead of masquerading as a per-recipient count. | Next time the dispatch response shape is touched, or when the trigger's alerting is wired | Stage 8 |
| ~~**The dispatcher's batch cap bounds EVENTS, not recipients**~~ ✅ **CLOSED 2026-08-06** — `MAX_RECIPIENTS_PER_RUN = 300` fills the batch by recipient count in urgency order, with a head:true count per candidate; a single event bigger than the whole budget is always taken so it cannot be starved. Was: (found 2026-08-05, same audit) — `MAX_EVENTS_PER_RUN = 20` while the per-recipient cost (QR render + email render + provider round-trip, sequential) is what actually consumes the function timeout: 20 events × 30 registrants is 600 sequential sends. Partially mitigated 2026-08-05 — `selectDue` now orders by soonest-closing window so the cap degrades by urgency rather than arbitrarily, and `maxDuration = 60` replaces the 10–15s host default. Not closed: a genuinely large batch still exceeds 60s, and a mid-batch kill leaves rows at `'queued'`, which is TERMINAL under `email_log_dedup_idx` — so a timeout is permanent mail loss, not a retry. Real fix is recipient-level chunking with a resumable cursor. | A single dispatch run approaches the 60s bound, or any real event exceeds ~100 registrants | Stage 8 / first real cohort |
| **The swallowed-error lint rule is production-only** (2026-08-06, closing Decision 2 of the 2026-08-05 frontend review) — `no-restricted-syntax` in `eslint.config.mjs` now bans `const { data } = await …` without `error`, the shape that produced six of that review's eight fixes and had bitten the repo at least three times before. It is `error` across `app/`, `lib/`, `components/` and `proxy.ts`, and **off under `tests/`** by Ivan's call. Rationale, corrected after measuring: it fires on 80 test read-backs and **none of them are the defect** — on failure supabase-js returns `data: null`, never `[]`, and every assertion the suite makes on a read-back rejects null (`toHaveLength(0)`, `toEqual([])`, and `toBeNull()` on `row?.field` → `undefined` all throw; verified in a scratch vitest run, not reasoned about). An earlier pass of this same work claimed 14, then 9, "genuine false greens" in `tests/` and edited them; those edits were reverted once the matchers were actually measured. The one shape that WOULD defeat this is `expect(data ?? []).toHaveLength(0)` — grepped for, appears nowhere. **Residual: nothing stops a new test being written that way**, since the rule is off under `tests/`. The queued escalation (Ivan's stated second preference) is turning it on across `tests/`, which now costs ~80 ceremonial edits for zero known bugs — weaker than it looked when the decision was taken. Six production sites carry a deliberate `eslint-disable` + reason (metadata decoration, two `auth.getUser()` reads where "no session" and "call failed" are the same fact, a rate-limit key whose fallback is better than the token, an attribution-only read, and a staff read that already degrades to `unavailable: true`). | A test appears using the `?? []` shape, **or** a supabase-js upgrade changes the failure return from `null` to `[]` — which would silently turn all 80 into real false greens | Next RLS-suite pass / on any supabase-js major |
| **`events.published_at` is unreliable for anything published before `20260802182411`** (recovered 2026-08-09 from an orphaned handoff in `.claude/worktrees/busy-merkle-b00f7d`; the code landed as `8748db8`, the evidence never did) — the probe that produced that migration ran all five publish paths on the local stack as a real `organiser_member` JWT in one rolled-back transaction, and found `published_at` **NULL on 4 of 5**: only `publish_event()` set it. `create_event_with_blocks(status='published')`, a raw `UPDATE`, `update_event_with_blocks` with `status` in the payload, and a raw `INSERT` all produced a published event with no timestamp and **no `event_published` audit row**. All four were reachable by any authenticated organiser through PostgREST on their own event, because `events_organizer_update_own` carried no column restriction and `status` had a table-level grant — *"the audited definer function was a convention, not a constraint."* The hole is closed. What is NOT closed is the **data**: every event published through paths A/C/D/E before that migration has a NULL or meaningless `published_at` and no audit row, so any report, evidence pack or regulator-facing answer that reads publication time from this column is reading a value that was never trustworthy. Nothing in the repo said so until now. | Before `published_at` is read by anything a body sees — an evidence pack, an audit response, or a "published on" column in a report. Decide then whether to backfill from `audit_events`, expose it as unknown, or bound queries to post-`20260802182411` events | Stage 8 / first body-facing report |
| **Self-serve check-in is a tap from anywhere, not a scan at the venue** (found 2026-08-06 when Ivan rejected the frontend review's Decision 1 premise) — the product model is: an attendee scans a QR **at the venue**, or staff scan their QR, or staff key their code. **No such venue-scan path exists.** The poster/event QR encodes `/events/{id}` (`lib/qr.ts:21`), a content page; the attendee's personal QR encodes their own pass page. What shipped as "self-serve" is `ConfirmButton.tsx`'s **"Confirm I'm here"** on the attendee's own phone, and `self_check_in()` has **no location check of any kind** — grepped for geolocation/venue token/proximity/NFC/beacon, all absent — so it works from anywhere on earth inside the event window. **Contained, not open:** `checkin_modes` defaults to `{"staff": true, "self_serve": false}`, and the one place it was switched on, the demo seed, was flipped to `false` on 2026-08-06 (`seed-demo.ts`, plus the run-sheet's Beat 4 and prep step 6). So no live path can reach the tap today. **Design settled, build deferred:** `docs/plans/2026-08-06-venue-scan-checkin-spec.md` specifies the replacement (three token options — static / rotating QR on a staff door display / rotating short code — recommendation is rotating-QR with the short code as fallback), the `check_in_method` CHECK widening that also closes review Decision 1, and the four questions Ivan must answer first. Not code, because the block-architecture admission checklist passes 5 of 6 and fails item 6: Stage 8 is the active phase and this is not on its critical path. | **Stage 8 closes** (deployed, scheduler triggered, first real tick observed) — **or sooner if an accrediting body asks how attendance is witnessed**, which turns "we cannot tell you, and the self-serve path had no location check" into a blocking answer rather than a deferred one | Post-Stage 8 |
| **`accrediting_bodies.short_name` has no UNIQUE constraint** (dev-lens review, 2026-08-13 dual-track Task 1) — pre-existing since `20260709160000`, not introduced by Task 1, but Task 1's seed migration (22 new rows, 15 of them Colleges) leans on `short_name` staying unique for its own self-verifying lookups (`select id from accrediting_bodies where short_name = 'HKAM'`), and the same column is now the join key for `lib/cpd/authorisedBodies.ts`'s picker. A future seed or manual insert with a duplicate `short_name` would not fail loudly at the constraint layer — a scalar subquery expecting one row would error, but an `IN`/join query would silently pick up both. | Before the next body-seeding migration, or the next time `short_name` gains a new caller | Task 10.4/10.9 / next seed touch |
| ~~**CPD readiness-strip copy says "points confirmed", every other CPD surface says "hours"**~~ ✅ **CLOSED 2026-08-14** — `app/events/[id]/details/page.tsx:188` now reads "hours confirmed" / "no body or hours", matching `cpd_hours` and the accreditation card's own label. Verified live: the readiness strip now shows "HKCP 3 · hours confirmed". | — | Closed |
| **Sidebar has a disabled "Accreditation" nav item next to the working inline CPD accreditation section** (user-lens review, 2026-08-13 dual-track Task 1 — pre-existing stub pattern shared with "Participants"/"Communications", not introduced here) — a first-time organiser seeing "Accreditation" greyed out with "surface not built yet" in the sidebar, on the same page where `CpdAccreditationSection` is live and working inline, could reasonably conclude the feature doesn't exist. Naming collision only; not a functional bug. | Whenever the sidebar's disabled-stub set is next revisited, or a real "Accreditation" surface is designed | Frontend polish pass |
| ~~**`PT_BOARD` is the only accrediting-body `short_name` using an underscore-slug style**~~ ✅ **CLOSED 2026-08-14** — migration `20260814040000_ptboard_short_name_cosmetic.sql` renamed it to `PTB` (matching VSB's own `<initial><Board→B>` style). Confirmed safe first: grepped the whole repo, nothing outside migration seed files references the literal string `'PT_BOARD'`; the real FK target everywhere is `accrediting_bodies.id`, never the short_name. | — | Closed |
| **[PROPOSAL — R1, prospect/strategy session 2026-08-14, Claude's recommendation, NOT Ivan's decision]** Category-per-role instead of a single hardcoded category column — store a `role` (attendee/chair/presenter) on the registration and let each accrediting body's rule pack map role → that body's own category code, rather than one hardcoded category column. Rationale: HKAM uses codes 6.1–6.16 (the vocabulary Task 10.3/10.9's `fcaa_category` currently assumes), but MCHK and CNE — both on the ICI Summit's real accreditation sheet — don't use that vocabulary at all, and HKCP's own Operational Guidelines use point tables by category A–F, a different alphabet from HKAM's, for the SAME Fellow at the SAME event. A single hardcoded category column cannot hold multiple vocabularies simultaneously. If adopted, this reshapes Task 10.3/10.9's category-column design — not yet decided, not yet built. | Ivan reviews and decides whether to adopt, before Task 10.3/10.9's category-column design is finalised or Task 10.8/10.9 (multi-body) is built on top of it | Before Task 10.8/10.9 |
| **[FINDING — R2 withdrawn; superseded by the attribution model, Ivan 2026-08-14]** ~~Store only day-scoped rows and derive "Both Days" by summing~~ — **withdrawn.** It assumed additivity the regime does not guarantee. Settled instead in **`docs/adr/0002-multi-body-accreditation.md` § The attribution model**: a body publishes an award schedule keyed by attendance scope, values are stored as published, and the applicable one is *selected* by matching the check-in record — never computed by arithmetic across scopes, never summed across bodies. **What remains open is the prerequisite, not the schedule:** `registrations` carries exactly one `check_in_at`/`check_in_method` (`20260523010000_init_checkin_columns.sql`), so "attended Day 1 only" vs "attended both days" is unrepresentable today. Day-level attendance is a flat **yes/no per day** (ADR-0002) — narrower than the rule-13-deferred multi-track session model, and it does not reopen it, but it does need a storage shape. **RESOLVED 2026-08-14** — attendance is counted in configurable **units** (daily default, half-day/session available); packs express either a proportional or an explicit per-scope award scheme; MVP records **one check-in per unit, no check-out**. See ADR-0002 § Attendance units. Task 10.8/10.9 is now designable. | — | Resolved |
| **[PROPOSAL — R5, prospect/strategy session 2026-08-14, Claude's recommendation, NOT Ivan's decision]** `body_confirmed` attestation tier — add a `body_confirmed` tier alongside the existing `organiser_attested` → `attendance_verified` ladder, so a ledger row computed from a body-signed rule pack is distinguishable from one computed from a still-provisional pack. Rationale: Task 10.4/10.9's rule packs ship provisional by design (pack hash per ledger row makes "correct later" reachable); this proposal is the attestation-tier mechanism that would make a wrong provisional row findable-by-hash and correctable later. **M4 below depends on this shipping.** | Ivan decides whether to adopt alongside Task 10.4/10.9 (rule packs) or before Task 10.8/10.9 (multi-body), which multiplies the exposure this tier would bound — see M4 | Before Task 10.8/10.9 ships, if adopted |
| **[PROPOSAL/FINDING — M1, prospect/strategy session 2026-08-14]** Cycle-scoped caps can only ever be advisory in Eventar — per-cycle caps (90 points/3yr, ≥30 active+30 passive, the 75-point passive ceiling) can only ever be ADVISORY, because Eventar only sees the fraction of a practitioner's CPD activity that runs through Eventar itself; practitioners also attend other providers' events. Only PER-EVENT caps (≤8pts/day, ≤35/conference — already the kind of ceiling Task 10.4/10.9's rule packs carry) are things Eventar can actually enforce, since the event is fully visible to it. Recommendation: never render a cycle-scoped figure (e.g. "62/90 points") as though it were the practitioner's true compliance status. | Before any practitioner-facing compliance-progress UI is designed | Stage 13 |
| **[PROPOSAL — M2, prospect/strategy session 2026-08-14]** `cycle_started_on` belongs on `practitioner_licences`, not the accrediting body — a licence's CPD cycle anchor date should live on `practitioner_licences` (per-licence, since admission date varies per Fellow), not on the accrediting body or its rule pack, which should own the cycle's SHAPE (length, minimums) rather than any specific practitioner's anchor date. | Before cycle-anchored compliance tracking is built (gated by M1's advisory-only framing above) | Stage 13 |
| **[PROPOSAL — M3, prospect/strategy session 2026-08-14]** "Claimed" should be a separate append-only record, not a mutable ledger field — "claimed" (submitted to iCMECPD/the body) should be modelled as a separate append-only record referencing which `credit_ledger` entries it covers, NOT as a mutable field on the immutable ledger itself, consistent with the ledger's existing append-only/no-UPDATE posture (Hard Rule 11). | Before a "mark as claimed" surface is designed for any practitioner-facing wallet | Stage 13 |
| **[FINDING — M4, prospect/strategy session 2026-08-14, depends on R5 above]** Eager credit-posting under multi-body needs R5 to stay safe — the existing architecture posts credit "eagerly" at check-in time (already decided, unrelated to this session). Under multi-body (D-A, Task 10.8/10.9) that could mean up to 13 immutable ledger rows per attendee per event, computed from provisional (unconfirmed) rule packs. This is only safe **if R5's proposed `body_confirmed` tier ships**, making wrong provisional rows findable-by-hash and correctable later — if R5 does NOT ship, this eager-posting design needs revisiting before Task 10.8/10.9 goes live. | Before Task 10.8/10.9 (multi-body) ships to a real event — confirm R5 shipped, or revisit eager-posting | Before Task 10.8/10.9 ships |
| **[PROPOSAL — S1, prospect/strategy session 2026-08-14]** Rule packs should carry an explicit `exclusions` list — accrediting-body rule packs (Task 10.4/10.9's `category_taxonomy`) should carry an explicit `exclusions` list (HKCP excludes research/undergrad-teaching/clinical-audit-M&M that HKAM accepts), so the evaluator can emit an explicit "excluded" outcome distinguishable from "no licence" or "no rule matched" — surfaced to the organiser at event-configuration time, not after the fact. | Ivan decides whether to adopt, before Task 10.4/10.9 (rule packs) is built | Task 10.4/10.9 |
| **[PROPOSAL — S9, prospect/strategy session 2026-08-14]** Price the evidence-pack export INTO the accredited-event tier, not as a per-event add-on — price the evidence-pack export (Task 10.7/10.9, College → Academy export) as INCLUDED in the accredited-event pathway's price tier (D-E), not as a separate per-event add-on, so pricing doesn't undercut the dual-pathway architecture by inviting organisers to buy the cheap ordinary-event tier and do the compliance work by hand anyway. | Ivan decides pricing structure before the accredited-event tier is priced/sold | Stage 12 |
| **[FINDING — user-lens review, 2026-08-15, Task 10.9/10.9]** A body with no sourced `prior_approval` lead time renders nothing on the CPD accreditation card — same silent absence as a body that genuinely has no such requirement. Confirmed live: switching the picker from HKCP to MCHK makes the whole advisory line disappear with no placeholder, no "not yet available" message. The copy is accurate for the ~1 sourced body (HKCP) but ambiguous for the ~20 unsourced ones — a time-pressed organiser can't tell "this body doesn't require prior approval" from "we haven't researched this body yet." Not fixed — deliberately deferred rather than built unprompted, since a "not yet available" state for ~20 bodies is a real product/content decision (which bodies get it, what it says), not a bug fix. | Ivan decides whether a distinct "not yet sourced" state is worth building, and if so, source enough bodies' lead times first that the state isn't the common case | Before more bodies' `prior_approval` data ships, or before this card sees real organiser traffic |
