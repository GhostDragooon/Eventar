# DEFERRED — technical debt with a due date

Every deferral carries a **re-entry criterion** — the observable event that makes it wrong to keep deferring. Reviewed at each sprint boundary (standing ritual). Living tracker, not a phase doc.

_Last updated: 2026-07-25 (**three-lens phase-completion review of the CPD MVP build** — 3 rows added for what the review found and could NOT fully close in the same pass: the missing organiser↔body authorisation model (grant hole shut, product model deferred), the credit window's generous ±24h bound (real fraud closed, semantically-right bound blocked on the demo fixture), and `credit_ledger.actor_id`'s inability to name a staff actor (worked around via the staff auth-user id). Everything else the review found was fixed outright — see `PROJECT_STATE.md`'s CPD MVP section. Previously: CPD MVP execution plan Stage 7 close-out: 6 rows added for the ⚪ Full-product items the config-free attendance-issuance build deliberately left out — retry-queue/`pending_credit`, `creditIssued` API field, live kill-switch toggle, real-world issuance-rate bound (self-serve accounts), per-body chains + lock-timeout, and cross-cutting UX/perf polish. Previously: 2026-07-18 Milestone A phase-completion review — 4 non-blocking rows from the three-lens review + backtest (`set_staff_role` hardening, `anon` staff-grant footgun, HR11 positive-test gap, `WK-` prefix optics), item 19 closed. Before that: 2026-07-11 external-review reword per the 3a/3b split — see `docs/plans/handoff_10072026.md`)._

| Deferred item | Re-entry criterion | Earliest |
|---|---|---|
| **`NewEventForm.test.tsx` "clears the save confirmation on the next submit attempt" is flaky under full-suite concurrency, clean in isolation** — 2 occurrences across the week prior (`handoff_25072026.md`, which set the "promote to an active fix if it recurs a third time" threshold — not `_v2`, that doc doesn't mention this flake at all) + 2 more directly observed in this session (2026-07-26), crossing the threshold. Diagnosed, not fixed: `onSubmit`'s `setSavedAt(null)` is a plain synchronous setState, but the button's `disabled`/label both key off `useTransition`'s `pending` flag, and the confirmation banner's `setSavedAt(new Date())` is set inside the transition's post-`await` continuation — under CPU pressure from concurrent test-file workers, React's transition scheduler can interleave these renders differently than in isolation. Two credible hypotheses examined (stale `findByRole` resolution vs. transition-continuation batching), neither confirmed — the bug is non-deterministic by nature, so a "fix" attempted without reliable reproduction can't be trusted by TDD's own discipline (can't watch it fail on demand, can't prove a change actually addresses the race vs. got lucky). | Build a reliable repro harness first (e.g. run this file alongside synthetic concurrent load) before attempting a fix, or the flake starts costing more than the investigation would | Next time this file is touched, or it blocks a real gate run |
| **Staff MFA workstream** — password+TOTP backend on `auth.mfa`, `mfa_is_fresh` helper, wrapper freshness-gate wiring, enrolment UI, TOTP-mandatory enforcement | Signing ships; moved wholesale from Sprint 2 (had no Sprint 2 consumer) | **Sprint 4** |
| **Turnstile provisioning + escalating unauth strike ladder** | **Cloudflare keys provisioned** — gates public exposure (ADR Finding 2). Unchanged by Sprint 2 | When keys land |
| **Resend cutover** — real-user OTP + email-template customisation + zh-HK variant | Pre-pilot (before any real attendee cohort) | Sprint 2 exit / Sprint 3 start |
| **CSP enforcement mode + nonce-boundary decision** | A clean real-user violation window in report-only mode (Sprint 2 shipped report-only, allowlist verified against actual app calls — nominatim + Google Fonts) | Sprint 3/4 |
| **Historical registration linking** | Wallet unfreeze (ledger keys on `licence_id`, so not needed for credits) | post-M4 |
| **`request_events` table + pg_cron scoring job** (4 inference signals → flags) | Ships alongside the first authenticated API surface, one sprint after data accrues | Sprint 5 |
| ~~**`Staff.role` TS union widen**~~ ✅ **CLOSED 2026-07-09** — shipped in Sprint 3a Task 2 (`6c31bff`): widened to the 5-role union at all 3 declaration sites, plus the 4 app-layer authorization checks and cascading test fixtures the widen broke. | — | Closed |
| ~~5-role staff enum~~ ✅ **CLOSED 2026-07-09** — shipped in Sprint 3a Task 2 (`6c31bff`): `staff_role_check` widened to `organiser_admin`/`organiser_member`/`body_admin`/`auditor`/`eventar_staff`, the sole live row remapped from `manager`. | — | Closed |
| ~~**P2 shared-vs-separate audit-chain lock decision**~~ ✅ **CLOSED 2026-07-09** — answered in the vault before this tracker caught up: separate chain, own advisory lock (avoids doubling contention with `audit_events` under check-in bursts). See vault `10 — Architecture/Credit Ledger.md` §8.4. | — | Closed |
| **`lib/abuseTier.ts` live-caller wire-up** — the §4 3-in-60 session-revoke capability shipped in Sprint 2 has no live caller; nothing authenticated-attendee-facing exists yet to feed it | First authenticated attendee-facing mutation surface (post-freeze) | Sprint 3+ |
| ~~**Three residual bare-`PUBLIC`/`anon` grant entries** (`self_check_in`, `app_private.require_active_staff`, `public.pseudonymise_user`)~~ ✅ **RESOLVED 2026-07-16** by `20260716233440_grant_hygiene_residual_public` (Milestone A Task 10): `pseudonymise_user` closed to `authenticated`-only, `require_active_staff` explicitly revoked from all PostgREST roles, `self_check_in` re-asserted anon-open by design — all confirmed on live Seoul via the migration's self-verifying DO block + a phase-completion backtest (`has_function_privilege` matrix + `get_advisors` no longer flags `pseudonymise_user` under 0028). | — | Closed |
| **Exit-gate burst-throughput test's 2s P99 threshold occasionally flakes** (~1-in-4 runs) against real network conditions to the remote Seoul project — genuine variance, always well under 3s when it happens, not a functional regression | Ivan's call: loosen the bound, make it informational-only, or measure DB-side timing instead of client-observed latency | Whenever it becomes annoying |
| Audited-function template / codegen | ~12 near-identical definer functions accumulate (gate-drift risk) | Sprint 3/4 |
| **Replay-from-zero + chain/tamper test as a *standing CI check*** — the local-stack replay was run once by hand on 2026-07-10 and immediately found two portability bugs (now fixed: `rls_auto_enable` guard, stale `seed.sql`). But it's not automated: nothing re-runs it, so the next migration that assumes hosted-Supabase state or drifts from the seed would only be caught by hand again. The chain/tamper test (owner-level edit → `verify_*_chain()` catches it) also has no CI home — the *capability* is proven (demonstrated live this session), the *harness* isn't. Both need a CI job that spins a local stack (Docker), `supabase db reset`s, and runs the assertions. Foundation now proven working (local stack comes up clean with the fixes). | **Before the Singapore fresh-provisioning** (a fresh replay must be green first) or when CI is stood up | Before Singapore provisioning / Sprint 4 |
| **[AUDIT-COVERAGE GAP — body-onboarding critical path] `staff` role mutations are unaudited and `service_role`-writable** — found during the Sprint 3a Task 10 service_role sweep. Same *class* as the service_role tamper gap (privilege-layer integrity), but audit-coverage rather than tamper-resistance: `staff` is the role source gating everything (`is_manager()`, every `body_admin`/`eventar_staff` check), yet there's no definer function and no `audit_events` write when a role changes, so "who granted `body_admin`, and when" is recorded **nowhere**. Correctly deferred (no live consumer, no staff-role churn yet — it's a *build*, not a hole), but for a platform selling audit-ready evidence, the privilege-granting layer being the one unaudited mutation surface is exactly what a body's security review asks about first. Build: `set_staff_role()` audited definer fn + the Hard Rule 11 grant posture. **Not undifferentiated backlog — this is on the critical path for the first body enterprise contract**, not merely "later". | **Before the first accrediting-body enterprise/security review** (a body will ask "who can change roles, and is it logged"), or first staff-role churn at scale | Before first body contract |
| **`credit_disputes` has no automated RLS test** (Task 9) — its `ledger_entry_id` is a NOT NULL FK to `credit_ledger`, and after Task 8's C3 hardening `service_role` (the only credential a committed vitest suite has) cannot DELETE `credit_ledger` rows by design. A committed test that creates a real ledger row as a fixture (via `record_credit_entry`, which service_role CAN call) would therefore permanently reside in the live ledger — the exact outcome the branch-testing decision for Task 9's concurrency test was made to avoid. `credit_disputes`' own RLS shape (self-read/self-raise, now with an ownership check on raise) is a well-precedented pattern already covered elsewhere (`consent_records`, `practitioner_licences_self_read`), so the risk is judged low, but it's genuinely untested. | Sprint 3b's real event-transition wiring produces real ledger rows as a natural byproduct (test against those, not synthetic ones), or the Supabase plan upgrades to support branching for CI | Sprint 3b |
| ~~**`credit_ledger`'s hash chain has zero automated test coverage**~~ 🟡 **PARTIALLY CLOSED 2026-07-24** — `tests/cpd/attendance_issuance.rls.test.ts` (CPD MVP Stage 5) now exercises `record_credit_entry`/`verify_ledger_chain` live for the attendance-issuance path specifically (issue, idempotency, a real concurrent race, guard skips) and asserts the chain stays fully valid. The broader hash-chain properties this item originally named (hash-collision safety, GUC-independence, `credit_transferred`/other entry types) are still only manually verified, not in CI — that residual is unchanged. Was (Tasks 8/9): `compute_credit_ledger_hash`/`verify_ledger_chain`/`record_credit_entry` are the actual tamper-evidence mechanism for the regulator-facing ledger, and no committed test exercised any of them (no safe cleanup path against the live ledger — see the next row, which is why this is 🟡 not ✅). | Same as above for the residual — Sprint 3b's real event-transition wiring, or Supabase branch-based CI support | Sprint 3b |
| **`tests/cpd/attendance_issuance.rls.test.ts` leaves permanent residue on every run that actually issues a credit** (Stage 5, 2026-07-24) — confirmed empirically: 3 dev-project runs while building this test left exactly 6 permanent `credit_ledger` rows (2 per full run: the issue-test's credit + the concurrency-race winner). Root cause, found running this file: `credit_ledger.licence_id`/`.user_id`/`.event_id`/`.body_id` are all plain `references` (Postgres default **NO ACTION**), so once a real credit is issued, its licence, practitioner (auth user + `public.users` mirror), event, accrediting-body fixture, AND — one hop further, via `events.created_by -> staff.id`, also NO ACTION — the staff row that created the event ALL become permanently undeletable, not just the ledger row itself (a materially wider blast radius than items above already accepted for `credit_ledger` alone). Regulator-grade property by design (can't erase the entities behind an issued credit); the test's `afterAll` only cleans what carries zero credit reference (documented in the file's header). Same accepted-debt class as the rows above, logged separately because the blast radius (auth users + accrediting-body + event + staff fixtures, not just one ledger row) is worth tracking on its own. | Same as above — Supabase branch-based CI (a throwaway branch per run makes residue moot), or accept as permanent dev-project cost | Sprint 3b |
| ~~**`declare_licence` leaks a raw Postgres FK-violation dump for a bad `p_body_id`**~~ ✅ **CLOSED 2026-07-26** — TDD red→green: `tests/audit/licence_mutations.test.ts` now asserts a bogus `p_body_id` fails `P0002` ("accrediting body ... not found"), not a raw FK dump; watched it fail (`23503`) before the fix. `declare_licence` now checks `exists (... accrediting_bodies ...)` before the insert — not the same shape as `verify_licence`/`lapse_licence`/`revoke_licence` (those add an org-ownership check after their not-found check, since a *licence* is scoped to an org's body; a *body* itself has no such ownership dimension for the caller declaring against it), so "mirrors the siblings" means same not-found *mechanism* (`P0002`, no raw constraint text), not an identical function shape. Still not a security leak: `accrediting_bodies` is publicly readable (`accrediting_bodies_public_read_active`), and the function only ever echoes back the UUID the caller supplied — no new information crosses the boundary. Migration `20260726070748_declare_licence_clean_not_found`, applied live to Seoul. 19/19 green, no regressions. | — | Closed |
| **The licence-mutation functions have two different not-found philosophies, not one** (found during the 2026-07-26 backlog review, pre-existing, not a regression) — `verify_licence`/`lapse_licence`/`revoke_licence` (staff actors) return `P0002` for a nonexistent licence id but `42501` for one that exists at a different org, which is a narrow cross-tenant existence oracle (a staff member can distinguish "no such licence" from "exists, not yours" for a guessed UUID). `set_primary_licence`/`supersede_licence` (self actors) deliberately collapse both into a single `42501`, avoiding exactly that oracle. `declare_licence`'s new check (above) is a third shape with no ownership dimension. Nothing forces these to reconcile — the self-actor functions made a real security choice the staff-actor ones didn't visibly consider — worth a deliberate decision, not silent drift. | Before a body's security review asks about this, or next time this file is touched | Sprint 3b |
| **`declare_licence`'s new not-found check has no `status='active'` filter** (found during the 2026-07-26 backlog review, pre-existing behavior, not a regression — the raw FK constraint it replaced didn't check status either) — `accrediting_bodies` currently has real `HKAM` (`deferred`) and `LSHK` (`onboarding`) rows; a caller who obtains either id directly (bypassing the public picker, which does filter to `status='active'`) sails past the new guard and creates a real `practitioner_licences` row against a non-active body. "Not found" in the new error message means "no such row," not "usable." | Before a real self-serve licence-declaration UI ships (post-freeze) — decide then whether declare_licence should also gate on status | Sprint 3b |
| **`record_credit_entry` doesn't cross-validate that `p_licence_id` belongs to `p_user_id`/`p_body_id`** (Task 10 user-lens review) — each FK is checked individually, but nothing enforces the three are mutually consistent; a caller could pass a real licence for user X at body Y alongside mismatched `p_user_id`/`p_body_id` and get a self-consistent-looking but wrong ledger row. Low risk today (service_role-only, no live caller), but exactly the kind of landmine Sprint 3b's event-transition wiring could hit silently. | Before Sprint 3b wires a real caller to record_credit_entry | Sprint 3b |
| **`record_credit_entry`'s app-facing role parity with `is_manager()` is not yet extended** (Task 2/10) — `is_manager()` now grants cross-owner event/registration read to `role in ('organiser_admin','eventar_staff')`, but the 4 corresponding app-layer TS authorization checks (`app/dashboard/actions.ts`, `app/(public)/events/[id]/page.tsx`, `app/events/[id]/details/page.tsx`, `app/events/[id]/details/emailActions.ts`) still gate on `role === 'eventar_staff'` only — a 1:1 preservation of the pre-widen behavior, not yet extended to `organiser_admin`. Deliberate minimal-scope call for Sprint 3a (no `organiser_admin` account exists yet; widening would itself touch the frozen frontend beyond the approved carve-out). | First real `organiser_admin` staff account is provisioned (Sprint 3b onboarding) | Sprint 3b |
| **Redundant explicit `pg_advisory_xact_lock(hashtext('audit_events_chain'))` in the 6 licence-mutation functions** (Task 6) — the `compute_audit_hash` BEFORE-INSERT trigger already takes that lock during `write_audit_event`'s insert, so the explicit acquisition is reentrant/redundant. Harmless (same lock, same xact) but a convention break (no other audited fn does it) and a cargo-cult risk into the credit_ledger functions, which use a *different* lock. Left in place (already applied, all tests green); flagged so it isn't copied. | Next time these functions are touched, or a grant/hash-hygiene pass runs | Sprint 3b/4 |
| **Licence-mutation functions are transition-agnostic on `verify`/`lapse`/`revoke`/`set_primary`** (Task 6) — only `supersede_licence` got a from-state guard (Ivan's call, blocks superseding revoked/superseded). The staff-gated ones will e.g. flip a `revoked` licence to `verified` leaving `revoked_at` set. Consistent with `transition_dsr`'s own transition-agnostic precedent; lower risk since staff-gated. Decide whether this family should enforce a real state machine. | Before a body-reviewer workflow relies on status transitions (Sprint 3b) | Sprint 3b |
| Cross-jurisdiction schema polymorphism (bodies, licence types, credit schemes) | AU or 2nd jurisdiction onboards — **migration cost accepted as debt** | Sprint 5+ |
| Practitioner-in-a-week product-flow sketch | Before wallet-unfreeze consideration | Before Sprint 4 |
| External review by real accrediting body + organiser | **Gates Sprint 3b** (the CPD domain: `body_rules`/evaluator/issuance/roster + the governance half) — the 3a/3b split resolved the original wording: 3a's *ungated* foundation incl. the `credit_ledger` core shipped 2026-07-10 without the review, deliberately config-reference-free; the review also resolves Q26 | Before Sprint 3b |
| Bulk-verify endpoint for accreditors | Documented request from an onboarded body | On request |
| W3C Verifiable Credentials + DID:web + Status List 2021 | First accrediting-body request, or verifier-tooling maturity | Phase 2 |
| RFC 3161 TSA anchor (external tamper proof) | First body asks how tampering is externally provable | Phase 2 |
| Behavioural detection graduation (flags → auto-action) | **Locked at "never"** — standing pushback flags any attempt | — |
| ~~7-year retention grounding~~ ✅ **CLOSED 2026-07-09** — no body's CPD retention clause actually reads 7 years; grounded per-body via primary-source citations (GL24 §3.8 = 3yr, Law Society Guideline 10.6 = 2yr, VSB FAQ Q18 = 6yr, PT Board CPD Manual = 6yr, HKICPA Statement 1.500 = 5yr, MPFA Guidelines VI.4 §15(a) = 3yr; HKIE confirmed no stated figure; HKCR unverified). Full detail: vault `10 — Architecture/Credit Ledger.md` §8.5, Decisions Log Q24. | — | Closed |
| Organiser-attested liability T&Cs (HK counsel) | Before first pilot organiser signs a contract with attested-vs-confirmed terms, per Slice 0.11 §6.7 | Before launch |
| ~~**`tests/rls/checkin_throughput.rls.test.ts` grabs "a" staff row via unfiltered `select('id').limit(1).single()`**~~ ✅ **CLOSED 2026-07-26** — root-cause fix: the file now owns a disposable, uniquely-timestamped staff fixture (created in `beforeAll`, deleted in `afterAll`) instead of reading whatever staff row happens to exist, matching every other RLS suite's pattern. Can no longer race another file's staff insert/delete lifecycle. Re-verified green (1/1, 200 concurrent check-ins, P99 < 2s, chain valid). | — | Closed |
| **Systemic: multiple RLS test files' `afterAll` don't check their `staff`/`auth.users` delete errors, and orphaned fixture rows recur under back-to-back `pnpm test:rls` invocations** — first found in `tests/rls/dsr_audited.rls.test.ts` during Sprint 3a Task 2 (`dsr-manager@rls-test.invalid` recurred repeatedly), then again in `tests/rls/mark_attended.rls.test.ts` during Task 5 (`markattended-owner`/`markattended-nonowner@rls-test.invalid`) — different files, same root shape (unchecked `.delete()` in `afterAll`, or a run that aborts before `afterAll` completes under connection/socket pressure from repeated invocations in one session). Each orphan silently blocks the next run's `beforeAll` insert with a `staff_email_org_key`/similar violation until manually deleted via the Supabase MCP. Not blocking any Sprint 3a task so far — always caught and cleaned during that task's own review — but worth a real fix (check delete errors + throw, or a session-scoped cleanup sweep) before it costs more time than it has already. | Recurs a third time, or before Sprint 3b adds more RLS test files that would compound it | Sprint 3b/4 |
| **`users.locale` vs `users.display_language` overlap** — Sprint 1 shipped `locale text not null default 'en'` (no CHECK); Sprint 3a Task 1 added `display_language text not null default 'en' check (in 'en','zh-Hant','zh-Hans')` for the same practitioner-facing-language concern, per Q23 item 6. Neither column is read anywhere in the codebase yet (both dead). Found during Sprint 3a Task 1 code review, not reconciled in Data Model.md. | First feature that reads either column — decide then whether `locale` is superseded (deprecate/drop) or genuinely serves a different purpose (e.g. date/number formatting vs. UI translation) | Sprint 3b/4 |
| **`set_staff_role` residual hardening** (Milestone A Task 9 phase-completion review — dev + security lens) — the function's value-safety rests entirely on the `staff_role_check` CHECK defined elsewhere (a drifted/fresh DB without it turns `set_staff_role` into an arbitrary-role setter, and this repo has known migration-drift history), and there is no separation-of-duties guard: an `eventar_staff` can demote/suspend the last active `eventar_staff` (self-lockout) or mint another `eventar_staff`, with only the audit trail as a control. Not a live boundary break (gate is already the top role; every change is audited; CHECK bounds the value on the live DB — backtested), and org-admin delegation is explicitly deferred (item 23 build shipped, delegation model is Sprint 3b). Build when delegation lands: an in-body role allowlist (clean error vs raw `23514`), a "cannot remove the last active `eventar_staff`" guard, and the matching Zod enum on the future Server Action (three-layer validation). | When the org-admin delegation model lands, or first multi-`eventar_staff` operation | Sprint 3b |
| ~~**`anon` in the `staff` column-UPDATE grant-back**~~ ✅ **CLOSED 2026-07-24** — migration `20260724155945_staff_grant_narrow_anon` applied to live Seoul; `has_column_privilege('anon','public.staff','full_name','UPDATE')` = false confirmed (authenticated + service_role retain). Was (LOW-2 / dev M3): Migration `20260723000000_staff_grant_narrow_anon.sql` written (revokes `update (email, full_name, organisation_id, status)` on `public.staff` from `anon`; keeps `authenticated, service_role`; self-verifying DO block) and `seed.sql` updated to drop `anon` from the grant-back so a local `db reset` doesn't re-widen it. **Not yet applied to live Seoul** — the MCP `apply_migration` write was blocked by the auto-mode classifier; Ivan applies via `supabase db push` (uses the file's timestamp) or by re-authorising the MCP apply. Was dormant regardless (staff RLS is `SELECT`-only, so anon writes are default-denied — backtested), so no live exposure while pending. **Re-entry closes when the migration lands on Seoul and `has_column_privilege('anon','public.staff','full_name','UPDATE')` returns false there.** | Apply the written migration to Seoul | This session's follow-up (code ready) |
| ~~**Hard Rule 11 positive round-trip test missing for `staff`'s retained column grants**~~ ✅ **CLOSED, found already shipped 2026-07-26** — `staff_role_mutations.rls.test.ts` test 5 ("service-role update of a non-role column (full_name) still succeeds") was added in `d110c0e` alongside item 45's anon-grant fix, but never marked closed here. Re-verified live against Seoul this session: 5/5 green, including this exact test. Was: HR11 wants a positive round-trip for retained grants, not just the negative on the locked column — this pairs with test 1's `42501` on `role` to pin the column boundary exactly where intended. | — | Closed |
| **`WK-` (workshop) registration-code prefix visible during CPD/regulator demos** (Milestone A Task 6 user-lens review — cosmetic) — codes are `WK-` + 6 chars (`lib/registrationCode.ts`); the Beat-4 pass shows `WK-XXXXXX` and the staff manual-entry placeholder shows a 4-char `WK-XXXX` (real is 6). A sharp accrediting-body observer may notice "workshop" on a clinical seminar. Branding leak, not a defect; changing the prefix touches the (frozen) real app + existing live codes, so deferred. | Frontend unfreeze / first body demo where optics matter | Sprint 3b+ |
| **CPD issuance retry-queue + `pending_credit` state** (CPD MVP execution plan, component J, ⚪ Full) — today a transient technical failure on the inline award (Stage 3) is logged (`[cpd] award_attendance_credit failed`) and left for `reconcile-event.ts` (Stage 6) to backfill on next manual run. No automatic retry, no queue, no `pending_credit` flag surfaced anywhere. Deliberately not built for the MVP — the reconcile script already closes the gap it would solve, just not automatically/immediately. | Real event volume where a manual reconcile-per-event is no longer practical, or a scheduling substrate (pg_cron) exists to run reconcile automatically | Sprint 3b/4 |
| **`creditIssued` status not surfaced in the attendance API response** (component K, ⚪ Full) — `selfCheckIn`/`markAttended`'s response shape is unchanged by design (Runtime Contract: attendance is authoritative, a credit outcome must never affect it), so nothing in the UI can show "credit posted" vs "credit pending" today. Needs an unfrozen surface to display it meaningfully anyway. | First unfrozen attendee-facing surface that would show this (S-Attendee wallet) | Post-M2 unfreeze |
| **CPD kill switch is deploy-time only** (`CPD_ISSUANCE_ENABLED`, env-based, fails open) — no live mid-event toggle. If issuance needs to be killed *during* a real event, it requires a redeploy (Vercel) or an env change + restart (self-hosted), not a runtime flip. Found + explicitly scoped out during Stage 4's live verification (the kill-switch polarity bug). | A config-row/feature-flag substrate exists for something else and can be reused, or a real event needs a live kill mid-run | Sprint 3b/4, or when it actually bites |
| **Real-world CPD issuance rate is bounded by self-serve identity resolution** (Stage 4/5 finding, architecture doc's "identity reality" note) — `award_attendance_credit` only fires for attendees who already have a real account + a verified licence matching the event's body; today that's exactly one seeded identity (Karen Lau) and, in production, will be close to zero real attendees until self-serve accounts + licence declaration ship (S-Attendee, K1). Config-free issuance is real and correct, but its *coverage* — not its *correctness* — is the open question until then. Roster ingestion (bulk-resolving many attendees against a body's member list, rather than one-at-a-time self-serve) is the other mechanism that would raise coverage, tracked separately as a B3 item. | First self-serve attendee account ships, or roster ingestion lands | Sprint 3b (roster) / post-M2 (self-serve) |
| **Global `credit_ledger` advisory lock has no acquisition timeout; per-body chains deferred** (2nd review, point #11) — every issuance serialises through one global lock, awaited on the check-in request. A spiky arrival distribution at a real live event could queue tail requests behind a held lock. `SET LOCAL lock_timeout` (degrade to `skip` rather than block the scanner) is a cheap, recommended-before-real-events knob, not yet added — the MVP's seeded-demo load profile never approaches this ceiling. Per-body/per-event chains remain the real fix at scale, already deferred at the K2/B3 design level. **✅ CLOSED 2026-08-04** (the timeout half; the chain-split stays deferred) — migration `20260804020000_credit_ledger_lock_timeout`, applied local + Seoul. Measured red→green on the local stack with the chain lock held by a second session: **before**, `record_credit_entry` blocked the full 6s ceiling and was stopped only by an explicit `statement_timeout` (57014) that nothing in the product sets — i.e. unbounded in production; **after**, it returns `55P03` at **1074 ms**. `alter function … set lock_timeout to '1s'` — not `create or replace`, because re-declaring a 14-argument body to change one setting is exactly how two agents clobbered each other earlier the same day (`20260803120000`). **Deliberately NOT caught and rewritten to `skipped:lock_timeout`:** 55P03 propagates past `award_attendance_credit`'s `unique_violation` handler to `lib/cpd/awardAttendanceCredit.ts`'s error path, which logs `console.error` and returns `{status:'failed'}` — the correct classification, since a lock timeout is a *technical* failure meaning "reconcile this", not a business skip that logs at info because nothing is wrong (rule 12). The scanner is never blocked regardless: `checkin/confirm/actions.ts` treats attendance as authoritative and ignores the credit outcome. 1s chosen against the burst gate's P99 < 2s budget. **Per-body/per-event chains remain the real fix at scale — still deferred to Stage 10.** | Chain-split only: at scale, with the B3 chain design | Stage 10 |
| **No organiser↔body authorisation model — an organiser can declare their own event accredited by any body** (2026-07-25 three-lens review, HIGH-1; *partially* closed) — the grant-layer hole is shut (`20260725144446` column-locks `accrediting_body_id`/`cpd_hours` so no app role can write them via PostgREST; only service_role paths and definer RPCs can), and `cpd_hours` is now bounded at 24. What remains open is the **product** question the hole exposed: there is no model of which organisations may legitimately claim a relationship with which accrediting body, so when a real CPD-config surface ships, *something* must authorise the claim. Deliberately NOT invented during the review — registry relationships are a B2 decision requiring the body conversation, not a fix to be guessed at mid-hardening. Was true until 2026-08-02 that nothing could set these columns except the seed script. **🔴 NO LONGER TRUE — RE-ENTRY CRITERION FIRED 2026-08-02.** M2 Stage 3 shipped `set_event_cpd_config()` (migration `20260802022345`), the CPD-config surface this row was waiting for. Verified 2026-08-03 on the local stack: `has_function_privilege('authenticated','set_event_cpd_config','EXECUTE')` = **true**, and an organiser was driven end-to-end through the UI binding their own event to PT_BOARD at 3h. NOT a security regression — the grant-layer hole stays shut (columns still locked; only the audited definer writes them), and the function is role-gated (`organiser_member` excluded), owner-checked inside the body, active-body-checked and capped at 24h. What is live is the **self-assertion**: nothing models which organisation may claim which body. `handoff_02082026.md` §5 acknowledged this in prose ("the organiser still self-asserts. Approval workflow is B3 / Milestone C") but this row was never updated. **✅ CLOSED 2026-08-04** — migration `20260804000000_organisation_body_authorisations`, applied local + Seoul. An explicit allow-list (`organisation_body_authorisations`: organisation × body × status, unique pair) is now checked inside `set_event_cpd_config` against the **event's** organisation, with no `eventar_staff` exception — the body's authority is not ours to override. Only *asserting* a claim is gated; **clearing** stays open, since withdrawing is never an assertion of authority and must survive a body revoking the org. Hard Rule 11 posture: `authenticated` revoked at the grant layer for INSERT/UPDATE/DELETE (proven `42501`, not merely RLS-denied), org-scoped SELECT only, `service_role` retains DML as the seeding path. Backfilled from reality — every (organisation, body) pair already in use on an event became active, so no in-flight accredited event was stranded, but nothing else was blanket-granted, so the gate bites from minute one. TDD red→green: watched it fail as a real `organiser_admin` JWT (`expected undefined to be '42501'` — the call *succeeded* and wrote `accrediting_body_id`, i.e. the hole demonstrated rather than described), 8/8 green after. Test: `tests/cpd/organisation_body_authorisation.rls.test.ts`. **Still deferred to Stage 10:** the body-driven approval workflow (organiser submits → reference number → body confirms/suspends). Rows are seeded service-role-side until then. | — | Closed |
| **Attendance-credit window is a generous ±24h bound, not the real check-in window** (2026-07-25 review, HIGH-2; *partially* closed) — `award_attendance_credit` now skips `outside_window` (start−24h .. end+24h) and `cancelled` registrations, which kills the actual fraud the review found (never attend, POST the emailed code days later; or check in on a cancelled registration). But 24h is not the semantically right bound — presence should mean the event's own check-in window (`start − CHECKIN_OPEN_MINUTES`). It can't be tightened yet because the demo fixture registers at Beat 3 and checks in at Beat 4 minutes later, ~170 min BEFORE start, and registration closes exactly when the check-in window opens — so the two beats cannot both sit inside a strict window. Underlying reality: `self_check_in` itself still enforces no time window, no `checkin_modes.self_serve` check, and no publication check; the credit guard is compensating for that at one remove. **✅ CLOSED 2026-08-04** — the root cause is fixed rather than the symptom. Migration `20260804010000_self_check_in_lifecycle_guards`, applied local + Seoul: `self_check_in` now enforces publication (+ `deleted_at`), `checkin_modes.self_serve`, the event's own window `[start − CHECKIN_OPEN_MINUTES, end]`, and a cancelled registration — and its UPDATE predicate tightened from `status <> 'attended'` to `status = 'registered'`, which is what let a **cancelled registration be flipped straight to attended**. TDD red→green: all six guard cases returned `'ok'` before the fix (check-in 3h before the window, after the event ended, on a draft event, on a *cancelled* event, with self-serve explicitly off, and on a cancelled registration), 15/15 after — with a deliberate control test asserting the legitimate pre-start case still works, since the reminder QR goes out exactly at `start − 60`. Refusals return **distinct result codes** (`not_open` / `self_serve_off` / `cancelled` / `unavailable`) each mapped to its own attendee copy in `app/(public)/checkin/confirm/actions.ts`; the previous single `default` told an early attendee their registration was invalid. **`award_attendance_credit`'s ±24h bound is deliberately KEPT** as defence in depth — `mark_attended` (staff scan, trusted physically-present actor) is intentionally not time-gated, so that path still needs a backstop. **Surfaced, not fixed:** closing this proved the registration and self-serve check-in windows are *disjoint by design* (`registerForEvent` closes at `min(registration_close_at, start − CHECKIN_OPEN_MINUTES)`), so the demo run-sheet's Beat 3 → Beat 4 sequence only ever worked because the guard was missing — three options recorded in `demo-run-sheet.md` Beat 4, Ivan's call, none implemented. | — | Closed |
| **`credit_ledger.actor_id` cannot reference a staff actor** (2026-07-25 review, MEDIUM-4; *worked around*) — `actor_id` references `public.users(id)`, but `staff.id` is an unrelated `gen_random_uuid()` (zero staff ids exist in `users`), so the execution plan's literal "pass `actorId = staff.id`" would have raised `23503` on every staff-scanned check-in. The staff path now passes the staff member's **auth user id** instead, which is the correct mapping and is already inside the hash envelope. Residual: `staff`↔`auth.users` are matched by email with no FK, so they can drift — verified live that 1 of 5 Seoul staff rows currently has no `public.users` mirror. `award_attendance_credit` degrades attribution to NULL with a `raise warning` rather than costing the practitioner a credit. A real `staff`→`auth.users` FK (or an `actor_staff_id` column, which would need ledger hash versioning) is the durable fix. | The staff↔auth linkage is formalised (likely alongside the org-admin delegation model, DEFERRED item 44), or an audit asks "which operator issued this credit" | Sprint 3b/4 |
| **UX/perf polish deferred with the frontend freeze** (skeleton loaders, caching, optimistic rendering, tooltips — architecture doc's cross-cutting table) — all four bind to S-Attendee/S-Organiser surfaces that can't change before the M2 review. Each already has a native/already-installed target picked (Next `loading.tsx`, Next 16 fetch/`unstable_cache`, React 19 `useOptimistic`, `@base-ui/react` Tooltip) so the unfreeze plan doesn't have to re-research them — no new dependency for any of the four. | M2 frontend-unfreeze review | Post-M2 |
