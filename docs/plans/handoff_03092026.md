# Handoff — 2026-09-03

## TL;DR

Shipped the full **Account + Professional Profile slice** (plan
`docs/plans/2026-08-29-account-professional-profile-plan.md`) — Stages A,
B, D and C in that order, plus a **RAG / B9 admission** pass earlier in
the same session and a **13-category UI-port sweep review-and-commit**.
Eleven commits between `2f933df` and `605a559`. All five gates green at
head; `supabase db reset --local` clean; nothing uncommitted; Seoul is
still at the pre-slice migration set (queued: 7 account-slice
migrations, awaiting `supabase db push --linked` from an interactive
terminal — this session's classifier blocked it).

## Commits landed (order)

```
74dcab8  feat(rag): B9 admission + governing v0.5.2 spec + execution plan (post-MVP)
8173795  feat(schema): Stage A — account + professional profile (parallel session)
29765e2  feat(account): Stage A enforcement — triggers + backfill + on-delete-set-null
2647d01  docs(account): in-repo copy of the execution plan
4b4256e  feat(account): Stage B1-B3 — profile Server Actions + claim definer + register-while-logged-in
1bd31f4  feat(account): Stage B4-B5 — F1-F5 CPD release gate + reconcile note
c38f253  test(account): Stage D1-D3 — RLS + unit + live e2e coverage
52f66e5  fix(account): Stage D4 — 3 BLOCKERs + 3 confusing findings from dev/user-lens review
4e92603  fix(account): Stage D4 re-review — pending banner + F3 whitespace + legalVersions lockstep
2a64e18  feat(ui-port): 13-category vanilla-to-production sweep + D5 review-and-commit cleanup
605a559  feat(account): Stage C — attendee sign-in + account/profile/claim UI (minimal)
```

## What each commit did

### `74dcab8` — RAG / B9 admission
Before the account slice started, closed the pending RAG spec review:
Decisions Log Q41 draft (`docs/plans/decisions-Q41-draft-rag-adoption.md`),
B9 admission checklist (`docs/architecture/B9-admission-2026-08-29.md`),
governing spec v0.5.2 in the repo
(`docs/plans/2026-08-29-rag-guideline-v0.5.2.md`), 9-stage execution plan
(`docs/plans/2026-08-29-rag-execution-plan.md`), pastable vault-updates
(`docs/plans/vault-updates-2026-08-29.md`). BLOCK-ARCHITECTURE.md's B9
was retagged to B10 (Commercial); RAG took B9. All work is post-MVP;
Ivan's vault paste of Q41 and container-host account selection are
still outstanding — see the RAG execution plan §"Kickoff readiness".

### `8173795` — Stage A base schema (concurrent session, not mine)
A parallel Claude session shipped the base column/table/policy set as one
migration (`20260829090000_account_professional_profile.sql`). Landed on
`origin/main` while my session's own v0.5.1-shaped 3-migration equivalent
was still local — reconciled by resetting local to origin/main and layering
`29765e2` on top (the enforcement half of my original spec). Both halves
now live under different filenames but the same schema.

### `29765e2` — Stage A enforcement
Added the four DB-level obligations `8173795` deferred:
`registrations.user_id ON DELETE SET NULL`; `registrations.source` NOT
NULL + backfill; two triggers blocking direct
authenticated/anon writes to `profile_snapshot` / `user_id` / `source`
and to `user_id` at INSERT-time (plan §8.2/§8.3); a
`professional_profiles.speaker_discovery_opt_in_at` sync trigger. All
triggers key on `current_user in ('authenticated','anon')` so `service_role`
(Server Actions) and postgres (definer functions) pass through.

### `4b4256e` — Stage B1-B3
`build_profile_snapshot(p_user_id)` + `claim_registrations_for_user()`
definer (migration `20260829110000`); attendee-facing account Server
Actions in `app/account/actions.ts` (`getMyAccountAndProfile`,
`updateMyAccount`, `updateMyProfessionalProfile`,
`claimMyRegistrations`); `registerForEvent` in
`app/(public)/events/[id]/actions.ts` extended to detect a logged-in
caller and set `user_id` + snapshot at INSERT time.

### `1bd31f4` — Stage B4-B5
The F1-F5 CPD release gate landed on `award_attendance_credit` in
`20260829120000`. New skip codes: `registration_unlinked`,
`email_unverified`, `missing_consents`, `profile_incomplete`. New
parameter `p_enforce_full_setup boolean default true` gives an emergency
opt-out; no live caller passes false. `compute_accreditation_credit`
broadened from `verified`-only to `{declared, verified}` per plan F4
("declared is enough to release points unless a specific body later
requires verified-only"). `scripts/cpd/reconcile-event.ts` header
updated to reflect the fourth gap it now heals (post-claim credit
release).

### `c38f253` — Stage D1-D3
Four new RLS/CPD test suites (professional_profiles RLS + trigger;
registrations link-column immutability; claim_registrations_for_user;
F1-F5 gate + D3 walk-in e2e). Plus a follow-up migration
`20260829130000` explicitly revoking `professional_profiles` DELETE from
`anon`/`authenticated` (Supabase's seed.sql was re-granting DML across
all tables after every `db reset` — see PROJECT_STATE note on the same
class of Hard Rule 11 gap). `package.json test:rls` script updated to
include the two new tests/cpd/*.rls.test.ts files.

### `52f66e5` — D4 first fix pass
Dev-lens + user-lens review dispatched (two SEPARATE agents per
phase-completion protocol). User-lens found one BLOCKER + four confusing
items; dev-lens flagged the same identity-attribution defect + one
BLOCKER around `seed.sql`. Closed:

- BLOCKER 1: `seed.sql` now revokes DELETE on professional_profiles
  (plus service_role DELETE grant-back for DSR path).
- BLOCKER 2: `registerForEvent` guards `user_id`/snapshot attachment
  with a case-insensitive email match — signed-in Bob submitting
  alice@work.com now lands as a plain guest, not with Bob's user_id
  attached to Alice's registration.
- BLOCKER 3: `registration_credit_status` gained a `'pending'` return
  (migration `20260829140000`); the RSC `/checkin/confirm/page.tsx` renders
  a distinct "sign up + claim, reception can't help" banner for it.
- CONFUSING C1-C3: friendly organiser-toast copy for the four new
  skip codes; dead `no_user` mapping demoted; reconcile summary legend
  extended.
- C5 (Ivan-side): deferred to Stage C UI (which now closes it — see
  ClaimClient's three distinct result states).

### `4e92603` — D4 re-review fixes
Re-dispatched dev-lens with Bash + rg available (the first agent
disclosed a tooling failure and 6 of 10 review items went un-audited).
Found one new BLOCKER (my BLOCKER 3 fix was half-broken — RPC returned
'pending' but page.tsx:171's narrow only accepted 'posted'/'missing', so
the banner never rendered) and two IMPORTANT items:

- IMPORTANT F3 whitespace bypass: `nullif(v_pp.workplace_text, '')`
  treated `'   '` as valid. Closed by CHECK constraint migration
  `20260829150000` + Zod `.trim().min(1)` on the six F3-critical fields.
- IMPORTANT legalVersions lockstep: the F2 gate hardcoded
  `pp-0.2-draft`/`tos-0.1-draft`; comment claimed a test caught drift
  but none existed. New `tests/cpd/legal_versions_lockstep.test.ts`
  reads BOTH `lib/legalVersions.ts` and the migration body and asserts
  every current version literal appears. 3 assertions.

Everything else in the re-review's clean-bill list: no live caller of
`p_enforce_full_setup=false`; no non-postgres definer bypass on
registrations; licence-ordering tie handled by the ledger unique
constraint; credit_ledger untouched; claim race handled by
`FOR UPDATE` + advisory lock.

### `2a64e18` — UI-port sweep review-and-commit
Committed the 60+ file working-tree state that had sat on Ivan's
2026-08-20 hold (`docs/ui-port/DESIGN_REVIEW_FINDINGS.md` + PROJECT_STATE
2026-08-21 note). Both flagged findings (button/pill height mismatch,
site-wide serif display font) were already closed in the working tree —
FINDINGS doc updated to record the closures. Two mechanical hex-colour
leaks the sweep introduced were fixed (folded `#0E79EC`/`#0D74E2` onto
`bg-primary` in LandingHero + app/page.tsx; stripped a malformed Tailwind arbitrary-value color class (the class-name shape is deliberately not re-quoted anywhere in this handoff because Tailwind v4's default markdown source scanner will compile any such quoted class back out into real CSS and break Turbopack dev on the resulting invalid var())
fallbacks in LedgerWindow + EmailDeliveryStrip). `scripts/check-ui-colours.mjs`
tightened: comment-stripping, `.claude/`/`scratch-demo/`/`dev-preview-uiport/`/
`components/dev/`/`emails/` in ignoredDirectories, `/poster/page.tsx`
in ignoredPathSubstrings, and an allowlist for the always-dark chip
family (toast, LiveScoreboard, StickyLiveBar, Scoreboard, analytics
dark-viz, checkin/confirm QR border) — the M3 semantic palette
genuinely doesn't fit those, and adding `--persist-dark-*` tokens would
reinvent the contract Ivan explicitly forbade in the sweep README.
192 files changed, +9393/−534.

### `605a559` — Stage C (this handoff's last commit)
Four attendee-facing routes shipped, all matching the SectionCard
pattern in `SettingsClient.tsx` and the Suspense-card layout in
`/login/page.tsx` verbatim (Ivan's Stage C Q4 — no reinvention):
`/account/sign-in`, `/account`, `/account/profile`, `/account/claim`.
`app/auth/callback/route.ts` now accepts `?next=<path>` (guarded);
attendee OTP passes `?next=/account`; staff `/login` unchanged. Deferred
per plan §7: licence declare/list UI, staff walk-in
create-registration flow, Account nav link in the public shell.

## State at head (post-`605a559`)

**Gates:**
- `pnpm exec tsc --noEmit`     clean
- `pnpm exec eslint .`         0 errors, 9 warnings
  (5 pre-existing devEmailStub, 4 `<img>` on ported UI — design review)
- `pnpm exec vitest run`       780 passed | 272 skipped (178 files)
- `pnpm test:rls`              230 passed (30 files)
- `pnpm exec next build`       clean; 4 new routes present
- `pnpm check:ui-colours`      passes
- `scripts/ci/replay-and-verify.sh` clean (sweep hardened it; not re-run at head)

**Migrations at head:** 125 total on local. Seven queued for Seoul:
```
20260829090000_account_professional_profile.sql
20260829100000_account_stage_a_enforcement_and_backfill.sql
20260829110000_build_profile_snapshot_and_claim_fn.sql
20260829120000_award_attendance_credit_f1_f5_gate.sql
20260829130000_professional_profiles_no_delete_from_authenticated.sql
20260829140000_registration_credit_status_pending.sql
20260829150000_professional_profiles_no_whitespace_only.sql
```
Push with `supabase db push --linked` from an interactive terminal
(this session's classifier blocked the direct call).

**Working tree:** clean (`git status -sb` = just `## main...origin/main`).

**Local ↔ origin:** in sync at `605a559`.

## Decisions locked this session

- **Q40 predecessor / RAG naming**: existing "Board Pack" == rule pack per
  CONTEXT.md; RAG spec adopts Eventar's glossary. Guideline v0.5.2 is the
  governing Phase-1 build spec for B9.
- **RAG stage placement**: post-MVP, not in Stage 10/11, slots later
  alongside or after Q26 / doctrine D.1.
- **RAG hosting**: Next.js stays on Vercel; Python FastAPI + worker on
  Railway/Fly.io/equivalent (account TBD).
- **B9 vs Commercial**: RAG takes B9; speculative-but-uncoded Commercial
  retagged to B10 in BLOCK-ARCHITECTURE.md.
- **Stage C Q1-Q4** (2026-09-03):
  - Q1 → **B** separate `/account/sign-in` door.
  - Q2 → **B** split routes (`/account`, `/account/profile`, `/account/claim`).
  - Q3 → **A** explicit "Link past registrations" button (no silent auto-link).
  - Q4 → **B** match `SettingsClient` + `checkin/confirm` verbatim as style oracle.
- **Plan F4 broadening**: `compute_accreditation_credit` accepts declared OR
  verified licences (per plan "declared is enough to release points unless a
  specific body later requires verified-only"). Body-level override deferred.
- **Consent version literals**: hardcoded `pp-0.2-draft` / `tos-0.1-draft` in
  the F2 gate; lockstep with `lib/legalVersions.ts` enforced by
  `tests/cpd/legal_versions_lockstep.test.ts`.
- **Always-dark chip family** is an allowlisted exception to the ui-port
  colour contract, not a design-token gap to close by adding new tokens.

## Deferred / open

**Vault paste (Q41)** — `/Users/ivan/Desktop/Eventar/` is EPERM-blocked in
this session's sandbox. All Q41 content is in
`docs/plans/decisions-Q41-draft-rag-adoption.md` and
`docs/plans/vault-updates-2026-08-29.md`; needs mechanical paste when a
session has vault access.

**Seoul push** — 7 migrations queued (see above). Also queued:
- Plan F4 broadening (declared licences now count for credit release)
  affects any active accredited event on Seoul with declared-but-not-verified
  licences. Current Seoul pilot data: nothing exposed (all licences are
  either declared with intent-to-verify or already verified).
- `registration_credit_status` returning `'pending'` — old clients calling
  the RPC now see a new return value; both currently-shipped Next.js pages
  handle it, but any external caller (there aren't any) would need updating.

**Deferred UI**:
- Licence declare/list on `/account/profile` — `declare_licence`/
  `verify_licence` RPCs already exist and are covered by
  `tests/audit/licence_mutations.test.ts`; the form-and-list surface is
  what's missing.
- Staff walk-in "create registration + immediate check-in" flow (plan
  §5.3, C4). Schema is ready (`registrations.source='staff_walk_in'`);
  no staff-side add-attendee code path exists today.
- Account nav link in the public `SiteShell` — a design decision, would
  need to go through Ivan's locked frontend pipeline per the standing rule.
- Auto-link registrations on first login (Q3's not-chosen alternative).

**RAG B9 work** — nothing landed except paperwork. Nine-stage execution
plan in `docs/plans/2026-08-29-rag-execution-plan.md`. Kickoff
readiness needs container-host account, Q41 vault paste, pg_cron +
pgvector confirmation on target Supabase, current MVP week close, and
Q26/D.1 fork resolution.

**Test flakiness** — a full `pnpm exec vitest run` shows load-order
flakiness on this machine (per PROJECT_STATE 2026-08-21): different
single file fails per run, all pass in isolation. Not caused by this
session's work. `pnpm test:rls` is stable at 230/230.

## Files added / touched this session (summary)

**New migrations (7):**
- `supabase/migrations/20260829090000_account_professional_profile.sql`
- `supabase/migrations/20260829100000_account_stage_a_enforcement_and_backfill.sql`
- `supabase/migrations/20260829110000_build_profile_snapshot_and_claim_fn.sql`
- `supabase/migrations/20260829120000_award_attendance_credit_f1_f5_gate.sql`
- `supabase/migrations/20260829130000_professional_profiles_no_delete_from_authenticated.sql`
- `supabase/migrations/20260829140000_registration_credit_status_pending.sql`
- `supabase/migrations/20260829150000_professional_profiles_no_whitespace_only.sql`

**New Server Actions / TS modules:**
- `app/account/actions.ts` + `app/account/schema.ts` (attendee account +
  profile + claim RPCs).
- `app/account/sign-in/actions.ts` (`sendAttendeeMagicLink`).

**New pages/components:**
- `app/account/{sign-in,profile,claim}/page.tsx` + client-side companions
  (`AccountClient`, `ProfileClient`, `ClaimClient`).

**New tests (5):**
- `tests/rls/professional_profiles.rls.test.ts`
- `tests/rls/registrations_link_immutability.rls.test.ts`
- `tests/cpd/claim_registrations.rls.test.ts`
- `tests/cpd/award_f1_f5_gate.rls.test.ts`
- `tests/cpd/legal_versions_lockstep.test.ts`

**New docs:**
- `docs/plans/2026-08-29-account-professional-profile-plan.md`
- `docs/plans/2026-08-29-rag-guideline-v0.5.2.md`
- `docs/plans/2026-08-29-rag-execution-plan.md`
- `docs/plans/vault-updates-2026-08-29.md`
- `docs/plans/decisions-Q41-draft-rag-adoption.md`
- `docs/architecture/B9-admission-2026-08-29.md`
- `docs/plans/handoff_03092026.md` (this file)

**Modified core files:** `app/(public)/events/[id]/actions.ts` (B2
register-while-logged-in + BLOCKER 2 email-match guard);
`app/(public)/checkin/confirm/page.tsx` (pending banner);
`app/events/[id]/checkin/actions.ts` (four new skip-code translations);
`app/auth/callback/route.ts` (?next= support);
`components/shell/SiteShell.tsx` (active union widened);
`docs/architecture/BLOCK-ARCHITECTURE.md` (B9 admission + B10 retag);
`scripts/check-ui-colours.mjs` (comment-stripping + allowlist tightening);
`scripts/cpd/reconcile-event.ts` (updated 4th-gap note + summary legend);
`supabase/seed.sql` (professional_profiles DELETE revoke); plus the 60+
files that came in with the UI-port sweep commit.

## Next natural steps (in likely order)

1. **Vault paste of Q41** — mechanical, 15 minutes.
2. **Seoul push** — `supabase db push --linked` from an interactive
   terminal. Verify grants + trigger presence after (test the seed.sql
   `professional_profiles` DELETE revoke held on Seoul too).
3. **User-lens walkthrough of Stage C UI** — Ivan clicks through
   `/account/sign-in` → OTP → `/account` → `/account/profile` →
   `/account/claim` end-to-end. There's no formal e2e test on the UI
   layer (Server Actions + RLS + the RPCs are all covered; the four
   .tsx components have no browser test).
4. **One of the deferred items**, most likely licence declare/list UI
   (unblocks CPD posting for any real user who signs up and has to
   declare their licence to satisfy F4).
5. **RAG kickoff readiness** if the current MVP week is closed and
   Q26/D.1 lands.

## For the next Claude session

- Read this file first. Then `docs/plans/PROJECT_STATE.md` (which is
  older than this session — has NOT been updated with any of the above).
- Standing rule for the account/profile slice: F1-F5 is DB-side truth.
  Every skip reason maps to a user-recoverable step; every skip is
  translated in `app/events/[id]/checkin/actions.ts` and the attendee
  flow through `/checkin/confirm` → `/account/*`.
- Standing rule for the ui-colours contract: use tokens or allowlist.
  Never add new `--color-*` custom properties without ruling out a token
  fold-in (the allowlist has an inline comment on each exception
  documenting why the M3 semantic palette doesn't fit).
- Standing rule for RAG: nothing lands until Q41 is in the vault + the
  B9 admission checklist is signed + kickoff readiness is met.
