# Handoff — 2026-08-14 v2 (terminology alignment + close-out)

_Continues `handoff_14082026.md` (the full-stack review — docs consistency, the review-mode auth gap, grant hygiene, deps, tests/CI, the full lifecycle backtest). This addendum covers what happened after that doc was written: reviewing an external strategy document, one concrete architectural finding from it, and a terminology-alignment pass across the live roadmap docs._

## 1. Committed this session

Two commits landed on `main` (not pushed to a remote — this repo has no `origin` push in scope beyond Supabase):
- `6c27219` — Task 10.1/10.7: seed MCHK + 15 HKAM Colleges, close the empty-authorisation gate.
- `ae6d270` — full-stack review: review-mode auth gap, grant hygiene, deps, docs consistency.

**Not yet committed** (this session's terminology-alignment work, done after the second commit): `docs/DEFERRED.md`, `docs/plans/2026-08-10-dual-track-stage-scope.md`, `docs/plans/PROJECT_STATE.md`. Small, low-risk, doc-only. Ask before I commit again — didn't assume continued blanket permission from the earlier "commit" instruction.

## 2. Strategy document reviewed — `Eventar_Strategy_Detailed_Summary.txt`

Ivan handed over a 4-part consolidated strategy document (business positioning, AI direction, agent-access-point vision, HK CPD/CME reframing, OSINT on competitor OccuInfo) and asked for a critical review. Findings:

- **Part C (the HK CPD/CME reframing) matches the actual codebase** — verified directly, not taken on faith. The body-centric schema, the four ADR-0001 corrections, the "no medical-only CHECK" genericity constraint (already recorded against Task 10.3) all check out against what's actually built.
- **One stale claim, now fixed**: the document correctly caught that `docs/plans/2026-08-10-dual-track-stage-scope.md` still said rule packs live in `lib/cpd/rulePacks/` — they don't, they live in `accrediting_bodies.category_taxonomy` (Task 10.1's own Decision 11). Fixed this session.
- **One stale claim now moot**: the document said "Task 1 remains the unblocker" — it shipped 2026-08-13, before this document was written.
- **Real architectural finding, verified against the schema**: the document's R&D thesis ("verifiable multi-authority credit protocol" — one event evaluated against multiple accrediting bodies at once) has **no path to reality today**. `events.accrediting_body_id` is a single scalar UUID, confirmed directly against `information_schema.columns` — no join table, no array. One event, one accrediting body, full stop. Nothing in Tasks 10.1–10.7 adds a many-to-many. This is a real gap between the R&D thesis and the current data model, not a nitpick — see §3 below, it's the biggest open decision.
- **One scope-clarity finding**: the document's Part A/B (guest-ops platform, agent access point, LinkedIn sharing, multi-profession expansion beyond CPD) has **zero representation in `STAGES.md` or the Task 10.1–10.7 plan**. Nothing is being built for it. Not wrong to have as vision, but worth naming plainly rather than letting it sit as an implicit assumption.

## 3. Terminology alignment — Stage.Task decimal notation, locked

Ivan asked for exactly two axes, deliberately different alphabets: numerical for the engineering roadmap, alphabetical for the strategy document's own structure. Landed as:

| Axis | Format | Example | Scope |
|---|---|---|---|
| Engineering roadmap | `<Stage>.<item>/<Stage>.<total>` | `10.4/10.7` = Stage 10's 4th of 7 tasks | Live in the repo |
| Strategy-doc structure | `<Part>.<section>/<Part>.<total>` | `A.3/A.9` = Part A's 3rd of 9 sections | Only in the external doc, not referenced anywhere in the repo — nothing to convert there |

What "Task 1–7" became "Task 10.1–10.7" in: the actual plan file (`~/.claude/plans/target-product-for-2026-proud-castle.md`, outside the repo), `docs/DEFERRED.md`'s one forward-pointing reference, `docs/plans/PROJECT_STATE.md`'s CURRENT banner. **Historical/dated references were deliberately left alone** — the already-applied migration's own header comment (`20260813000000_seed_hkam_colleges_and_mchk.sql`, still says "Task 1"), past commit messages (immutable), and dated past-tense narration in `PROJECT_STATE.md`/`DEFERRED.md` describing what happened on 2026-08-13. Same rule `STAGES.md` already uses for its own renumbering: live/forward-pointing references get the new term, historical narration keeps the old one. Confirmed by full grep sweep — no stray old-numbered references left in the live 2026 dual-track work; the dozens of unrelated "Task N" mentions from old Sprint/Milestone plans elsewhere in `docs/plans/` were correctly left untouched (different work entirely, not this axis).

## 4. Gates

Nothing code-level changed this addendum — pure documentation. Last real gate run (end of `handoff_14082026.md`'s work) still holds: tsc clean · eslint 0 errors · vitest 583|174 · next build 22 routes · replay-and-verify PASS · RLS suite 162/162 · migrations 97 local / 94 Seoul.

## 5. What's needed from Ivan to move forward

Genuine open items — not busywork, things that actually change what gets built next:

1. **Push 3 migrations to Seoul.** Same command as before, same classifier block here:
   ```
   cd /Users/ivan/Eventar && supabase link --project-ref muieupgkpbxpqsrjjwol && supabase db push --linked
   ```
   Migrations: `20260814020000` (review-mode publish/check-in fix), `20260814030000` (review-mode eligibility fix), `20260814040000` (PT_BOARD→PTB rename).

2. **Multi-body-per-event — in scope for 2026, or explicitly Stage 13+?** This is the one that actually blocks a design decision, not just a nice-to-know. `events.accrediting_body_id` is single-valued today; real HKAM practice sometimes has one conference co-accredited by several Colleges at once, and the R&D thesis in the strategy doc assumes that's supported. If it's in scope for launch, it needs a schema decision (an `event_accreditations` join table, a rework of `award_attendance_credit`'s single-body lookup, cap-conflict handling across bodies sharing one event) that isn't in Task 10.1–10.7 anywhere — and would need slotting in before Task 10.3 (FCAA category) goes much further, since category assignment is per-registration-per-body. If it's explicitly Stage 13+, nothing changes right now and I'll proceed with Task 10.2 as scoped.

3. **Part A/B (the broader platform vision) — does it get a Stage number, or stay explicitly out of scope for now?** Not urgent, but worth a decision rather than an implicit assumption either way, since it changes what "what's next after Stage 13" means.

4. **The disabled "Accreditation" sidebar nav item** — still an open naming-collision question from the 2026-08-13 review (a real, working CPD accreditation section sits inline on the event-details page while the sidebar shows a separate, unrelated "Accreditation" stub as "not built yet"). Needs a product call about what that future surface actually is, not something I should guess at.

5. **Docker/local-stack flakiness** — recurring this session (stuck `supabase start`, a lock conflict needing a hard `pkill`). Not blocking, just flagging per the standing note that it's already a known, recurring cost.

**Not waiting on anything to start Task 10.2/10.7 (primary licence per body)** — it's fully scoped in the plan file already (replace the primary-licence unique index, add `track` to the licence, extend `declare_licence`, close DEFERRED 30) and doesn't depend on how #2 or #3 above resolve. Say the word and I'll start there.
