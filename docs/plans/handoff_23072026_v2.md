# Handoff — 2026-07-23 (v2, backend thread) — graph refresh done + backend hardening (1 item prod-apply-blocked); MVP reframe recorded

> **NOTE:** this is the **backend/graph thread's** handoff for 2026-07-23. `handoff_23072026.md` (no suffix) is the **parallel design-exploration thread's** handoff — different work, different area. Both are real; don't merge them.

> **Resume line for the next session:**
> *"Read docs/plans/handoff_23072026_v2.md. Milestone A is shipped + protocol-passed (see handoff_22072026.md). This thread then (1) completed the graph refresh (`graphify-out/` now 1635 nodes; 2–3 handoff docs still pending extraction — retry `/graphify --update`), and (2) ran the 'backend hardening' MVP track Ivan authorised. ONE thing needs Ivan's hand: the anon-staff-grant migration (`supabase/migrations/20260723000000_staff_grant_narrow_anon.sql`) is written + verified-in-logic but its live-Seoul apply was blocked by the auto-mode classifier — apply it via `supabase db push` (or re-authorise MCP apply_migration), then confirm `has_column_privilege('anon','public.staff','full_name','UPDATE')` = false on Seoul and mark DEFERRED.md LOW-2 closed. MVP itself is gated on Ivan's decisions + the body review, NOT engineering — see the reframe below."*

## What this thread did since handoff_22072026.md

### 1. Graph refresh (Task 12 from the prior handoff) — ✅ DONE, with a tracked gap
`/graphify --update` after the weekly limit cleared. **1635 nodes / 2668 edges / 183 communities** (up from the Jul-8 baseline of 1306; diff +365/−36 nodes — stale re-extracted versions correctly pruned, not duplicated). All 183 communities hand-labelled. `graph.html` / `GRAPH_REPORT.md` / `manifest.json` regenerated; health check clean (the 1 self-loop is a pre-existing May-doc artifact).
- **Gap left open honestly:** extraction subagents hit three infra walls in a row (weekly limit → session limit → a 64K-output-token ceiling on the retry). `handoff_17072026.md` + `handoff_22072026.md` never got real extraction, so they were **excluded from the manifest save** (not silently marked done) — they + the new `handoff_23072026*.md` files will re-appear as pending on the next `/graphify --update`. Do that when convenient; it's cheap (AST cache warm, only a few docs need the LLM).
- **Graph accuracy note:** the fresh graph asserts two `EXTRACTED` edges that are FALSE — `rateLimit.ts --imports--> abuseTier.ts` and `rateLimitBySession() --calls--> recordAbuseHitAndMaybeRevoke()`. Verified against source: the real direction is one-way `abuseTier → rateLimit` (a reverse edge would be a circular import). Logged as a `corrected` outcome in `graphify-out/memory/`; washes out on the next full (non-`--update`) rebuild.
- **Architecture insight from the graph tour** (verified against source, worth keeping): `requireStaff()` is the degree-50 chokepoint bridging 16 communities — Hard Rule 3 made structural. `withSecurity()` + `lib/abuseTier.ts` are fully built but have **zero live call sites** — pre-positioned substrate awaiting the first authenticated attendee mutation surface (post-freeze), exactly as `DEFERRED.md` says.

### 2. Backend hardening track (Ivan authorised via AskUserQuestion) — 1 of 2 code items fully landed
Ivan's pick when asked to "push to MVP": **backend hardening** (over "authorize D0 deploy" or "scoped frontend unfreeze"). Executed:

- **✅ Hard Rule 11 positive round-trip test** (review finding M4) — added test 5 to `tests/rls/staff_role_mutations.rls.test.ts`: a service-role UPDATE of a non-role column (`full_name`) succeeds, proving the retained column grant from `staff_role_update_lock_fix` works. Paired with test 1's `42501` on `role`, it pins the column boundary exactly at `role`. **Ran green: 5/5 (was 4/4)** via `RLS_TESTS=1 vitest run tests/rls/staff_role_mutations.rls.test.ts` against live Seoul. Non-vacuous by construction (same admin client gets `42501` on the locked column in test 1).
- **🟡 anon staff-grant narrowing** (review finding LOW-2) — **CODE DONE, PROD APPLY BLOCKED.** `supabase/migrations/20260723000000_staff_grant_narrow_anon.sql` written (revokes `update (email, full_name, organisation_id, status)` on `public.staff` from `anon`; keeps `authenticated, service_role`; self-verifying DO block), and `seed.sql` updated to drop `anon` from its grant-back re-assertion (otherwise a local `db reset` would re-widen it after the migration replays). **The live-Seoul apply was blocked by the auto-mode classifier (2 attempts) — same guard that gated the git push earlier.** Not applied. Dormant either way (staff RLS is `SELECT`-only, so anon writes are default-denied), so no live exposure while pending. `DEFERRED.md` LOW-2 updated to the honest 🟡 partial state — **do not mark closed until the migration lands on Seoul.**

### 3. Deploy-readiness audit (the safe reading of "get ready for deployment") — ✅ DONE
- **Static gates:** tsc clean · eslint 0 err / 5 pre-existing warn (`lib/devEmailStub.*`) · next build OK, **19 routes** · vitest **460 pass + 1 FLAKY** — `app/events/new/NewEventForm.test.tsx > "clears the save confirmation on the next submit attempt"` fails under full-suite concurrency but passes **21/21 in isolation**; zero changes to `app/events/new/` this thread. **Real readiness finding:** this flake can red CI on a full run — candidate for the standing flaky-test cleanup already tracked in `DEFERRED.md`.
- **Secrets scan:** clean — no key literals in tracked source (only `.env.example` placeholder + `dev-local.sh` reading from env).
- **Env-var gap (expected):** code reads `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `NEXT_PUBLIC_SITE_URL`; none are in `.env.local` (Resend cutover deferred → `devEmailStub` in use; SITE_URL set at deploy time). Not a bug — the documented pre-deploy state.
- **Migrations:** 70 local files = 69 applied to Seoul + 1 pending (the blocked one).

## The MVP reframe (recorded — this is the real critical path)

When asked to "push to MVP / get ready for deployment," the honest map was surfaced and Ivan chose the unblocked track. **MVP is not engineering-blocked.** The three walls are Ivan-locked decisions + one external dependency:
1. **Frontend freeze** — blocks Task 1b (role-union widen needs `StaffShell.tsx`/`SettingsClient.tsx`). Holds until the M2 review.
2. **Deploy paused by D0** ("no deploy by momentum") — and a literal deploy isn't clean anyway: canonical prod is a **fresh Singapore** project (never provisioned; live is still Seoul), pricing is still `[PRICE]`, Turnstile keys unprovisioned, email still `devEmailStub`, PDPO privacy notice needs sign-off.
3. **MVP = one real body piloting** — gated on the HKCP manual (Ivan sources), the review meeting (unscheduled), and Q26. Per the roadmap: *"the pilot lands ~10–14 weeks out, dominated by the body's schedule, not engineering."*

Highest-leverage move remains **scheduling the body review** — it unblocks Sprint 3b's design, resolves Q26, and produces the pilot body in one conversation.

## Uncommitted / unpushed state at handoff

- **This thread's changes** (committed in this session's final commits — see git log): the HR11 test, the anon-grant migration file, `seed.sql`, `DEFERRED.md`, and this handoff.
- **Parallel design thread:** 2 commits (`060a5c8`, `4f25117`) were ahead of `origin/main` at this thread's start (design-exploration + `handoff_23072026.md`). Review + push separately.
- **`graphify-out/`** churn (regenerated graph + cache) is uncommitted — commit or gitignore per your convention (it was untracked/modified at session start too).

## Ivan's inputs outstanding (carried, still open)

1. **Apply the anon-grant migration to Seoul** (`20260723000000_staff_grant_narrow_anon.sql`) — the one new action item this thread produced.
2. `[PRICE]` in `docs/collateral/one-pager.md` (+ outreach email/phone).
3. First internal-meeting date; the body-review schedule.
4. D0 deploy flip (+ the Singapore provisioning, Resend cutover, Turnstile, privacy-notice sign-off it gates).
5. Whether to reconcile the vault `CPD Roadmap — Backend First.md` Sprint body into the A–E spine (flagged 2026-07-22, still Ivan's call).
