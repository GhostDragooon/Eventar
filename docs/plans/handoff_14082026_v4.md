# Handoff — 2026-08-14 v4 (documentation alignment across repo, vault, graphify)

_Continues `handoff_14082026_v3.md` (the multi-body design record, written on explicit instruction to stop before building). This addendum covers a separate conversation thread: two more prospect documents were reviewed (regulatory-source injection + grant strategy; multi-association engine + expansion pivot), producing refinements rather than new decisions, then S3 (prior-approval deadline advisory) was chosen as the next build, then the full alignment pass this handoff records. Still no code — this is documentation and index work only._

## 1. Refinements reviewed, not decisions

Two more prospect documents were internalised and given structured pushback (R1–R8, S1–S10, M1–M8 — full detail in conversation, summarised into `docs/DEFERRED.md` below). None of these are Ivan's decisions; they are Claude's recommendations from reviewing the prospects, and are recorded everywhere as hedged proposals. The one exception is a set of corrections verified directly against the live code (M6: multi-membership already shipped; M8: CNE already on the ICI Summit sheet), which are recorded as fact since they're independently checkable, not judgment calls.

## 2. S3 chosen as next work (D-B)

Ivan chose the prior-approval deadline advisory ("S3", now **Task 10.9/10.9**) as the next build, ahead of Task 10.2 and the multi-body work (**Task 10.8/10.9**). Investigated but not built this session:
- **No schema migration needed** — `accrediting_bodies.cycle_config` is an existing not-null jsonb that already carries deadline-shaped facts (IA's `report_by_month`/`report_by_day`, MPFA's `pro_rata_first_cycle`).
- **Frontend change is MECHANICAL** — `CpdAccreditationSection.tsx` already has three message-banner states with settled styling; a deadline line matches its siblings.
- **Advises, never blocks** — Eventar can't see out-of-band applications; `set_event_cpd_config` gains no new gate.
- **Real risk is timezone** — an event at 00:30 HKT is the previous calendar day in UTC, and this is a legal deadline.
- Scoped out deliberately: "mark as applied", reference numbers, body confirmation — `set_event_cpd_config`'s own header already gates those to B3/Milestone C.

Full design captured in the plan file at `~/.claude/plans/crispy-tinkering-candy.md` (not part of this repo).

## 3. Task numbering resolved

Multi-body = **Task 10.8**. S3 = **Task 10.9**. Stage 10's total is now **10.9** (was 10.7). Task 10.9 is built *before* Task 10.8 — the number is an identity, not a build-order index; this is stated explicitly everywhere the fraction appears so a future reader doesn't "fix" the apparent inconsistency.

## 4. Documentation alignment — three parallel subagents (Wave 1), partitioned by file

- **W1-A** (repo state): `docs/plans/PROJECT_STATE.md`, `docs/DEFERRED.md`, `docs/plans/STAGES.md`, `docs/plans/2026-08-10-dual-track-stage-scope.md`, `~/.claude/plans/target-product-for-2026-proud-castle.md`.
- **W1-B** (architecture/doctrine/vocabulary): `docs/architecture/BLOCK-ARCHITECTURE.md`, `docs/doctrine.md`, `CONTEXT.md`, new `docs/adr/0002-multi-body-accreditation.md`.
- **W1-C** (Obsidian vault): `02 — Decisions Log.md` (appended Q34–Q37 for D-A through D-G), `20 — Roadmap/CPD Roadmap — Backend First.md`, `30 — Reference/Out of Scope.md`, `00 — Index.md`.

All three confirmed no PROPOSED item was written as a settled decision anywhere — the one rule that mattered most.

**One contradiction found and fixed directly (not by a subagent):** the vault's `Out of Scope.md` still had a stale row — "Multi-body simultaneous event accreditation submission... deferred" — directly contradicting D-A now that multi-body is in scope. Its own re-entry trigger ("first multi-body event confirmed") had literally fired (the ICI Summit). Removed per the register's own stated convention ("removed when they re-enter scope, linked ADR"), pointing to ADR-0002/Q34.

## 5. Graphify refresh (Wave 2)

`--update` against the repo. Manifest was 2026-08-12 19:33, two days stale — predated the 2026-08-13 seed migration and every 2026-08-14 change. 37 changed files detected (19 code, 18 docs — everything Wave 1 touched, plus unrelated churn from other work in `app/`). AST extraction: 116 nodes/313 edges. Semantic extraction (no `GEMINI_API_KEY` set, so a dispatched subagent served as the LLM per the skill's default): the subagent's own status reported "failed" (hit the account's session limit mid-run), but the chunk file had already been written to disk before the limit hit — 172 nodes, 283 edges, 3 hyperedges, schema-validated clean (0 bad `file_type`, 0 bad node-id chars, 0 dangling edges, all `confidence_score` present). Per the skill's own rule, the file on disk is the success signal, not the agent's self-reported status — treated as a successful chunk.

Merged into the existing graph: **2274 nodes, 3272 edges, 253 communities** (was ~2236/~3216 pre-update — diff showed 137 new nodes/298 new edges, 99 removed/242 removed, the removals being `build_merge`'s expected replace-on-re-extract of every node sourced from a file that just got its content rewritten, e.g. the old `PROJECT_STATE.md`/`DEFERRED.md` nodes). Health check: 2 self-loop edges flagged, both pre-existing artifacts unrelated to this session's files (`app_events_new_page_accordion`, `scripts_backtest_send_ledger`) — not fixed, out of scope.

253 communities was too many to hand-label individually for what this update actually needed. Hand-labeled the 11 communities containing today's changed docs (Repo Rules & Block Architecture, CPD Glossary & Vocabulary, ADR-0002 Multi-Body Accreditation, Doctrine Open Forks, etc.); the remaining ~242 pre-existing communities got a degree-based heuristic label (highest-degree member's own node label) rather than manual authorship, since they're unrelated to this session's work.

## 6. Two-agent review (Wave 3) — separate consistency and cold-reader lenses

Per the phase-completion protocol's separate-lenses rule, dispatched as two independent read-only agents rather than one:

**W3-A (consistency lens)** — cold-read every Wave 1 file plus the code it describes. Highest-severity check (no PROPOSED item written as DECIDED) came back clean everywhere, including the vault Decisions Log. Independently re-verified M6 against actual migrations/`lib/cpd/` code rather than trusting any doc — confirmed correct. Found two real defects:
1. `docs/adr/0002-multi-body-accreditation.md` self-contradicted its own body count — opens stating "13 bodies" (11 Colleges + MCHK + CNE, correct) but its "Proposed consequence" section said "the sixteen bodies on a single sheet", an apparent bleed from ADR-0001's unrelated "sixteen categories" figure. **Fixed** — corrected to "13 bodies".
2. `PROJECT_STATE.md` claimed its M6/M8/S10 corrections were "recorded as fact in `docs/DEFERRED.md`'s change log" — they were not; `DEFERRED.md`'s change log only lists the 9 PROPOSED rows. **Fixed** — rewrote the sentence to state the corrections inline instead of pointing at a file that didn't contain them.

**W3-B (cold-reader lens)** — simulated a fresh session with zero memory, following only `CLAUDE.md`'s own prescribed reading order. Verdict: the trail works end-to-end — reached Stage 10 / Task 10.9-next / the correct "why" without external help, and the DECIDED-vs-PROPOSED split held up without being told in advance to look for it. Found two friction points:
3. `PROJECT_STATE.md`'s 2026-08-14 full-stack-review entry stated "3 new migrations... not yet pushed to Seoul" as a live fact, only self-corrected 14 lines later — a cold top-to-bottom read briefly held a wrong belief. **Fixed** — converted to the repo's own established strikethrough-and-close convention (matching the existing "SEOUL IS ONE MIGRATION BEHIND ✅ CLOSED" pattern already in the same file) instead of leaving a stale claim to be corrected later.
4. `handoff_14082026_v2.md` and `_v3.md` — which contain the actual `event_accreditations` design sketch behind D-A — were never cited from any live document; only the base `handoff_14082026.md` was. A reader trusting the citation trail would never find the design detail. **Fixed** — added the citation to `PROJECT_STATE.md`'s 2026-08-14 prospect-session entry.
5. Minor: the `/10.9` fraction notation was used unexplained at the very first line of `PROJECT_STATE.md`'s banner, defined only later in the file. **Fixed** — added a 6-word inline gloss at first use.

All five fixed directly (not re-delegated — small, unambiguous, single-file edits).

## 7. Gates

Full static suite re-run after all fixes (doc-only changes, but per the phase-completion protocol the gates run before any commit regardless): tsc clean · eslint 0 errors (5 known pre-existing warnings) · vitest **583 passed | 174 skipped** · `next build` **22 routes**. All match the pre-session baseline exactly — nothing moved.

## 8. Commits — one, not two, and here's why

The original plan called for two commits: the 3 doc files pending since `ae6d270` first (on their own), then the Wave 1–3 alignment work second. That split is no longer possible — Wave 1's `W1-A` agent edited `PROJECT_STATE.md`, `DEFERRED.md`, and `2026-08-10-dual-track-stage-scope.md` starting from their already-pending-modified state, so the pre-existing terminology fix and today's new content are now interleaved in the same lines with no clean git boundary between them. Reconstructing an artificial split would mean manually attributing individual lines to the wrong commit rather than actually separating anything real. One commit for all documentation/vault-adjacent alignment work instead, with this noted in the commit message.

GitHub push not yet done as of writing this handoff — see "Remaining" below.

## 9. GitHub repo description — drafted, not applied

Still reads "Internal Workshop Manager". Ivan confirmed this is in scope; the description was drafted (pulled from `CONTEXT.md`'s own opening line, not invented) and is pending application. `gh` is not authenticated in this session (`gh auth login` or `GH_TOKEN` needed), so Ivan applies it himself either via CLI or the GitHub UI. Draft:

> CPD/CME compliance platform for regulated professions in Hong Kong. Practitioners attend accredited events; Eventar records attendance and issues tamper-evident credit their regulator will accept.

## 10. Remaining

- Push the pending commit(s) to `origin/main` (2 commits ahead before this session's commits, plus whatever lands here).
- Apply the GitHub repo description (§9) — Ivan's action, `gh` not authenticated here.
- Wave 5 (subagent execution plans for S3 and the multi-body build) — not started this session; the plan file has the scoping detail for S3 already investigated (§2 above).
- Open inputs still blocking multi-body specifically: does "Both Days" always equal Day 1 + Day 2 across all 13 ICI bodies (R2, only 3 of 13 checked); certificates one-per-body or combined; licence cycle anchor source (M2); whether Eventar ever ingests external CPD history (decides if M1's advisory framing is permanent); grant timeline; pilot event identity.
