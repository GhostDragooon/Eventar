# Handoff — 2026-07-12 (Milestone A execution started; paused on session limit)

> **Resume line for the next session (after 23:10 HKT):**
> *"Read docs/plans/handoff_12072026.md, then execute docs/plans/2026-07-12-milestone-A-executable.md from Task 2 via superpowers:subagent-driven-development, honouring each task's embedded guardrails."*

## What today produced (all committed on `main`, unpushed — Ivan pushes per cadence)

**Strategy & planning layer:**
- **Q29** (vault): terminology locked — practitioner module = **"CPD Ledger"**; "CPD Passport" retired, refers only to the competitor. Swept across live docs; collateral unblocked.
- **Q30** (vault): payments posture — pass-through only, Stripe as placeholder direction, structured to stay outside HKMA SVF/MSO licensing; invoice-first through Milestone E.
- `2026-07-12-market-realignment.md` — production-grade expectation map vs build; top gaps: external activities + evidence locker (E-W1), session/track model (E-W2), verify-record URL (M3), calendar links, walk-ins, online-CME MCQ gate.
- `roadmap-to-mvp.md` — consolidated milestone spine A→E (one table, gates, wall-clock honesty).
- `2026-07-12-program-plan-A-to-E.md` — scope/spec/outcome + execution per phase; Ivan's six answers folded in (incl. **RLS-scoped concurrent-event visibility for admin roles** and external-entries-as-backfill framing); "production-ready at end of E" = 4-part checklist + acceptance test (body #2 onboards unaided).
- `2026-07-12-milestone-A-executable.md` — full 14-task TDD plan (environment facts baked in).
- `2026-07-12-execution-plans-index.md` — B deploy runbook, C checklist, M2/M3/D/E skeletons with explicit detailing gates (M2 SQL stays gated on the body review + Q26).
- `2026-07-12-organiser-ia-spec.md` — organiser IA (sidebar + counted tabs, PDPO-bounded Participants, Accreditation module) — feeds task #8.
- Vault reference catalogs: `CPD Source Documents` (12 body documents; **HKCR retention = verified absence**, closing that open item), `Competitor Analysis — CPD Passport` (differentiator confirmed: real-time first-party attendance vs self-report + ~30-working-day third-party verification), `UI-UX Design References` (13 new images unreviewed; **Schedule folder = multi-track agenda scope signal, unconfirmed — ask Ivan before building toward it**), `Agent Working Method` (the full review/decision/debugging protocol; mirrors `~/.claude/DECISION_PROTOCOL.md`).

## Milestone A execution state

| Task | Status | Evidence / notes |
|---|---|---|
| T1 demo helper + tsx | ✅ `5167071` + pg follow-up | **Fully verified end-to-end**: `localEnv` (URL + keys) ✓, `sqlLocal` staff count ✓, `verify_audit_chain()` = 0 rows ✓. Real bug found + fixed in the process: `docker exec` from nested child processes ETIMEDOUTs in this harness → replaced with a direct `pg` connection to `:54322` (superseding note added to the plan header; use `sqlLocal`, not `psqlLocal`). Verifier return shape confirmed for T7. |
| T11 collateral | ✅ `67920d2` | Three drafts in `docs/collateral/`; only `[PRICE]` pending (Ivan). Q29 enforced. |
| T2–T5 demo scripts | ⏳ pending | Local stack running; seed fixture spec in plan + run sheet. |
| T6 rehearsal backtest | ⏳ pending | Needs T2–T5; browser flow proven 2026-07-11 (worktree `:3100`, Mailpit login). |
| T7–T8 CI harness | ⏳ pending | Verify the two chain-verifier signatures against migrations before finalizing assertions. |
| T9 set_staff_role | ⏳ pending — **live Seoul; security lens mandatory** | Subagent died pre-work (only reads; zero writes — tree verified clean). Column-level-revoke design already decided in plan. |
| T10 grant hygiene + afterAll | ⏳ pending — live Seoul; after T9 | Runs the ONE full `pnpm test:rls` of the session. |
| T12 graph refresh | ⏳ pending | Controller-run (graphify needs the main session to dispatch extraction agents). Abort-cleanly rule binds. |
| T13 three lenses | ⏳ pending | dev + user + security-reviewer (`everything-claude-code:security-reviewer`) + backtest = T6/T7 reruns. |
| T14 close-out | ⏳ pending | PROJECT_STATE block, handoff, vault status, board. |

## Why paused + operational notes for the resumer

- **Account session limit** (resets **23:10 HKT 2026-07-12**) killed all three Batch-1 subagents mid-flight (T1/T9/T11 — T1 and T11 were then completed inline by the controller; T9 died before any writes). Subagent dispatch is dead until reset; don't retry before it.
- Batch discipline when resuming: **serialize local-stack users** (T2→T3→T4→T5; T7 resets the stack — never concurrent with them) and **serialize the two live-DB tasks** (T9 then T10). Parallel-safe pairs are listed implicitly by those constraints.
- Late-session this session, foreground Bash began auto-backgrounding (task saturation); a few stale verifier background jobs may linger — harmless, ignore.
- Working tree at handoff: clean except the standing `graphify-out/cache/stat-index.json` modification (AST cache — leave it; it rides with T12's commit).
- Local stack: RUNNING with migrations at the 3a tail; T9/T10's new migrations reach local automatically on the next `db reset` (T7 does one).
- `pnpm test:rls`: **once per session** (socket exhaustion) — T10 owns the run.

## Ivan's inputs outstanding

1. `[PRICE]` in `docs/collateral/one-pager.md` (+ his email/phone in the outreach draft).
2. First internal-meeting date (Milestone B / E1).
3. D0 deploy flip (gates Milestone B's deploy track — not needed for A).
4. Push the commit backlog (per end-of-session cadence).
5. Standing from earlier: pilot-event-shape said "expandable" (encoded); Schedule-folder intent still worth a one-line confirmation before any session/track build.
