# Handoff — 2026-07-22 (Milestone A phase-completion protocol RUN + PASSED, close-out written, backlog pushed; graph refresh still owed — weekly-limit-blocked)

> **Resume line for the next session:**
> *"Read docs/plans/handoff_22072026.md. Milestone A is done and pushed — the owed Task 13 (three-lens phase-completion protocol) + Task 14 (close-out) both landed this session. The ONE thing still owed is Task 12 (graph refresh via `/graphify --update`), which was attempted this session but hit the hard weekly account limit mid-extraction and was aborted cleanly (graph.json untouched at the Jul-8 baseline, no manifest trap). Retry `/graphify --update` once the weekly limit resets — AST cache is warm, 6/29 docs already semantic-cached, ~23 docs + 66 code files pending. Then it's Ivan's call on next milestone (B field-proven vs continuing to C body review) per docs/plans/roadmap-to-mvp.md — all gated on Ivan's outstanding inputs (price, first-meeting date, D0 flip)."*

## What this session did (2 commits on `main`, both pushed)

Picked up from `handoff_17072026.md`, whose resume line owed **Task 13 (three-lens phase-completion protocol) + Task 14 (close-out)** before Milestone A could be called done, plus a push of the commit backlog. All three landed.

| Commit | What | Verified how |
|---|---|---|
| `2876e06` | **Task 13 fix** — `seed-demo.ts` timing 90→180min + run-sheet/`DEFERRED.md` corrections | `reset-demo.ts` re-run end-to-end: reseed printed a 120-min registration window, both chains clean; tsc + eslint green |
| `31cfeb4` | **Task 14 close-out** — PROJECT_STATE.md Milestone A section + vault roadmap drift-flag | doc-only; structure verified (slotted after Sprint 3a) |

Both pushed to `origin/main` (`GhostDragooon/Eventar`) — the full 37-commit backlog that had accumulated across this + the prior + the parallel Opus design session is now published. `origin/main` is at `31cfeb4`, 0 behind.

## Task 13 — the three-lens phase-completion protocol, actually run (was owed, not done in the prior session)

Ran per CLAUDE.md's mandatory protocol: **three separate agents + a live backtest**, none trusting the prior session's self-review.

- **Static gates:** tsc clean · eslint 0 err / 5 pre-existing warns (`lib/devEmailStub.*`) · vitest **461 pass | 106 skip** · next build OK, **19 routes** (freeze held).
- **Dev-lens** (`superpowers:code-reviewer`): nothing Critical/Important. Notably corrected the brief's own stale premise — the 5-role enum was NOT deferred to Sprint 3, it shipped 2026-07-09, so `set_staff_role`'s free-text `p_new_role` is bounded by a live CHECK, not an open privesc gap.
- **User-lens** (general-purpose, cold-start operator journey): found the one real bug — see below.
- **Security** (`everything-claude-code:security-reviewer`, scoped to Tasks 9–10 live grants): **verdict "ship as-is," no Critical/High.** Two latent Low findings, both deferred.
- **Backtest** (controller, live Seoul via Supabase MCP): confirmed the exact grant matrix, the live `staff_role_check` CHECK, `staff` RLS = SELECT-only (anon column grant inert), migration list two-sided 69/69, and `get_advisors` (pseudonymise_user no longer under the 0028 anon finding). **Every "NEEDS LIVE PROBE" request from all three agents was answered by this backtest independently, before the reviews returned — full convergence, zero contradictions.**

**The one real bug (found by user-lens):** the prior session's `0cddd2e` registration-timing fix was **incomplete**. It correctly moved the event start off 45min, but its 90-min replacement left only a ~30-min registration-open window (`start − CHECKIN_OPEN_MINUTES`=60), which the run sheet's own 30-min prep block consumes — so registration could still be closed by Beat 3 live, reproducing the original symptom one level up. Fixed to **180 min** (~120-min window); the seed banner now prints the registration-close deadline directly. Check-in (Beat 4) is gated on the static `self_serve` flag, not the lifecycle clock, so widening the offset has zero downside — verified by re-running `reset-demo.ts` end-to-end.

Also fixed (run sheet, all cold-operator traps the user-lens surfaced): reset regenerates codes → must re-render (was unstated); `render-emails.ts` was never named in prep; Beat 4 described an attendee-typed-code UI that doesn't exist (that's the staff surface); stale "to build" labels on shipped scripts.

**4 new `DEFERRED.md` rows** (all non-blocking, added 2026-07-18): set_staff_role residual hardening (in-fn allowlist + last-operator guard, Sprint 3b), the `anon` staff column-grant footgun (dormant — no write RLS policy), the Hard Rule 11 positive-round-trip test gap, and the `WK-` code-prefix optics. Item 19 (three residual PUBLIC/anon grants) marked **✅ CLOSED** — `pseudonymise_user`'s share was exactly what Task 10 fixed, confirmed live.

## Task 14 — close-out (written AFTER the protocol passed, per CLAUDE.md ordering)

- **PROJECT_STATE.md**: new "Milestone A — ✅ SHIPPED + PHASE-COMPLETION PROTOCOL PASSED (2026-07-18)" section (commit table, gates, full three-lens+backtest writeup, carried-forward items), mirroring the Sprint 1/2/3a template; top summary line updated.
- **Vault `CPD Roadmap — Backend First.md`**: status line + a `[!warning]` callout flagging that the note's Sprint 0–5 body is stale against the Milestone A–E spine that `docs/plans/roadmap-to-mvp.md` (2026-07-12) made canonical and that Ivan's own Q31 (2026-07-16) already treats as settled. **Flagged, not resolved** — whether to fully rewrite the vault body, retire it, or keep both is Ivan's scope call, not made here.

## What's NOT done — read before claiming everything shipped

1. **Task 12 (graph refresh) — attempted this session, BLOCKED on the weekly account limit (resets Jul 22 6am HKT).** `/graphify --update` detected **95 changed files** (66 code + 29 docs) since the Jul-8 graph. AST extraction completed (225 nodes from the 66 code files); semantic-cache check hit 6/29 docs, leaving 23 docs needing LLM extraction. The two extraction subagents both failed — chunk 1 hit the weekly limit mid-run, chunk 2 never dispatched (classifier unavailable). **Aborted CLEANLY per the [partial-update-manifest trap](../../../../.claude/projects/-Users-ivan-Eventar/memory/graphify-partial-update-manifest-trap.md): no `build_merge`, no `save_manifest`, scratch files removed.** `graph.json`/`GRAPH_REPORT.md`/`manifest.json` are UNTOUCHED at the Jul-8 baseline (1306 nodes) — confirmed by file mtime + node count. A future `--update` will still see all 95 files as changed. **Retry after the limit resets** — the AST cache is warm (code re-extraction is fast), 6 docs are semantic-cached, so only ~23 docs need real LLM work.
2. **`replay-verify.yml`'s first real CI run** — the push triggered it (`on: [push]`); this is the first time Task 7's replay/tamper harness runs in a real runner (it can't run in-sandbox — `docker exec` hangs). Result **unconfirmed** this session (no `gh` auth available). Container name `supabase_db_Eventar` was verified to match `config.toml`'s `project_id` before the push, so it should execute; check the Actions tab.
3. **Vault Sprint→Milestone-A–E reconciliation** — flagged in the roadmap note, Ivan's call.

## Operational notes for the resumer

- **`origin/main` at `31cfeb4`, clean push, 0 behind.** The 37-commit backlog is fully published.
- **Uncommitted working-tree changes left untouched** (not this session's work, from other/parallel sessions): `M CLAUDE.md` (design-system-libraries skill row), `M .claude/launch.json` (demo-local config), and the `graphify-out/cache/*` + `graphify-out/cache/stat-index.json` churn (extraction cache — harmless, gitignored-adjacent). Decide their fate separately; they were pre-existing at this session's start.
- **Demo timing (post-fix):** `seed-demo.ts` start offset is now **180 min**; registration window = offset − 60 = ~120 min. Run `seed-demo`/`reset-demo` as the LAST prep action (the seed banner prints the exact `Reg. closes` deadline). Local stack was healthy this session (all `supabase_*_Eventar` containers up).
- **Live Seoul migration list:** two-sided through `20260716233440_grant_hygiene_residual_public` (**69 migrations**) — verified via `list_migrations` during the backtest.
- **Milestone A security posture (backtested live, for the record):** `set_staff_role` gated `eventar_staff`-only, value bounded by `staff_role_check`, audit-last; `pseudonymise_user` authenticated-only; `staff` RLS SELECT-only (`staff_self_read`); no new `get_advisors` exposure beyond the standard shared definer WARN.

## Ivan's inputs outstanding (carried from `handoff_17072026.md`, still open)

1. `[PRICE]` in `docs/collateral/one-pager.md` (+ email/phone in the outreach draft).
2. First internal-meeting date (Milestone B / E1).
3. D0 deploy flip (gates Milestone B's deploy track).
4. HKCP vs HKAM in `ledger-demo.ts` (uses HKAM since HKCP isn't seeded — easy swap if HKCP gets seeded).
5. Next milestone direction per `docs/plans/roadmap-to-mvp.md`: **B (field-proven — real events on a hosted URL)** vs continuing toward **C (the body review)** — both gated on the inputs above, not on engineering.
