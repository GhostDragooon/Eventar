# Eventar — Handoff Summary

**Date:** 2026-05-21
**Project root:** `/Users/ivan/Eventar` · **Vault:** `/Users/ivan/Desktop/Eventar`
**Status:** Phase 2 (registration flow) implementation complete and live-DB-backtested; awaiting user's manual browser smoke. **6 commits ahead of `origin/main`.**

---

## Where we are

### Eventar repo state
- Branch: `main`, working tree clean
- **6 commits ahead of `origin/main`** — Phase 2 work not yet pushed
- All gates green: `tsc --noEmit` clean · `eslint .` exit 0 · `vitest run` 59/59 · `next build` 8/8 pages

### Last 6 commits (unpushed)
```
54f34f1  docs(phase-2): live-DB backtest + V3 deviation + handoff filename normalisation
18d109d  docs(phase-2): finalize implementation notes — V2 deviation, log table, manual-smoke checklist
2868fe4  feat(phase-2): inline registration form on public event page (TDD)
a9c61d5  feat(phase-2): registrations + email_log tables — anon-insert RLS, email-log-first ordering
5c69676  fix(review-v2): VenueSearchBox — abort in-flight search on blur; correct viewbox comment
56ad2c7  docs(review): full-stack review report + MVP gap analysis    ← last pushed: 5c69676
```

### Phase progress

| # | Phase | Status |
|---|---|---|
| 1   | Foundation (login, dashboard, draft, public render) | ✅ shipped |
| 1.5 | Events reshape + `agenda_blocks` + atomic-insert RPC | ✅ shipped |
| 1.5b | Form UX overhaul (accordion, Nominatim, range pill) | ✅ shipped |
| —   | Full-stack review (4 critical + 3 important fixes) | ✅ shipped |
| **2** | **Registration flow (inline form, email_log ledger)** | ✅ **shipped; awaiting manual smoke** |
| 3   | Publish + static QRs | ⏸️ next |
| 4   | Check-in (tablet, Realtime, registration_code) | ⏸️ |
| 5   | Survey flow | ⏸️ |
| 6   | Analytics (per-event + manager dashboards) | ⏸️ |
| —   | **End of core MVP — entire flow runs locally with no external service** | |
| 7   | Add Resend (replace email stubs with real sends) | ⏸️ |
| 8   | Deploy to Vercel | ⏸️ |
| 9   | Add pg_cron (scheduled reminder + survey emails) | ⏸️ |
| 10  | MS SSO | ⏸️ future |

---

## What landed in this session

**Phase 1.5b (form UX overhaul) — finalised:**
- New-event form is now an accordion of 4 sections (Basics / Venue / Date+Time / Agenda) with summary chips when collapsed and a sticky bottom save bar
- Venue search switched from Mapbox to OSM Nominatim (HKCEC, Taipei 101, AsiaWorld-Expo all return correct results — Mapbox's POI dataset is genuinely thin for HK/Asian landmarks; verified by direct curl tests)
- Time picker is now a single-row range pill (1-hour chips 08:00–20:00, two-click range with click-to-clear)
- shadcn/ui primitives installed (`components/ui/*`) — Radix-based; categorically fixes the white-on-white `<select>` bug from Phase 1.5
- Renamed Program → Agenda across UI

**Full-stack review (mid-session):**
- 4 critical bugs fixed: `publishEvent` silent-failure on RLS denial, root `/` was Next scaffold, layout metadata still said "Create Next App", proxy `signOut()` dropped cookies on redirect (infinite-loop bug class — same as the earlier auth/callback fix)
- 2 second-pass UX bugs fixed: VenueSearchBox dropdown reopened after blur (race), viewbox comment was wrong
- Review report committed at `docs/plans/2026-05-20-full-stack-review.md` (every finding catalogued, including the 7 nice-to-haves we deferred)

**Phase 2 (registration flow) — built and live-DB-backtested:**
- 2 migrations applied to `muieupgkpbxpqsrjjwol`: `registrations` (anon-INSERT-when-published RLS, organizer/manager SELECT, unique(event_id,email), lowercase trigger) + `email_log` (service-role-only ledger, partial index on `status='queued'`)
- Server Action `registerForEvent` at `app/(public)/events/[id]/actions.ts` — implements email-log-FIRST ordering per CLAUDE.md hard rule 5
- Client component `components/RegisterCard.tsx` — 5-state machine (idle / submitting / success / error / at-capacity)
- Wired into the public event page below the agenda
- 7 new vitest cases covering the Zod input schema (UUID v4 strictness, email regex, trim+lowercase, name length bounds)
- **Live-DB backtest passed 15/15** (schema, flow happy path, lowercase trigger, duplicate rejection both same- and mixed-case, cascade delete, 5 anon-role defense-in-depth checks). Backtest section in `implementation-notes.html`.

**Two deviations from the original Phase 2 spec, both documented in `implementation-notes.html`:**
- **V2:** public page's registration count uses `supabaseAdmin()` (anon has no SELECT on registrations — PII; count would always be 0)
- **V3:** Server Action's registrations INSERT also uses `supabaseAdmin()` — anon's RLS grants INSERT but not SELECT, so Supabase JS's standard `.insert(...).select(...)` pattern fails because Postgres's RETURNING needs SELECT on the new row. The anon-INSERT RLS policy still provides defense-in-depth for direct `/rest/v1` hits.

---

## Key reference paths

### Plans + docs (in repo)
- **Running implementation notes**: `implementation-notes.html` — Phase 2 design decisions, deviations (V1/V2/V3), tradeoffs (T1/T2), open questions (Q1/Q2/Q3), live-DB backtest results, manual smoke checklist
- **Full-stack review report**: `docs/plans/2026-05-20-full-stack-review.md` — every finding from the audit + cross-check tables
- **MVP gap analysis**: `docs/plans/2026-05-20-mvp-gap.md` — status per phase vs roadmap, what remains for MVP (4.5 days)
- **Geocoder decision**: `docs/plans/2026-05-18-geocoder-decision.md` — Mapbox→Nominatim rationale + migration triggers to LocationIQ / Google Places
- **Phase 1.5b design + impl**: `docs/plans/2026-05-17-eventar-phase-1.5b-form-redesign.md` + `-impl.md`
- **Previous handoffs**: `docs/plans/handoff_16052026.md`, `docs/plans/handoff_20052026.md`

### Vault (Obsidian) — source of truth
- Index: `/Users/ivan/Desktop/Eventar/00 — Index.md`
- Decisions Log (Q1–Q10): `/Users/ivan/Desktop/Eventar/02 — Decisions Log.md` — **updated through Phase 1.5b**
- Data Model: `/Users/ivan/Desktop/Eventar/10 — Architecture/Data Model.md` — **updated for Phase 1.5 events reshape + agenda_blocks**; **NOT YET updated for Phase 2's registrations + email_log** (action item below)
- Phased Roadmap: `/Users/ivan/Desktop/Eventar/20 — Roadmap/Phased Roadmap.md`
- Phase 1 — Foundation: `/Users/ivan/Desktop/Eventar/20 — Roadmap/Phase 1 — Foundation.md` — **marked shipped + Phase 1.5/1.5b complete**
- Security + Robustness: `/Users/ivan/Desktop/Eventar/10 — Architecture/Security + Robustness.md`
- Auth Flow: `/Users/ivan/Desktop/Eventar/10 — Architecture/Auth Flow.md`

### CLAUDE.md (auto-loaded when working in repo)
- `/Users/ivan/Eventar/CLAUDE.md` — 12-rule Coding Behavior Contract + 10 Eventar hard rules
- `/Users/ivan/Eventar/AGENTS.md` — Next 16 breaking-changes warning

---

## Live infrastructure

| Service | What | Where |
|---|---|---|
| Supabase project | `muieupgkpbxpqsrjjwol` | https://supabase.com/dashboard/project/muieupgkpbxpqsrjjwol |
| GitHub | `GhostDragooon/Eventar` | https://github.com/GhostDragooon/Eventar |
| User's dev server | `pnpm dev` on :3000 (user's own terminal — keep running for HMR) | http://localhost:3000 |

### Remote DB state
- **7 migrations applied** (init_staff, init_events, init_agenda_blocks, reshape_events_venue, rpc_create_event_with_blocks, **init_registrations**, **init_email_log**)
- 1 staff row: `ahf.ivan@gmail.com` (role: manager)
- 1 published event remaining from earlier smoking: `REPRO-PUB` (id `cca2897f-3d2e-417e-9bff-ca54b0ed22b6`), max_attendees=2, 0 registrations
- 0 rows in `agenda_blocks`, `registrations`, `email_log`
- RLS enabled on all 5 tables; helpers `auth_email()`, `is_manager()`, `current_staff_id()`, `lowercase_email()` live
- All Phase 2 RLS policies + constraints + indexes + triggers verified live (see backtest section in `implementation-notes.html`)

### `.env.local` (gitignored, 5 vars)
```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_DB_PASSWORD
NEXT_PUBLIC_MAPBOX_TOKEN          ← unused since Phase 1.5b's Nominatim swap; safe to leave
```
**Never echo, cat, or paste any line of `.env.local`** in commit messages, plan docs, or subagent prompts.

---

## Outstanding decisions / risks

1. **6 commits unpushed to origin.** Larger than the handoff_16052026's "6 commits" risk note; should push soon. Suggested gate: user's manual smoke passes first, then push.
2. **Phase 2 manual smoke not yet run by user.** The 8-step checklist lives in `implementation-notes.html` (preview-rendered). Live-DB backtest covers the data layer; manual smoke covers the UI/click path that SQL can't.
3. **`REPRO-PUB` test event still in DB.** Capacity 2/0. The user previously stated they'd come back to smoke — this event is intentionally still there for that. Delete it (or use it) at smoke time.
4. **Vault Data Model note not yet updated for Phase 2 tables.** The `registrations` + `email_log` SQL DDL is in migrations 0006 + 0007 but the vault's `10 — Architecture/Data Model.md` doesn't yet have sections for them. ~5 min to add.
5. **Vault Decisions Log doesn't yet have Q11 (Phase 2 admin-vs-anon-RLS choice).** Worth recording the V3 deviation there as a locked decision.
6. **Phase 2 deferred items (in `implementation-notes.html`):**
   - **Q1** — visible capacity counter on public page ("12 of 50 spots remaining"). Defer until first user complaint.
   - **Q2** — affiliation field on registrations. Defer until first analytics need.
   - **Q3** — organizer view of registrations on the edit page. Land in Phase 6 (analytics).
7. **Phase 1.5b small leftovers (in `2026-05-20-full-stack-review.md`):**
   - I3 — `supabaseServer` swallows cookie-set errors (defensive; revisit if stale-auth bug surfaces)
   - I5 — per-block client-side validation in agenda editor (Zod catches at submit; cosmetic)
   - I6 — dashboard manager view doesn't show `created_by` (UX gap for multi-organizer view)
   - I8 — FillerEditor doesn't visually distinguish break vs transition
   - N2 — edit page doesn't render `event.topic`
   - N7 — `searchVenues()` could log fetch errors for telemetry

---

## How to resume

### Option A — Push to origin first, then continue (Recommended)
Push the 6 unpushed commits before more code. Safer rollback story; the live DB is in a clean state to match.

```bash
git push origin main
```

Then either:
- Wait for user's manual smoke result, OR
- Move to Phase 3 (publish + static QRs) — ~1 day work per the MVP gap doc

### Option B — Run user's manual smoke first
The 8-step browser checklist is in `implementation-notes.html`. Boot dev (if not already running on :3000), log in as `ahf.ivan@gmail.com`, walk through. Report any deviations.

### Option C — Continue building Phase 3 immediately
Per the MVP gap doc, Phase 3 brings:
- `qrcode` npm dep
- Server-side QR PNG generation (info-link QR + register-link QR)
- Download buttons on the edit page
- Possibly: full edit UI (Phase 1 explicitly deferred edit-form to Phase 3)

Smallest-scope-first: just the two QR PNGs + download buttons. Defer edit-form to its own slice.

### Option D — Update vault first (D2 from the previous handoff cycle)
Add Phase 2's registrations + email_log to `10 — Architecture/Data Model.md`. Add Q11 (admin-vs-anon-RLS) to `02 — Decisions Log.md`. ~10 minutes. Keeps the vault as source of truth per CLAUDE.md.

**Recommendation order if no specific user direction:** D (vault) → A (push) → B (smoke) → C (Phase 3).

---

## Hard rules for anyone resuming

1. **Read the vault first** — Index + Decisions Log + Data Model + Security + Robustness notes
2. **Read `implementation-notes.html`** — captures Phase 2 design choices that haven't migrated to the vault yet
3. **Single `main` branch** — no feature branches
4. **`requireStaff()` at the top of every staff Server Action** — no exceptions
5. **Insert `email_log` row FIRST, send email SECOND** — this hard rule now has a working implementation in `registerForEvent` to copy from when Phase 5 (survey emails) lands
6. **Three-layer validation** — form → Zod → DB constraint
7. **Verify Next 16 / shadcn / Mapbox / Nominatim APIs against installed `node_modules/**/*.d.ts`** before using any vendor-specific code — this pattern has now caught 5 separate issues this project (Next 16 proxy rename, Mapbox `context` array-vs-object, styled-jsx absence, Base UI vs Radix in shadcn base-nova, Zod 4 UUID v4-strictness)
8. **Never paste secrets into commits, plan docs, vault notes, or subagent prompts**
9. **`server-only` import on any module that imports `next/headers` or service-role keys** — `lib/supabase/admin.ts`, `lib/auth.ts` already have this
10. **When in doubt: ask the user before guessing**

---

## Next concrete action

**For Phase 3 (publish + static QRs)**, follow the same shape that worked for Phase 1.5b and Phase 2:

1. Brainstorm (one focused question if the spec has a fork) → design doc → executable plan → implement
2. TDD the pure pieces (URL builder, QR encoder wrapper)
3. Use the same shadcn primitives already in `components/ui/*`
4. Apply migrations via Supabase MCP as they're written
5. Maintain `implementation-notes.html`-style running doc per phase (lighter than full design+impl docs for small phases)

Phase 3 inputs:
- `qrcode` npm package — server-side PNG/SVG generation
- New action in `app/events/[id]/edit/actions.ts` — generate-and-return-base64 OR write-to-storage
- Two QR codes per published event: `/events/{id}` (info, for the audience) + `/events/{id}/register` (registration, for organisers to print/post)
- Download buttons on the edit page below the Publish button

Estimated 1 day per the roadmap. Likely fits in 2 focused commits: dep + helpers + first QR (one), edit-page wiring + second QR + download UX (two).
