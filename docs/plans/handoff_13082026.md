# Handoff — 2026-08-13 (Task 1 shipped: MCHK + 15 HKAM Colleges, authorisation gate closed)

_Continues `handoff_10082026_v2.md`. That session left Task 1 of the approved 7-task 2026 dual-track plan (`~/.claude/plans/target-product-for-2026-proud-castle.md`) blocked on one thing: the 15 HKAM College names are regulatory data going into a production seed, and writing them from model recall was explicitly refused. This session resolved the blocker, shipped Task 1, and ran the full three-lens phase-completion protocol on it._

> **Next session starts here:** this doc → `~/.claude/plans/target-product-for-2026-proud-castle.md` (Task 2 is next: primary licence per body) → the Task 3 genericity note appended there this session.
>
> **⚠️ `.env.local` still points at Seoul.** Point it at the local stack (`scripts/demo/dev-local.sh`, port 3100) before running any RLS/CPD suite or demo script — unchanged from last session's warning.

---

## 1. What shipped

**Blocker resolution, confirmed with Ivan (not model recall):**
1. The 15 HKAM College names — fetched directly from `https://www.hkam.org.hk/en/academy-colleges`; Ivan independently supplied the same URL as confirmation.
2. Authorisation scope — the default org gets authorised for the 6 pre-existing `status='active'` bodies (IA, HKICPA, MPFA, HKIE, VSB, PT_BOARD) + MCHK + all 15 Colleges. LSHK stays excluded (`status='onboarding'`, ungrounded data).

**New migration:** `supabase/migrations/20260813000000_seed_hkam_colleges_and_mchk.sql`
- MCHK as a top-level body (`status='active'`).
- 15 HKAM Colleges as `parent_body_id`-linked children, `status='active'`, placeholder `cycle_config`/`category_taxonomy` (real per-body packs are Task 4).
- HKAM parent flipped `'deferred'` → `'active'` (stays deliberately **unauthorised** — it's the parent Academy, not itself an accrediting body; only its Colleges + MCHK accredit events).
- 22 `organisation_body_authorisations` rows for the default org — the table was **0 rows on every environment** before this, which made `set_event_cpd_config()` refuse every event.
- Self-verifying `do $$ ... $$` block asserting the counts, HKAM's status, and LSHK's continued exclusion.

**Two demo-fixture fixes** (found live-testing the migration, not part of the original Task 1 scope but directly caused by it): `scripts/demo/seed-demo.ts` and `scripts/demo/ledger-demo.ts` both hardcoded the accrediting body to `'HKAM'` with a comment explaining why — HKCP didn't exist as a seeded row yet. It does now. Both switched to `'HKCP'`, matching the original pivot narrative ("HK launch, HKCP first accrediting body") and each file's own comment, which literally said to make this exact swap once HKCP was seeded.

**Two doc corrections:**
- `~/.claude/plans/target-product-for-2026-proud-castle.md` — Task 3's section now carries Ivan's mid-session genericity directive: no CHECK constraint permanently limited to medical category codes, `track` stays a medical convenience denormalisation not an identity key, units/ceilings/export shape stay inside each body's pack. This has to survive to whichever session executes Task 3.
- Vault `30 — Reference/CPD Source Documents — Body Manuals & Forms.md` — corrected a stale claim that HKCR was already seeded (it wasn't; confirmed against both the live DB and the seed migration file — it's one of the 15 Colleges this session added).

## 2. Verification

**Gates:** tsc clean · eslint 0 errors (5 known `devEmailStub` warnings) · vitest **583 passed | 174 skipped** (baseline match, zero regressions — expected, this is pure data + 2 constant swaps) · `next build` clean, **22 routes** (unchanged) · `bash scripts/ci/replay-and-verify.sh` → `REPLAY + TAMPER GATE: PASS` · `verify_ledger_chain()` invalid rows = 0.

**Live backtest on the local stack** (not Seoul — this migration hasn't shipped there yet, see §4):
- Positive: `set_event_cpd_config` against a test event + HKCP succeeds (rolled back).
- Negative (status): the same call against LSHK fails — `not an active body`.
- Negative (authorisation, the actual gate Task 1 closes): the same call against **HKAM itself** — `status='active'` but deliberately unauthorised — fails with `not_authorised_for_body`. This is the specific case that proves the authorisation check is live, not the status check firing for an unrelated reason.
- Reseeded the local demo event via `seed-demo.ts` and loaded it in the browser (`localhost:3100`, `scripts/demo/dev-local.sh`): the CPD accreditation picker, previously **empty for every organiser on every event** (0 authorisation rows anywhere), now correctly lists all 22 authorised bodies, cleanly labelled, no HKAM in the list (as intended), and the seeded event shows "HKCP · 3 · confirmed."

## 3. Phase-completion protocol — run in full

Two separate agents, per the standing rule (never the implementer's own review):

**Dev-lens** (`superpowers:code-reviewer`) — reviewed the migration against its two sibling migrations' conventions, the two demo-script edits, and both doc edits. Confirmed clean: schema compliance, no injection/quoting risk, zero grant/RLS surface (pure DML), placeholder packs can't crash anything before Task 4 (grepped — nothing reads `cycle_config`/`category_taxonomy` off `accrediting_bodies` yet outside test fixtures), fixture edits complete (grepped every remaining `'HKAM'` reference repo-wide — only the two files fixed had it). Two real findings, both fixed before this handoff was written:
1. The migration's printed rollback comment only covered the 16 new bodies, not the 6 pre-existing bodies whose *authorisation* rows (not the bodies themselves) were also first created by this migration — running it verbatim would have stranded 6 rows. Corrected.
2. The self-verifying count check didn't filter `status='active'`, so it would have kept "passing" against a stale suspended/revoked authorisation. Added the filter, re-ran `replay-and-verify.sh` — still green.

**User-lens** (general-purpose, cold-start organiser journey against the live local server) — confirmed the core fix works exactly as intended: 22 clean picker options, HKAM correctly absent, both entry points (`/edit` and `/details`) share one picker with no drift, well-hardened empty/frozen/retired states. Three findings, **all pre-existing and out of this task's diff** — logged to `docs/DEFERRED.md`, not fixed here (surgical-changes discipline): the readiness-strip's "points confirmed" copy vs. "CPD hours" everywhere else, a disabled "Accreditation" sidebar stub sitting next to the working inline section, and `PT_BOARD`'s lone underscore-slug naming style among 21 clean short codes.

**Backtest:** covered above (§2) — live local-stack queries and a real browser render, not just the RPC calls.

## 4. What's NOT done

- **Commit.** Nothing in this session has been committed — held per standing instruction (never commit without being asked). Working tree has: the new migration, the two demo-script edits, `docs/DEFERRED.md`, `docs/plans/handoff_13082026.md` (this file), and `~/.claude/plans/target-product-for-2026-proud-castle.md` + the vault note (both outside the repo's git tree). Ivan's call whether/how to commit.
- **Task 2 onward** (primary licence per body, FCAA category, rule packs, proration, certificates, export) — untouched, per plan.

**Seoul push — DONE.** Ivan ran `supabase link --project-ref muieupgkpbxpqsrjjwol && supabase db push --linked` himself after a local Mac-keychain snag (login keychain password had drifted out of sync with the account password — standard macOS issue, unrelated to this repo). Confirmed live via a direct read against the production project (not just "migration appears in the list"): 93 migrations, 15 Colleges, HKAM/MCHK both `active`, 22 authorisation rows, LSHK correctly excluded.

## 5. Gotchas hit this session

- **`docker` isn't on `PATH` in this shell** — the full binary is at `/Applications/Docker.app/Contents/Resources/bin/docker`; Docker Desktop itself was already running (since Wednesday) despite `docker info` failing with a plain "command not found" that looked like the daemon was down. Prepend that path, don't assume the daemon needs restarting.
- **`seed.sql` seeds exactly one staff row**, `email='manager_email'` — a literal placeholder value, not a real-looking email. Fine for RPC probes (`set request.jwt.claims`), surprising if you expect a realistic fixture.
