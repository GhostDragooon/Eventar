# Handoff — 2026-08-10 (cross-source review → five frontend decisions → everything committed)

_Scope: Ivan asked for a review of the handoffs and progress cross-checked against the Obsidian vault and graphify, flagging discrepancies and hallucinations. That produced five frontend decisions taken against rendered specimens, a CI fix, and the first full commit of four sessions' work._

> **Next session starts here:** vault `30 — Reference/Frontend Design Standard.md` (new, the frontend authority) → `docs/plans/STAGES.md` → this doc §5 → `docs/DEFERRED.md`.
>
> **The two live blockers are in `PROJECT_STATE.md`'s banner, not here.** Both are about Seoul.

---

## 1. What the review found

Four documents marked "locked" gave four different answers about the same interface, and three record systems disagreed about the same project.

**Claims that were false, not merely stale** — each verified by execution:

| Claim | Where | Reality |
|---|---|---|
| "Seoul is 2 migrations behind (90 vs 92) — deliberate" | `handoff_08082026.md:86` | **91 vs 92**, and the stated reason is inverted — the other session's migrations were already on Seoul |
| "no in-flight accredited event was stranded" | DEFERRED 57 | **0 rows** in `organisation_body_authorisations`; 18 accredited events on Seoul |
| "nothing re-runs it … both need a CI job" | DEFERRED 23 | `.github/workflows/replay-verify.yml` has existed since 2026-07-15 |
| "⚠️ REGRESSED — migration drift is back" | `PROJECT_STATE.md` c-f #4 | Both named files exist; zero drift. Fixed. |
| "supabaseServer() now unconditionally delegates" | `app/login/actions.test.ts:102` | False since review mode was reinstated **the same day** |
| "Self-serve is on…" | `seed-demo.ts:586` | Line ~382 sets it `false`. Fixed. |
| "the **July** function-clobber" | `handoff_08082026.md:25` | Migration header: 2026-08-03, Stage 7 branches |
| "deploy = M4 … Singapore at Sprint 1 start" | `CLAUDE.md:130` | Stage 8; never provisioned. Fixed. |
| "`#0E79EC` ~3.9:1" | vault Q31 (a LOCKED decision) | **4.23:1** — copied downstream by two docs |

**Two of my own, for the record.** I read `.glass-nav` as being in only two shells (`head` truncated the grep — it is in all four), and I nearly reported four `<img>` as missing alt text (a line-based grep missed multi-line JSX). Both caught before they reached Ivan; both were wrong in the first draft.

**Two the explorer agent got wrong, corrected by checking:** `#7FB0F4` and `#4CC47D` are legitimate dark-mode tokens in `globals.css`, not rogue hexes — only `#0D74E2` is outside the ramp. And sweet-leavitt's "RLS gate is 108 not 113" **self-settled in its own text**; it was never open.

## 2. Ivan's decisions (now vault Q32)

All five taken against rendered specimens — *"i cannot decide visual setting without seeing"*.

| Decision | Answer |
|---|---|
| Primary CTA | **`#0070F3`**, reversing Q31's retirement — but fill-role only |
| Which token carries it | **Route (ii)** — `--primary` = `#0070F3`, new `--primary-ink` = `#1C3C94`, 27 sites swept |
| Colour doctrine | **Strict §7a** — green stays live/verified only |
| `Live` pill | **Soft** (container + ink), 3.28:1 → 7.21:1 |
| Wordmark | **All four shells**, one shared `BrandMark` |
| Events nav | **Option C** — row deleted; `/dashboard` is the organiser list |

The audience-boundary rule is Ivan's, and it is the one that explains a class of past bugs: the old Events link pointed at `app/(public)/events`, the **attendee** directory, so clicking it from staff loaded the public page and the sidebar vanished. `SOON` hid the symptom.

## 3. CI: replay-from-zero is green

DEFERRED 64 said the CLI "zeroes" the grants. It does not — default privileges differ **by creating role**: `supabase_admin` grants `arwdDxtm`, **`postgres` grants only `Dxtm`** (no DML), and migrations run as `postgres`.

Fixed in `20260514164414_init_staff.sql`, the first migration — not the CI harness (`supabase start` applies migrations itself, too late) and not `seed.sql` (`db reset` runs it *after* migrations, the original trap). Proven: `bash scripts/ci/replay-and-verify.sh` exits **0**, `REPLAY + TAMPER GATE: PASS`. Hard Rule 11 re-verified intact afterwards — `service_role` INSERT is `true` on `events`/`email_log`, `false` on `credit_ledger`/`audit_events`.

**This unblocks the Singapore provisioning precondition.**

## 4. Commits

Seven, all on `main`, first commit of four sessions' work:

```
9409945  feat(review-mode)  the local auth bypass, with the guard it lacked
e8af7be  feat(frontend)     one design language, five decisions against renders
bec303d  fix(security)      two claims the review found false
bcadd19  feat(orchestrator) retryable, bounded, and honest
7a0f2da  feat(security)     anon write paths closed at the grant layer
d60b596  fix(ci)            replay-from-zero green
<this>   docs               vault standard + Q32 + CLAUDE.md + state
```

Review mode is deliberately its own commit so it can be reverted alone — **the keep-or-strip call is still Ivan's** and `dev-local.sh` still defaults it ON.

**Gates:** tsc clean · eslint 0 errors (5 known) · vitest **583 | 174** · build clean, 22 routes.

## 5. Still open

1. **Seoul is one migration behind and it breaks reminders + surveys.** See the `PROJECT_STATE.md` banner. Highest priority.
2. **`organisation_body_authorisations` is empty on Seoul** — 18 accredited events unconfigurable; cause unresolved.
3. **Review mode default** — Ivan's call.
4. **Hex literals in 10+ components** — the landing CTA is still `#0D74E2` while `--primary` is `#0070F3`. Two brand blues, ~300px apart, same screen.
5. **`published_at` is unreliable** before `20260802182411` — recovered from an orphaned worktree handoff, now DEFERRED.
6. **Five `claude/*` branches are fully merged** and safe to prune, along with their worktrees. Not done — destructive, not asked for.
7. **Vault is still stale outside the new note** — `00 — Index.md` and `CPD Roadmap — Backend First.md` still say Sprints 0–3a and "frontend frozen".

## 6. Process notes

- **The browser pane lied and the JS did not.** It painted a blank landing hero, then hung. Computed styles showed every element `opacity: 1` with real heights. `handoff_09082026.md` warns the pane mis-composites; extend that note — here the *screenshot* was the unreliable one. Headless Chrome via CLI rendered it correctly.
- **A safety comment is a cache, and nothing invalidates it.** `actions.test.ts` vouched for a function that changed underneath it hours later, and told future sessions not to add the guard that catches the regression. The recovered test was **mutation-tested** before landing: with the bug reintroduced it fails alone, 1 failed | 2 passed.
- **Two live-infrastructure claims were validated by reading migration files rather than querying the target.** Rule 14 catches this at the code layer; nothing yet applies it at the deployment layer. A migration file is a request; `has_table_privilege` is the answer.
