# Handoff — 2026-07-26 — CPD MVP pushed, course-finder track opened + parked, DEFERRED.md backlog closed

> **Resume line for the next session/agent:**
> *"Read `docs/plans/handoff_26072026.md`. Three separate threads happened this session: (1) the CPD MVP attendance-issuance build from `handoff_25072026_v2.md` was backtested end-to-end through the real app and pushed — `origin/main` is now current at `da7ef87`. (2) A new parallel research track, 'course finder / discovery layer,' was opened at Ivan's direction, then explicitly paused — 'no need to build now, focus on the app building 1st.' Read `20 — Roadmap/Course Finder — Discovery Layer (2026-07-26).md` (vault) before touching it again; it is NOT integrated into the app/infra and should stay that way until Ivan says otherwise. (3) Ivan redirected to 'app building' and named the unblocked path himself: work off `docs/DEFERRED.md`. Four items closed, TDD-verified live against Seoul, two-agent phase-completion review run and its own findings fixed. Everything in (3) is verified and reviewed but NOT YET COMMITTED — sitting in the working tree. Ivan's last message was 'will have another agent to review, handoff' — treat this doc as written for that reviewer, not as permission to push or commit further without asking."*

---

## Thread 1 — CPD MVP backtest + push (continuation of `handoff_25072026_v2.md`)

That handoff left 6 commits (`fe3756d`…`da7ef87`) sitting locally, unpushed, with Ivan's explicit "not approved a push yet." This session:

1. Ran a full pre-push backtest at Ivan's request ("full stack review, back test everything... full traceability"): fresh static gates, a clean local `supabase db reset` (76 migrations), the full loop (publish → self-check-in → credit issuance) driven through the **real app** in the local demo worktree (not just RPC calls), both `audit_events`/`credit_ledger` hash chains verified valid after each step, `RLS_TESTS=1 vitest run tests/cpd/` 12/12 green live against Seoul (re-proving both HIGH-1/HIGH-2 authorisation-hole fixes still hold), direct grant introspection on Seoul confirming the fix (and its own self-correction) are actually live, migration list two-sided 76/76, `get_advisors` clean of new findings.
2. Fixed the demo worktree (`/Users/ivan/Eventar-demo`) — it was still on a stale pre-Stage-3 commit with manually-copied stray files identical to what's already on `main`; fast-forwarded it via `git checkout --detach main` after verifying the stray content was redundant, not unique work.
3. Found the `NewEventForm.test.tsx` flake's **3rd occurrence** during this backtest (crossing the threshold `handoff_25072026.md` set) — confirmed clean in isolation, flagged, did not fix (out of scope for a backtest pass).
4. **Pushed.** `origin/main` is now at `da7ef87` — confirmed via `git status --short --branch` showing `## main...origin/main` (no divergence).

Nothing else to do here. This thread is closed.

---

## Thread 2 — Course finder / discovery layer (opened, then explicitly paused)

Ivan supplied 5 real source documents (PT + OT AHP Council manuals, a filled CPD Record spreadsheet, later an HKIST speech-therapist source) and 3 live URLs (HKIE CPD Calendar, HKMF/HKU, AHP Council OT), asked for a review of "how we can populate a course finder," then greenlit scope ("public directory first, seeded from public sources, self-listing later"), then had me actually solve the HKIE pagination scraping mechanism and pull real batches. Then: **"no need to build now. focus on the app building 1st please."** — explicitly paused mid-stream after a second addition (HKIST + a proposed RAG retrieval-layer design) was logged but not built.

**Everything from this thread lives outside the app:**
- Vault: `20 — Roadmap/Course Finder — Discovery Layer (2026-07-26).md` — full source landscape (HKIE live-scrapeable calendar, AHP Council's live HTML tables — corrected from an earlier "PDF dump" assumption, HKMF's one-event-many-bodies matrix, HKIST's self-reported points-precedent register), a proposed lifecycle/trust-tier flow map, registry cross-check findings, 5 open questions (2 resolved: trust-tier reading confirmed correct, registry stays disconnected for now; 2 explicitly deferred: reproducibility/ToS posture, medical sourcing plan), and the later retrieval-layer addition (canonical dedup store + vector index + hybrid query, `source_type` field — flagged the proposal's Microsoft-stack shape doesn't match Eventar's Supabase/pgvector-native stack, not resolved).
- Repo: `docs/research/course-finder/` — `README.md` + two real sourced CSVs (`hkie-sample.csv`, 47 real HKIE rows across all 5 pages with category tags pulled from DOM classes; `ahp-council-ot-2023-24-sample.csv`, 14 real AHP Council OT rows).
- Memory: `eventar-course-finder-discovery-layer.md` (project-type, both the initial scope and the later addition).
- `PROJECT_STATE.md`'s carried-forward section: one pointer line, same pattern as the frontend-design-exploration thread.

**Real, load-bearing finding from this thread, worth surfacing to whoever reviews:** cross-checking the source manuals against the *actual* `accrediting_bodies` seed migration found `PT_BOARD` already seeded and matching the PT manual exactly (good), but **`OT_BOARD` is not seeded at all**, and **`HKAM` exists only as an inert parent shell — none of its 15 Colleges (the real medical accrediting bodies, the ones that showed up 15-deep on the HKMF accreditation matrix) are seeded as rows.** Not fixed — flagged, per "keep apart from infrastructure." Worth a real registry-expansion pass whenever Ivan wants it, using the same citation-grounding discipline Sprint 3a Task 7 already established.

**Status: parked, not abandoned.** Do not resume building this without an explicit go from Ivan — the pause was direct and unambiguous.

---

## Thread 3 — DEFERRED.md backlog (Ivan's own direction: "work off DEFERRED.md backlog")

Asked which task to prioritize since Milestones B/C are gated on Ivan's own outstanding inputs (price, NGO intros, D0 deploy decision) that hadn't landed — Ivan picked the DEFERRED.md backlog explicitly over waiting or naming something else.

### Four items closed, all TDD'd or live-reverified against Seoul (not mocked)

| Item | What actually happened |
|---|---|
| `declare_licence` raw FK-dump leak | **Real red→green TDD.** Wrote the test first, watched it fail at `23503`, added an `exists(...)` not-found check (`P0002`) mirroring the sibling functions' mechanism (not their exact shape — see below), confirmed green. Migration `20260726070748_declare_licence_clean_not_found.sql` applied live to Seoul via MCP, filename reconciled against `list_migrations`. |
| `checkin_throughput.rls.test.ts` racy staff lookup | Root-cause fix, not a band-aid: the file previously read "a" pre-existing staff row unfiltered, which could race another RLS file's concurrent staff insert/delete. Now owns a disposable, uniquely-timestamped fixture (`beforeAll`/`afterAll`), matching every other RLS suite's established pattern. Re-verified: 1/1, 200 concurrent check-ins, P99 < 2s, chain valid. |
| HR11 positive round-trip test (staff column grants) | **Found already shipped**, not re-implemented — commit `d110c0e` added this test alongside item 45's anon-grant fix, but DEFERRED.md was never updated to close it. Re-verified live (5/5 green) before closing the doc, so the closure is evidence-backed, not just "it exists so I assume it works." |
| `NewEventForm.test.tsx` flake | **Diagnosed, deliberately NOT fixed.** Hit 2 more occurrences this session (4th and 5th total, well past the 3rd-occurrence promotion threshold). Root cause identified with reasonable confidence: `setSavedAt(null)` is a synchronous setState outside `useTransition`, but the confirmation banner and the button's `disabled`/label both key off the transition's `pending` flag, set in the post-`await` continuation — under concurrent-test-file CPU pressure, React's scheduler can interleave these differently than in isolation. Two hypotheses written down, neither proven. **Did not attempt a blind fix** — a non-deterministic bug can't be TDD'd without reliable reproduction, and guessing at a fix I couldn't verify would have been worse than leaving it flagged. |

### Two-agent phase-completion review — run, and its own findings acted on

Per repo CLAUDE.md's phase-completion protocol: a dev-lens agent (`superpowers:code-reviewer`) and a separate consumer-lens agent (general-purpose, briefed on the API-consumer/documentation-reader angle since there's no frontend surface for this diff) reviewed independently, each re-verifying live against Seoul rather than trusting my report.

- **Dev-lens: clean.** No Critical/Important findings. Independently reproduced everything (grants, migration state, test counts, the `d110c0e` closure claim) rather than trusting the diff.
- **Consumer-lens: found real things**, all fixed before this handoff was written:
  1. A wrong citation in my own flake write-up (blamed `handoff_25072026_v2.md`, which says nothing about this bug — the actual "3rd occurrence" threshold is in `handoff_25072026.md`, no `_v2`). Corrected, and the occurrence count clarified as directly observed this session.
  2. I'd dropped a "not a security leak" reassurance line from the original DEFERRED entry, and overclaimed that the fix "mirrors the siblings" when really there are **two different not-found philosophies** in the licence-mutation functions (staff-actor functions split not-found vs. wrong-org into two error codes — a narrow cross-tenant existence oracle; self-actor functions deliberately collapse both into one). Restored the reassurance, corrected the claim, and logged the inconsistency as its own new DEFERRED.md row since it was previously untracked.
  3. A genuinely new (pre-existing, not a regression) finding: `declare_licence`'s new not-found check has no `status='active'` filter, so a caller with `HKAM` or `LSHK`'s real UUID (both non-active today) sails past the "not found" guard. Logged as its own row.

**Static gates at handoff:** tsc clean, eslint clean (5 pre-existing unrelated warnings), vitest 469/470 (only the diagnosed-not-fixed flake, confirmed unrelated in isolation), `next build` clean, 19 routes unchanged.

---

## State at handoff

- **`origin/main` is current** — Thread 1's 6 commits are pushed, nothing pending there.
- **Thread 3's work is verified, reviewed, and NOT YET COMMITTED.** Sitting in the working tree:
  - Modified: `docs/DEFERRED.md`, `tests/audit/licence_mutations.test.ts`, `tests/rls/checkin_throughput.rls.test.ts`
  - New: `supabase/migrations/20260726070748_declare_licence_clean_not_found.sql` (already applied live to Seoul — the migration file is a record of a live change, not a pending one)
  - Also uncommitted, pre-existing, unrelated: `CLAUDE.md` (1-line, flagged in every recent handoff, left alone deliberately), `graphify-out/` (dirty, pre-existing)
  - New, uncommitted: `docs/research/course-finder/` (Thread 2's flat-file research artifacts)
- **Ivan has not asked for a commit.** His last message was "will have another agent to review, handoff" — read as: hold here, let review happen, don't push or commit further without being asked.

## Operational notes (carried, unchanged from prior handoffs)

- Local stack: reset clean this session, all 76 (well — 77 now, counting the new migration) migrations replay green.
- Node: shell defaults to v14; prefix with `PATH="/Users/ivan/.nvm/versions/node/v24.6.0/bin:$PATH"`.
- Docker binary: `/Applications/Docker.app/Contents/Resources/bin/docker` (not on PATH).
- Migration filename drift is still real — `20260726070748_declare_licence_clean_not_found.sql` needed the same post-apply rename this session.
- `/Users/ivan/Eventar-demo` worktree is now genuinely current (detached at `da7ef87` as of Thread 1) — no more manual file-copying needed there.
- Local demo server (`demo-local` launch config, :3100) was used for live UI verification this session; not currently running.

## Ivan's inputs outstanding (all carried, none new)

1. `[PRICE]` in `docs/collateral/one-pager.md` + outreach email/phone.
2. First internal-meeting / body-review date.
3. D0 deploy flip.
4. Whether to reconcile vault `CPD Roadmap — Backend First.md`'s Sprint body into the Milestone A–E spine.
5. The two CPD MVP deferred product questions (organiser↔body authorisation model, real check-in-window semantics) — need the accrediting-body conversation.
6. **New this session:** the 5 course-finder open questions in Thread 2's vault note (2 resolved, 3 still open) — not urgent, the whole track is paused.
7. **New this session:** whether/when to commit Thread 3's work.

## The lesson worth carrying

Two separate "found it already done, don't redo it" moments this session (the HR11 test, most of the CPD MVP backtest ground already covered by the prior session's own three-lens review) and two separate "the review caught something I got wrong" moments (the citation error, the overclaimed sibling-mirroring). Neither invalidates the discipline — they're what it's *for*. Verify before claiming closed; verify the reviewer's own claims too (the consumer-lens agent's four findings were spot-checked against source before acting on any of them, not applied blind).
