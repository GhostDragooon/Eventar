# Handoff — 2026-07-17 (Milestone A: demo-ready + set_staff_role live; graph refresh + phase-completion protocol still owed)

> **Resume line for the next session:**
> *"Read docs/plans/handoff_17072026.md, then finish Milestone A: retry Task 12 (`/graphify --update`, 21/28 docs still uncached — 7 already cached from this session) once the account session limit clears, then run Task 13's three-lens phase-completion protocol (dev-lens + user-lens + `everything-claude-code:security-reviewer` on Tasks 9–10 + backtest = rerun Task 6's rehearsal beats and Task 7's CI script), fix whatever it finds, THEN write the real Task 14 close-out (PROJECT_STATE block, vault status line). This handoff is NOT that close-out — the phase-completion protocol has not run yet, do not skip it."*

## What this session did (13 commits on `main`, unpushed — user pushes per cadence)

Picked up from `handoff_12072026.md` (Tasks 2–8, 12–14 still open) and executed Tasks 2–10 + 6 of the [Milestone A executable plan](2026-07-12-milestone-A-executable.md) end-to-end via `superpowers:subagent-driven-development`, with heavy live verification throughout — not just static review.

| Commit | What | Verified how |
|---|---|---|
| `5167071`/`1e38b86` (prior session) | Demo helper + `sqlLocal` deviation | — |
| `603c761` | `seed-demo.ts` — draft event, 6 attendees, operator+practitioner | Ran twice, byte-identical, 6 registrations, zero dup-key errors |
| `4723b83` | `reset-demo.ts` — teardown+reseed+chain-verify | 3× back-to-back, chains clean, no orphans |
| `3e6f390` | `render-emails.ts` — confirmation+reminder HTML | QR data-URI decodes to valid PNG, code matches seed, no leftover `cid:` |
| `6daa7a7` | `ledger-demo.ts` — trust-moment (post/block/detect) | All 6 checkpoints pass on a fresh seed |
| `4ed6abf`/`8f1142d` | CI replay+tamper script + Actions workflow | Written; **NOT run** — `docker exec` from this sandbox hangs even with Docker healthy (Task 1's own note); needs a real terminal or actual CI |
| `a859cbb` | `seed.sql` grant-restore | Fixed Supabase CLI 2.109.1's default-priv gap; Hard Rule 11 reverified intact |
| `8c31b2a` | `set_staff_role()` **LIVE Seoul migration** | RED→GREEN on live; caught+fixed a real bug (column-revoke was a no-op against a table-level grant) |
| `e2730e9` | Grant hygiene + `mustDelete` **LIVE Seoul migration** | Closed `pseudonymise_user`'s anon/PUBLIC exposure (the exact thing `get_advisors` flagged); fail-loud cleanup wrapper surfaced and fixed 2 real 6-day-old orphan rows on live |
| `0cddd2e` | Fixed a real demo-breaking timing bug | Found by actually driving the full run-sheet in a browser (Task 6) — `seed-demo.ts`'s 45-min offset made registration close before it ever opened |

Plus a Supabase CLI upgrade 2.75.0→2.109.1 (user-approved) that fixed the local gotrue admin-API rejection blocking `seed-demo.ts` entirely.

**Six new memory files** written this session (`~/.claude/projects/-Users-ivan-Eventar/memory/`): the CLI/grant gotcha, the Postgres column-revoke-needs-table-revoke-first gotcha, the React `_valueTracker` browser-automation gotcha, plus updates to existing ones. Worth skimming before the next live-DB or browser-automation task.

## Task 6 — the full run-sheet, actually driven end-to-end (new ground, not in any prior handoff)

This is the first time the [demo run sheet](demo-run-sheet.md) was driven for real in a browser rather than verified script-by-script: fresh seed → worktree demo server (`/Users/ivan/Eventar-demo`, now at `0cddd2e`) on `:3100` → staff magic-link login via Mailpit → publish (Beat 2) → public registration of a 7th attendee with `email_log` confirmed (Beat 3) → self-check-in via pass code with the staff roster showing `1/7 checked in` live (Beat 4) → `ledger-demo.ts`'s full choreography against the same rehearsed event, including its own idempotent "already attended" handling (Beat 5).

**Real bug found and fixed:** `seed-demo.ts`'s 45-minute event-start offset put every fresh fixture already past the check-in-window boundary (`start − CHECKIN_OPEN_MINUTES` = 60 min, unconditional) at the moment of seeding — Beat 3 could never have succeeded before this session, in any prior demo prep, without anyone having actually clicked through the public registration form to notice. Fixed to 90 min (`0cddd2e`); run sheet corrected to match.

**Browser-automation gotchas hit and worked around** (documented in memory, not app bugs): `read_page` with `filter:"interactive"` intermittently needs 1–3 retries post-navigate; a backgrounded/multi-tab session can leave a page's `document.visibilityState` stuck `"hidden"` with zero layout (`resize_window` + `tabs_select` fixes it, or open a fresh tab); `form_input` / synthetic typing can set an `<input>`'s DOM `.value` without React's `onChange` firing because React's `_valueTracker` doesn't get reset — the fix is documented in [browser-react-controlled-input-value-tracker.md](../../../../.claude/projects/-Users-ivan-Eventar/memory/browser-react-controlled-input-value-tracker.md).

## Task 9 — set_staff_role() (LIVE Seoul, `muieupgkpbxpqsrjjwol`)

Role changes on `public.staff` are now definer-only. Two migrations (`20260716150421_set_staff_role`, `20260716150855_staff_role_update_lock_fix`):

- **Real bug caught by the RED→GREEN cycle itself:** the first migration's `revoke update (role) on public.staff` (a column-level revoke) was a **no-op** — `anon`/`authenticated`/`service_role` all hold table-level `UPDATE` (the Supabase platform default `arwdDxtm`), and a column-level revoke can't narrow a table-level grant. Test 3 caught it directly (`old_role` was already the post-mutation value). Corrective migration revokes table `UPDATE` then grants back column `UPDATE` on every column except `role`. New memory: [pg-column-revoke-needs-table-revoke-first.md](../../../../.claude/projects/-Users-ivan-Eventar/memory/pg-column-revoke-needs-table-revoke-first.md) — this will recur on any future single-column lock.
- `seed.sql` updated to re-assert this lock after its own blanket local grant-restore, mirroring the Hard Rule 11 pattern.
- Verified: 4/4 RLS tests green on live; `proacl` probe confirms no PUBLIC entry; `get_advisors` shows only the standard shared WARN every sibling definer function already carries.

## Task 10 — grant hygiene + fail-loud cleanup (LIVE Seoul)

`pseudonymise_user` had a bare PUBLIC grant + explicit `anon` — exactly what `get_advisors` flagged (0028). Closed to `authenticated`-only; `self_check_in`/`require_active_staff` re-asserted in their already-correct states with a self-verifying migration DO block. New `tests/helpers/mustDelete.ts` wraps every real fixture-cleanup delete (swapped into 12 files) so a cleanup failure throws instead of silently leaving orphans — **and it immediately proved its worth**: the one full `pnpm test:rls` run this session surfaced two real ~6-day-old orphan rows (`markattended-owner/nonowner@rls-test.invalid` staff rows, an `RLS Test Org 2 (licence mutations)` org) that had been silently surviving every prior test run. Deleted both live after confirming zero FK references.

**Also found, not fixed (flagged as a separate background task, `task_38c2c7d5`):** `self_check_in.rls.test.ts` and `audited_table_writes.rls.test.ts` aren't safe under vitest's default parallel-file execution — one borrows an arbitrary shared `staff` row via `.limit(1).single()` with no `ORDER BY`, the other asserts an *exact* `audit_events` row count. Both pass 100% in isolation; both can fail when the full suite runs concurrently against live. Pre-existing, unrelated to this session's changes — genuinely out of scope for a grant-hygiene task.

## What's NOT done — read before claiming Milestone A shipped

1. **Task 12 (graph refresh) — blocked on account session limit, resets 6:10pm HKT today.** Three attempts this session, all killed by session-limit/connection-drop failures (not content problems — the one chunk that DID complete, 7 of 28 changed docs, extracted cleanly and is now cached). `graph.json`/`manifest.json`/`GRAPH_REPORT.md` are untouched (still show Jul 8) — confirmed clean abort each time, per the [partial-update-manifest trap](../../../../.claude/projects/-Users-ivan-Eventar/memory/graphify-partial-update-manifest-trap.md). **Do not merge a partial result if you retry and hit the same wall again** — abort cleanly, same as this session did three times.
2. **Task 13 (phase-completion protocol) — NOT RUN.** Per CLAUDE.md this is mandatory before Milestone A can be called done: separate dev-lens agent, separate user-lens agent, `everything-claude-code:security-reviewer` specifically on Tasks 9–10's live grant changes, and a backtest (rerun Task 6's beats + Task 7's CI script for real). This session's own verification (RED/GREEN tests, live ACL probes, the browser rehearsal) is real evidence but is **not a substitute** for the formal three-lens review — it's what the reviewers will check, not the review itself.
3. **Task 14 (close-out) — this handoff is a pause-point, not the formal close-out.** PROJECT_STATE.md's Milestone A block and the vault's `CPD Roadmap` status line still need updating, but only after Task 13 passes — don't write "SHIPPED + PHASE-COMPLETION PROTOCOL PASSED" language until it actually has.
4. **CI harness (Task 7's script) is written but never executed anywhere.** It needs either a real terminal (not this sandbox — `docker exec` hangs from nested child processes here even with a healthy Docker) or an actual GitHub Actions run once pushed.

## Operational notes for the resumer

- **Local stack:** healthy as of this session's end (Docker restarted + Supabase CLI upgraded to 2.109.1 mid-session — see `eventar-local-stack-cli-2109-grants.md`). `demo-local` launch config exists in `.claude/launch.json`; worktree `/Users/ivan/Eventar-demo` is checked out at `0cddd2e` (`git -C /Users/ivan/Eventar-demo checkout --detach <sha>` after any future script commits).
- **A second Claude Code session (Opus) was active in this same repo for part of this session**, committing frontend design-exploration docs (`bd6495b`, `52fa96a`, `5a1d56d`, `99e5de8`, `fc13240`, `06e0106` — all `docs(design)`, no code, unrelated area). Worth being aware of if you see commits you don't recognize — check `git log` before assuming drift.
- **`pnpm test:rls`: already run once this session** (Task 10's exit gate) — don't run the full aggregate suite again without a good reason; use scoped single-file runs (`RLS_TESTS=1 pnpm exec vitest run tests/rls/<file>`) for any further iteration, per the standing socket-exhaustion caution.
- **Live Seoul migration list:** two-sided through `20260716233440_grant_hygiene_residual_public` (69 migrations). Verify with `list_migrations` before adding more.
- **`scripts/demo/dev-local.sh`** now correctly serves the current schema; pass code / event IDs printed by `seed-demo.ts` are fresh per-run (by design — see the timing-fix commit).

## Ivan's inputs outstanding (carried from `handoff_12072026.md`, still open)

1. `[PRICE]` in `docs/collateral/one-pager.md` (+ email/phone in the outreach draft).
2. First internal-meeting date (Milestone B / E1).
3. D0 deploy flip (gates Milestone B's deploy track).
4. Push the commit backlog (13 new commits this session, all unpushed).
5. HKCP vs HKAM: `ledger-demo.ts` uses HKAM (Hong Kong Academy of Medicine) since HKCP isn't in the seeded accrediting bodies — flagged in commit `6daa7a7`, easy to swap if HKCP gets seeded.
