# Handoff — 2026-09-06 (v2) — Organizer programme home: design locked, build not started

**Scope of this session:** design/planning only. No dashboard rebuild code was written. This is a planning handoff, not a shipped-feature handoff — do not read it as "the programme home is done."

## What actually changed in the working tree

Four files, small diffs, all **uncommitted**:

```
 M app/dashboard/page.tsx          | 19 ++++++++++++++++++-
 M components/shell/StaffShell.tsx | 35 +++++++++++++++++++----------------
 M lib/auth.ts                     |  3 ++-
 M lib/reviewMode.ts               |  3 ++-
```

This is **only** the P2 "real organisation name" slice from the *original* work instruction (before Ivan's mid-session pivot to the Luma-referenced spec):

- `lib/auth.ts` — `Staff` type gained `organisation_id: string | null`; `requireStaff()` selects it.
- `lib/reviewMode.ts` — `REVIEW_STAFF` fixture and `resolveReviewStaff()` select updated to match the new column.
- `components/shell/StaffShell.tsx` — accepts an optional `org?: { name: string } | null` prop, renders it in the top-bar workspace label with the old "Organiser workspace" fallback preserved; `BrandMark` now gets `href="/dashboard"` in the staff shell (was `/`, which had been ejecting staff to the marketing site — fixed per the component's own docblock, which already specified this).
- `app/dashboard/page.tsx` — added the org-name lookup (`organisations.name` via `staff.organisation_id`, non-fatal on error) and passes it to `StaffShell`.

**None of this reflects the locked design.** The locked plan removes the top bar entirely and moves the account chip to the sidebar bottom-left — so `StaffShell.tsx`'s current top-bar org-name rendering is about to be deleted, not built on. Flagging this explicitly so the next session doesn't assume the top-bar work is a foundation to extend.

No `DashboardWorkstation.tsx` changes were made. No migration exists. No tests were added or run this session.

## What was actually produced this session

1. **A locked execution plan**: [`~/.claude/plans/work-instruction-organizer-swirling-peacock.md`](file:///Users/ivan/.claude/plans/work-instruction-organizer-swirling-peacock.md). This is the authoritative spec for the next build session — every layout value, animation curve, copy string, and file path is written out. Read it in full before touching code.

2. **A full-fidelity HTML mockup**, iterated live with Ivan across ~10 rounds: `/private/tmp/claude-501/-Users-ivan-Eventar/fdca81ac-56cd-4961-80bd-e504d741bc2c/scratchpad/programme-home-decisions.html`, published as an Artifact at `https://claude.ai/code/artifact/6939fb72-9f8a-4e4f-9f27-216bdaa2d691`. **A background notification just reported this artifact was republished elsewhere (version `1788661243-261b`) — this session's local copy is stale.** If a future session needs the mockup's current content, re-read the artifact URL rather than trusting the scratchpad path (which is session-scoped and may not exist in a fresh session anyway).

## Sequence of decisions this session (so the "why" isn't lost)

1. Started executing the **original** work-instruction (metrics-first restructure into Hero → Attention → Calendar segments → Pulse → Manage, `format`/`event_type` chips debated as options A/B/C).
2. Mid-build, Ivan interrupted with a **second, larger work instruction** referencing Luma's community-calendar layout (`luma.com/spacexai-community`) as the structural model — cover image, org avatar, Events title with view-mode icons, filter chips, agenda + month calendar, `Upcoming | Past` segment. This explicitly superseded the first instruction's layout (not its underlying data/actions).
3. First attempt at translating Luma's layout was **built on a bad `WebFetch` read** (wrongly concluded the month calendar was per-view, hidden in compact mode) — Ivan caught this by pasting the actual Luma page HTML source directly, which corrected the record: calendar is present in **both** card and compact views, segment toggle sits **under** the calendar card, chips are **cities** in Luma's own product but the work instruction wants **event formats** (an intentional divergence, not an error).
4. Full mockup was then rebuilt against the literal HTML Ivan pasted, iterated through: top-bar removal + account-chip-to-sidebar-bottom-left, background unification, divider removal, padding increases (+30px then +40px more), card-view posters, and finally click-to-expand modal behaviour — which Ivan specified by name: `docs/ui-port/`, commit `2a64e184`, `components/event-discovery/ExpandableEventCard.tsx`. That component's exact animation (`translateY(16px) scale(0.96) → normal`, 220ms, `cubic-bezier(0.2, 0.8, 0.2, 1)`, Web Animations API) is now the locked spec for the modal — reuse the component, do not reimplement the curve.
5. Plan file rewritten from scratch to reflect the Luma-referenced layout as the sole design of record. The original three-question decision doc (format-naming, month-calendar-scope, chip-content) is superseded by the plan's "Locked decisions" section.

## What the next session must do

Follow `~/.claude/plans/work-instruction-organizer-swirling-peacock.md` in full. In short:

1. Migration: `events.format` (`text` + `CHECK`, 7-value enum) — SQL is written out in the plan.
2. Zod + `NewEventForm.tsx` — add a "Format" field (distinct from the locked "Event type" agenda-block label at `NewEventForm.test.tsx:443` — do not rename that one).
3. `app/dashboard/page.tsx` — extend the events select (`format`, `hero_image_url`, `city`, hosts join); keep the org-name lookup that's already in-flight, but note its StaffShell *consumption point* changes (see next item).
4. `components/shell/StaffShell.tsx` — **delete the top bar** (undoing the current in-flight top-bar org-name rendering), move the account chip to the sidebar bottom-left (exact JSX in the plan), unify sidebar/main background to `--ground`, remove the border between them. `org` prop is still useful — just rendered in the new `<h1>` on the programme page body, not the (now-deleted) top bar.
5. `components/dashboard/DashboardWorkstation.tsx` — full restructure per the plan's six-section breakdown (Programme header → Needs attention → Events [agenda + calendar] → Pulse → Manage). Reuse `ExpandableEventCard.tsx` verbatim for the click-to-expand modal.
6. Run the full phase-completion protocol: static gates, dev-lens + user-lens review (separate agents), RLS suite, live walkthrough, and the format-field backtest (create → verify chip count → delete → verify count drops) described in the plan's Verification section.

## Operator flags carried forward (nothing new this session, restated for continuity)

- Supabase dashboard **"Secure email change"** toggle on Seoul (and Singapore when provisioned) must be ON before any change-email flow reaches a public route. Not code, not touched this session.
- Migration `20260906000000_events_category_new_taxonomy.sql` (from the prior session, per `PROJECT_STATE.md`) still needs a push to Seoul — unrelated to this session's work, just still outstanding.

## Not run this session

- Static gates (no code change large enough to warrant it — the four in-flight files are pre-existing, already-reviewed edits from before the pivot).
- Two-agent phase review (nothing to review — no feature landed).
- Backtest (nothing to backtest).
- `pnpm test:rls` (no schema change happened in-repo; the migration is written in the plan but not applied).

## Session close

Uncommitted, as always per standing instruction pending Ivan's go-ahead. Four files touched (org-name plumbing, pre-pivot), one plan file, one artifact. Next session's job is implementation against the locked plan, not further design.
