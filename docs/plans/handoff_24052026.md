# Handoff — 2026-05-24 (end of session)

## Session outcome

Short session (no new phase). Four workstreams, all local on `main`, **not pushed**:

### A. Phase-completion-protocol full exercise on Phase 3.5 + Phase 4 (no commits — meta)

First real use of the protocol added in workstream D of the 23052026 session. Dispatched two SEPARATE review agents in parallel against the combined Phase 3.5 + Phase 4 surface (22 non-docs files changed since `78a010b`):

- **Dev-perspective** (`feature-dev:code-reviewer`) — found 5 issues, ranging from a PII enumeration oracle on `/checkin/confirm?code=` (real, design-level) to a CSV count↔rows race window. Also confirmed the closure-bug fix in `RosterClient.tsx` is complete and that no sibling stale-closure bugs exist.
- **User-perspective** (`general-purpose`, no dedicated user-review agent in plugin set) — probed 11 routes via curl, found 18 issues across confusing/ambiguous/polish bands. One was overstated on re-read (claimed EventCard disappears on success — it doesn't, it's a sibling Server Component) and was demoted.
- **Backtest** — me: HTTP-probed the four `/checkin/confirm` failure-mode branches after the fix landed. Real-DB backtest items (3 surfaces) deferred to next session as listed below — Supabase Dashboard SQL editor is the verification path since `db push` is still blocked by migration drift.

The two lenses surfaced different bug classes; the protocol earned its keep on first use. The overstated finding is also a useful data point — confirms that "two SEPARATE agents" doesn't mean "trust both blindly" but "cross-check before fixing."

### B. Seven surgical fixes to Phase 3.5 + Phase 4 surfaces (8 commits, `84bb9a6`…`76689be`)

Atomic-commit-per-fix discipline. Gates re-run after every commit that touched code. New test count: 87 → **95** across **11 files** (+8 in `lib/cameraError.test.ts`). Routes unchanged at 11.

| # | Fix | Lens | Touches |
|---|---|---|---|
| 3 | `markAttended` toast formats `check_in_at` in event timezone | dev + user (convergent) | action returns structured `alreadyAttendedAt`; client formats with `formatInTz` already in scope |
| 6 | `selfCheckIn` distinguishes "already checked in" from "registration no longer valid" | user | mirrors `markAttended`'s two-query distinguish pattern |
| 4 | `exportRegistrantsCsv` uses single `rows` query for both gate and metadata | dev | drops separate `count` query, eliminates the count↔rows race window |
| 5 | `/checkin/confirm` splits "no code" from "bad code" landing screens | user | new `<NoCode />` branch with action-oriented copy |
| 7 | Scanner `humanizeCameraError(e)` extracted + tested | user | maps 5 standard DOMException names to friendly copy with action; pure function, 8 unit tests |
| 8 | "Open check-in" link drops `target="_blank"` | user | single-tap nav on event-day tablet |
| 9 | Stale "registered for" framing on self-checkin success | user (demoted from 🔴 to 🟡 on re-read) | moved personal greeting into `ConfirmButton` so it hides on success; EventCard remains visible above both states |

### C. Phase-8 deploy gates documented (1 commit, `b7d0633`)

Two findings from the dev review are too design-shaped to land as code fixes and got promoted to documented deploy gates instead of silent debt:

- **PII enumeration oracle** on `/checkin/confirm?code=` — admin-client GET reveals `full_name` + `status` + `check_in_at` for any valid 4-char code. ~923K namespace per event × no rate limit = full registrant list extractable in ~2.6h at 100 req/s.
- **`Math.random()` for registration codes** — codes are bearer tokens; statistically biased distribution compounds the enumeration risk above.

Vault `10 — Architecture/Security + Robustness.md` got a new §14a with three mitigation options for the PII oracle and the trivial CSPRNG swap. Project `CLAUDE.md` got a new "Phase-8 deploy gates" section listing these two plus the two carried over from the 23052026 handoff (host-header spoofing, migration drift) — so future sessions can't miss them when planning the deploy.

### D. Previous-session handoff consolidation landed (1 commit, `beb7628`)

The consolidation edit drafted at the start of this session (7 workstreams → 4 in `handoff_23052026.md`, fixed `30 → 31` commit count, appended the missing `ed4f48a` row) was sitting unstaged from prior turn. Landed as docs-only commit, no gates re-run.

---

## Repo state at handoff

- **Branch:** `main`
- **Status vs origin:** **9 commits ahead, NOT pushed**
- **Working tree:** only `.claude/launch.json` modified (pre-existing IDE-local, never staged)
- **Gates:** `tsc` clean · `eslint .` clean · **vitest 95/95** across **11 files** · **next build 11 routes**
- **Routes (11, unchanged):** `/`, `/_not-found`, `/auth/callback`, `/checkin/confirm`, `/dashboard`, `/events/[id]`, `/events/[id]/checkin`, `/events/[id]/edit`, `/events/[id]/poster`, `/events/new`, `/login`

### Commits this session (oldest → newest, 9 total)

```
# B — Seven surgical fixes
84bb9a6 fix(checkin): format markAttended duplicate-scan toast in event timezone
47027d1 fix(self-checkin): distinguish already-attended from missing registration
dedd4fa fix(csv-export): eliminate count↔rows race in exportRegistrantsCsv
face35f fix(self-checkin): split "no code" from "bad code" landing screens
40ba28f fix(scanner): humanize camera errors with action-specific guidance
547714f fix(checkin): open check-in in the same tab from edit page
76689be fix(self-checkin): drop stale "registered for" framing after success

# C — Phase-8 deploy gates documented
b7d0633 docs(security): backlog PII oracle + CSPRNG as Phase-8 deploy gates

# D — Previous-session handoff consolidation
beb7628 docs(handoff): consolidate workstreams 7→4, fix commit count 30→31
```

### Files added this session

- `lib/cameraError.ts` — pure mapper, 31 LOC
- `lib/cameraError.test.ts` — 8 tests covering all 5 DOMException names + fallbacks + non-Error inputs

### Vault changes this session

- `10 — Architecture/Security + Robustness.md` — new **§14a Open security debt (Phase-8 deploy gate)** with Risk 1 (PII enumeration oracle, 3 mitigation options) + Risk 2 (CSPRNG swap, trivial fix). Located between §14 (Backups) and §15 (Quick-reference checklist).

### Schema changes this session

None.

---

## Open items (carry into next session)

### Highest priority — real-DB backtest of fixes that touched mutation surfaces

Per the phase-completion-protocol, the three fixes below need a real-system round-trip before they're truly "done." Supabase Dashboard SQL editor is the verification path (CLI `db push` blocked by migration drift, see B below).

1. **A1: `markAttended` toast formatting** — staff scan-or-mark twice on same code; toast should read `Already attended at 09:14 (Asia/Hong_Kong)` not raw ISO. Find an attended code with `select registration_code from public.registrations where status = 'attended' limit 1`.
2. **A2: `selfCheckIn` distinguished message** — open `/checkin/confirm?code=<attended-code>` and click "I'm here"; error should be `You're already checked in.` not the conflated `Already checked in or code not recognised.`
3. **A3: `exportRegistrantsCsv` row count** — export CSV on a closed/at-capacity event; metadata `Total registered,N` should equal data-row count below it.

### Phase-8 deploy gates (from project `CLAUDE.md`)

All four MUST be resolved before Phase 8 (Vercel deploy → public internet):

1. **PII enumeration oracle on `/checkin/confirm`** — pick a mitigation from vault §14a Risk 1 (POST-gate the reveal / per-IP rate limit / cryptographic rotation).
2. **CSPRNG for registration codes** — `crypto.randomInt()` swap in `lib/registrationCode.ts`. Trivial. Backward-compatible.
3. **Host-header spoofing on QR origin** — carried over from 23052026 handoff. Add `NEXT_PUBLIC_SITE_URL`; fall back to headers for local dev.
4. **Supabase migration history drift** — carried over. 3 local-only vs 4 remote-only mystery rows in `supabase_migrations.schema_migrations`. Reconcile via `db pull` (recommended) or `migration repair --status applied` per mystery row.

### Operational hygiene (anytime)

5. **Supabase Advisors re-run after Phase 4** — Dashboard → Database → Advisors → Security Lints. Phase 4 migration claimed "no new policies" — verify no new lints surfaced from the column additions or the migration's `do $$ ... $$` backfill block.
6. **9 unpushed commits** — `git push` when ready. None are in a state that requires further local work.

### Strategic / known debt (unchanged from 23052026)

7. **Cross-event scan on staff tablet succeeds intentionally** — manager scanning another organiser's code gets success toast with the other event's title. Documented in `markAttended` comments + Phase 4 vault note.
8. **No rate limit on `selfCheckIn`** — also covered by deploy gate #1 above (the PII oracle's rate-limit option, if picked, would close this).
9. **`createBrowserClient` called per-render** — `supabaseBrowser()` constructs fresh client inside `useEffect`. Not blocking; revisit if Phase 5/6 spawns more Realtime callers.
10. **MVP-vs-commercialisation fork: deferred until after Phase 6.**

---

## Vault state at handoff

- `10 — Architecture/Security + Robustness.md` — §14a added (see above)
- All other vault notes unchanged from 23052026

The Phase 4 vault note (`20 — Roadmap/Phase 4 — Check-in tablet.md`) is now slightly out of date in that it doesn't reflect the post-review fixes from this session. Worth a paragraph addendum next time someone touches it — or leave until Phase 5 lands and update both then.

---

## Next session pointer

Per the deferred-fork decision from 23052026, the next phase is **Phase 5 — Survey flow** (PRD §4.5; smaller than Phase 4, no Realtime, no camera).

Three reasonable starts:

- **Phase 5 brainstorm** — `superpowers:brainstorming` for the design, then per `superpowers:writing-plans` for the executable plan. PRD spec is tight so brainstorming should be short.
- **Backtest sweep + deploy-gate work** — knock out A1–A3 (5 min in the browser + SQL editor), then pick one of the four Phase-8 deploy gates to land before Phase 5. CSPRNG (#2) is the smallest; migration drift (#4) is the biggest unblock.
- **Push first** — `git push origin main` to get the 9 local commits into remote before any new work starts. No reviews block this; CI doesn't exist yet (Phase 8).

User's preference at end of 23052026 was: continue original phased roadmap → Phase 5 next. The two-lens-review pattern is now grooved enough that the Phase 5 cycle should follow it from the start rather than retrofitting.

The user's `pnpm dev` is **always running on port 3000** — never start another dev server.
